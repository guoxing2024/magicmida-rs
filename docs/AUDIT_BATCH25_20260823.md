# 总指挥审计裁决 — Batch 25

**审计日期**：2026-08-23  
**实际审计 HEAD**：`639eee362d69c1cbb3fc0852438bb6e461d506c9`（短哈希 `639eee3`）  
**审计范围**：`62ed608..639eee3`  
**范围实测**：5 commits、8 unique paths、+465/-8；全部为 `docs/` 或 `docs/fixtures/`，`crates/` 生产代码零修改。  
**性质**：只读审计裁决；不实现 Walker/V2 runtime/CLI，不授权 LIVE-4。

## 1. 总裁决

| 工单 | 裁决 | 原因 |
|---|---|---|
| WO-2501 thunk7 本机 harness | **REJECTED — P0 OPEN** | `rsp_probe` 结构偏移与汇编写入偏移不一致，call-pre-rsp 检查为零值假阳性；另有“60B fixture + probe”长度/字节比较错误。 |
| WO-2502 V2 arithmetic | **CONDITIONALLY ACCEPTED — offline fixture only** | checked-add 路径和 16/16 ASan 证据可复核；回绕分支未直接覆盖，不能升级为 runtime/Windows 证据。 |
| WO-2503 evidence rebinding | **REJECTED — P1 OPEN** | `D:\Temp\evidence_head_2503.txt` 仍明确为 `62ed608`；证据文档未绑定实际最终 HEAD `639eee3`。 |
| WO-2504 schema audit | **CONDITIONALLY ACCEPTED — source facts only** | 结论仍显示 v2/digest/Walker 未实现，但审计基线仍是 `62ed608`，需重绑最终树。 |
| WO-2505 caller audit | **CONDITIONALLY ACCEPTED — source facts only** | 生产 caller 为零的事实可由 docs-only 后续变更复用，但不能称为 `639eee3` 最终树审计。 |

**implementation gate：FAIL / HOLD。**  
**Walker runtime/CLI：NOT DISPATCHED。**  
**Windows SEH/VEH live test：NOT AUTHORIZED。**  
**LIVE-4：NOT AUTHORIZED。**  
**GTO/Oreans vault、manifest：未触碰。**  
**自动重试：禁止。**

## 2. P0 事实核对：WO-2501 必须返工

### 2.1 `rsp_probe` 偏移错位，当前 PASS 不能证明 call-pre-rsp

原始测试源 `D:\Temp\thunk7_final_test.c`：

- L12-L16 的 `Args10` 只有 `fn_ptr + arg0..arg6 + rsp_probe`，因此 `rsp_probe` 位于 **`+0x40`**；该结构没有 `+0x40 reserved` 槽。
- L22 将结构清零；L36-L39 读取 `args.rsp_probe & 15` 并据此判 PASS。

原始汇编 `D:\Temp\thunk7_final_full.asm` L19 的探针却是：

```asm
49 89 63 48    ; mov [r11+0x48], rsp
```

因此探针写入 **`+0x48`**，测试读取 **`+0x40`**。当前读取的字段没有被探针写入，清零后低 4 位为 0 会天然通过。`D:\Temp\thunk7_final_stdout.txt` 的 `call pre-rsp mod 16 = 0` 不能作为有效证据。

**结论**：callee entry 对齐与 7 参数回传可单独成立；call-pre-rsp 检查未成立，WO-2501 P0 保持 OPEN。

### 2.2 60B production fixture 与 test-only probe 被错误拼接

`docs/fixtures/WO-2301-thunk7-fixture.h` 的 `THUNK7_CODE` 是 60B，`call rax` 位于 `0x35`。`D:\Temp\thunk7_final_full.asm` L19 插入 4B probe 后，`call rax` 实际移到 `0x39`；该测试 thunk 总长为 **64B**（60B production code + 4B probe），不是 65B。

独立读取 `D:\Temp\thunk7_final_full.obj`：

- SHA-256：`BE1042DA3AA6947F539503E6655B90126E57292F1B7F3D97359DE6070C171A20`
- `.text$mn` 前 60B 结尾为：
  `... 4C 89 54 24 30 49 89 63 48 FF D0 48`
- 这不是 `THUNK7_CODE` 的前 60B；它是“55B production 前缀 + 4B probe + 1B call”的跨边界片段。

因此 `docs/AUDIT_EVIDENCE_BATCH24_20260823.md` L52-L58 的“前 60B = fixture THUNK7_CODE + probe，三者完全一致”表述不成立；L46 的“fixture-exact 65B”也不成立。

### 2.3 必须保留的边界

即使 WO-2501 修复，local x64 harness 也只能证明本机调用约定与离线机器码执行；不能证明 WPM、CreateRemoteThread、target-local SEH/VEH 链、runtime loader 或 LIVE-4 行为。

## 3. P1/条件接收复核

### 3.1 证据树仍停在旧 HEAD

`D:\Temp\evidence_head_2503.txt` 内容为：

```text
HEAD=62ed608652c9168913c2cb19671b24455bebeb16
```

`docs/AUDIT_EVIDENCE_BATCH24_20260823.md` L3、L81、L90-L101 仍以 `62ed608`/`2b3e680` 为绑定对象；当前实际 HEAD 是 `639eee3`，之后还有 `12bf21e`、`1da43df`、`639eee3` 三个 docs-only 提交。故不能把旧证据称为最终 HEAD manifest。

### 3.2 Arithmetic 结论只能条件接收

`docs/AUDIT_V2_ARITHMETIC_BATCH24_20260823.md` 的 16 用例及 `D:\Temp\hostile_asan_detail.txt`（16/16、零 ASan 报告）可作为离线 fixture 证据。追加的 off+k 可达性分析没有把 checked-add 回绕分支本身变成测试覆盖，也没有生产 runtime 绑定；因此维持 offline-only，不阻塞 docs-only 证据重绑，但不得升级为 V2 runtime PASS。

### 3.3 Schema/caller 事实可复用但须重绑

当前生产事实保持：

- `crates/antidebug-runtime/src/exports.rs:239` 仍为 `adr4-foundation-unbound`；
- `WalkerExecute`、`MidaAntidebugInitializeV2`、生产 `ThunkArgs7`/7-arg thunk、V2 attestation/digest consumer 均未接入；
- `crates/cli/src/unpacker/runtime_loader.rs` 的 `MidaExports`/wanted exports 仍为既有 3 导出；
- 协议生产 caller 仍为零，仅 lib re-export 与测试命中。

这些事实不等于实现通过；需以 `639eee3` 重新登记。

## 4. 下一批出口门

在 WO-2601 P0 关闭前，不得派 Walker runtime/CLI，不得起草或签发 LIVE-4，不得做远程/目标样本实测。

**本裁决状态：Batch 25 HOLD，进入 Batch 26 返工。**
