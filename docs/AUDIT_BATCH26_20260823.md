# 总指挥审计裁决 — Batch 26

**审计日期**：2026-08-23  
**实际审计 HEAD**：`928047face61cc343137938d5e5610c05a73a8a1`（短哈希 `928047f`）  
**Batch 26 范围**：`639eee3..928047f`  
**git 实测**：2 commits、7 unique paths、`+405/-8`；全部为 `docs/` 或 `docs/fixtures/`，`crates/` 生产代码零修改。  
**性质**：只读审计裁决；不实现 Walker/V2 runtime/CLI，不授权 LIVE-4。

## 1. 总裁决

| 工单 | 裁决 | 说明 |
|---|---|---|
| WO-2601 | **CONDITIONALLY ACCEPTED — local/offline core only** | `ThunkArgs7Probe` 偏移、sentinel、三项本机检查和 test 64B 原始流可复核；但 exact-byte 证明文档的 production 提取公式错误，需修正文档后才能完全关闭。 |
| WO-2602 | **REJECTED — P1 OPEN** | 证据主体绑定 `639eee3`，补充绑定只到 `97d6914`；实际最终 HEAD 已是 `928047f`。 |
| WO-2603 | **CONDITIONALLY ACCEPTED — fixture only** | checked-add/ASan 结论可复用；最终树基线仍需更新为 `928047f`。 |
| WO-2604 | **CONDITIONALLY ACCEPTED — source facts only** | v2/Walker/digest 未实现的事实可复用；必须重登记 `928047f`。 |
| WO-2605 | **CONDITIONALLY ACCEPTED — source facts only** | 生产 caller 为零的事实可复用；必须重登记 `928047f`。 |
| WO-2606 | **REJECTED — P1 OPEN** | 当前树仍保留可被误读为有效 PASS 的旧证据行和“fixture-exact”表述，未完成全仓 stale-claim scrub。 |

**implementation gate：FAIL / HOLD。**  
**Walker runtime/CLI：NOT DISPATCHED。**  
**Windows SEH/VEH live test：NOT AUTHORIZED。**  
**LIVE-4：NOT AUTHORIZED。**  
**GTO/Oreans vault、manifest：未触碰。**  
**自动重试：禁止。**

## 2. WO-2601：核心本机检查有效，但文档闭环未完全成立

### 2.1 已确认有效的部分

实际源文件：

- `D:\Temp\thunk7_final_test.c`
- `D:\Temp\thunk7_final_full.asm`
- `D:\Temp\thunk7_final_full.obj`
- `D:\Temp\thunk7_threecheck_stdout.txt`

当前源布局明确为：

```text
reserved  +0x40
rsp_probe +0x48
sizeof    0x50
```

测试使用非零 sentinel `0xDEADBEEFCAFEBABE`，运行后输出真实 rsp 值而不是零值；`reserved` 保持不变。三项输出分别报告：

```text
arg pass-through       PASS
callee entry alignment PASS
call-pre-rsp           PASS
```

本机运行 `D:\Temp\thunk7_final_test.exe` 也得到 `EXIT=0`。这部分只能接收为 **LOCAL x64 / offline evidence**，不能外推为远程 target、WPM、CreateRemoteThread、SEH/VEH 或 LIVE-4 PASS。

### 2.2 exact-byte 证明仍有一处硬错误

`docs/WO-2601-thunk7-probe-closure_20260823.md:90-93` 写成：

```text
production 60B = obj 前 56B + FF D0 + obj[60..63]
```

这个表达式的长度和切片语义不成立。实际 `.text$mn` 流是：

```text
0x00..0x34  : production 前缀，共 0x35 = 53B
0x35..0x38  : test-only probe，共 4B
0x39..0x3F  : call/add/ret，共 7B
```

正确的生产提取应明确写成：

```text
production = obj[0x00..0x35) || obj[0x39..0x40)
           = 53B + 7B = 60B

test       = obj[0x00..0x40)
           = 64B
```

独立复核结果：

```text
production SHA = 9B6F4A7A138B3C4C5523CEDD047745C96AA83CA01614BEB703E4994DA2E1F017
test SHA       = 01DC2017D8825EFD7E1C3FBE186C2FACF36FB22F2338C493C422E659476E17AE
```

实际字节和哈希是对的，但文档切片公式是错的。机器码与文字必须逐字节一致，所以 WO-2601 只能条件接收，不能无条件关闭。

## 3. WO-2606 stale-claim scrub 未完成

当前 `docs/AUDIT_EVIDENCE_BATCH24_20260823.md` 仍有以下未清理内容：

- L46：仍写 `thunk7_final_test.c` 为 `fixture-exact 65B`；
- L52-L58：仍保留“前 60 字节 = fixture THUNK7_CODE + probe，三者完全一致”的错误 PASS 叙述；
- L67-L68：仍把当前有效来源写成 `thunk7_v2`，而本批已声明唯一有效来源是 `thunk7_threecheck`。

这些行虽然后面有修正说明，但同一文档同时存在旧错误 PASS 和新修正，审计者仍可能误读。应保留历史记录，但必须显式加 `SUPERSEDED / INVALID EVIDENCE` 标签，不能让错误表格继续以 PASS 形式存在。

此外，`docs/fixtures/WO-2301-thunk7-fixture.h:3-7` 仍写“fixture-exact bytes”并把 call-pre-rsp 描述成生产 fixture 内探针；生产 60B 实际不含 probe。应改为“production byte table + test-only extension separately verified”。

## 4. 最终 HEAD 绑定问题

当前 HEAD：

```text
928047face61cc343137938d5e5610c05a73a8a1
```

但 `docs/AUDIT_EVIDENCE_BATCH25_20260823.md`：

- L4-L30 的主体证据仍绑定 `639eee3`；
- L73-L79 的补充只绑定 `97d6914`；
- 当前实际 HEAD 为 `928047f`，仍缺最终提交后的独立 manifest。

此外，交付报告写 Batch 26 为 `+406/-8`，git 实测为：

```text
+405/-8
```

原因是首提交 `+393/-8`，补充提交实际净变更 `+12`，不是 `+13` 的净统计。

## 5. 生产实现门禁

当前生产代码仍未接入：

- `WalkerExecute`；
- `MidaAntidebugInitializeV2`；
- production 7-arg thunk；
- Walker protocol production caller；
- V2 attestation/digest consumer；
- orphan/unconfirmed acceptance。

且：

```text
crates/antidebug-runtime/src/exports.rs:239
adr4-foundation-unbound
```

仍未替换。故三状态继续为：

```text
readiness          = ACCEPTED
implemented        = NOT IMPLEMENTED
acceptance allowed = NOT ALLOWED
```

---

**本裁决状态：Batch 26 HOLD，进入 Batch 27。**
