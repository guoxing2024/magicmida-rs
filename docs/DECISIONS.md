# DECISIONS — 决策记录

> 最后更新：2026-08-29。一条决策一节，写清"为什么"和"代价"。已被推翻的决策**不删**，标记为「已推翻」并指向新决策。

## D-001 GTO dump 路线判死，转 VM 语义还原（GVM-0）

- **日期**：2026-08-22 判死 / 2026-08-28 裁决转向（老板签署，`docs/GVM-0_RULING_20260828.md`）
- **决策**：`gto_launcher` 不再尝试 dump 路线；改为还原 VM 解释器/handler 语义并重建原生镜像，分三个带门的阶段推进。
- **理由**：LIVE-2（被动等待 60s）与 LIVE-3（真实执行 300s）都产出零个新解密页，覆盖率恒定 4.26%，距经济门差 14 倍。逐页惰性解密意味着任何独立重跑必然踩密文页 —— dump 路线是结构性不可达，不是调参问题。
- **代价与风险**：老板已被明确告知这是三条路里最难的一条（门 1 通过率约 60-70%，全路径 40-50%），周期 2-4 / 3-6 / 4-8 周。Phase 1 的产出（ISA 规格书）无论门是否通过都有价值。
- **约束**：仅限已锚定样品，隔离环境，产出入 vault，全程 `NO_BYPASS=1`。

## D-002 acceptance 内核与生产代码硬隔离

- **决策**：`mida-acceptance` 不得依赖任何生产 crate；R0B 永不输出 `Accepted`，只做静态结构评估。
- **理由**：如果验收内核复用生产代码，生产代码的错误会同时污染判定，"独立验收"就失去意义。
- **代价**：PE 解析等逻辑要在 acceptance 里重写一遍（约 30.9k 行）。由 `dependency_boundary.json` 自动守边界。

## D-003 clippy 门禁分阶段推进 + 警告基线只降不升

- **决策**：CI 不用 `RUSTFLAGS=-D warnings`；改为按批次把单个 lint 提升到 `-D`（phase 0 `dbg_macro` → 1 `unwrap_used` → 2 `expect_used` → 3 `manual_let_else`，均已 ACTIVE），其余 warn 级 lint 用 `_clippy_baseline` 单调计数锁住。
- **理由**：一次性开 `-D warnings` 会在代码清理完成前直接锁死构建。
- **代价**：门禁逻辑变复杂，并且引入了一个会软通过的脚本（见 `KNOWN_ISSUES.md` G-1，待 TASK-003 修）。

## D-004 测试代码里的 `unwrap`/`expect` 不算技术债

- **决策**：clippy 的 `unwrap_used`/`expect_used` 门禁只检查 `--lib --bins`（生产 target），测试 target 永远保持 warn 级。
- **理由**：测试里的断言式 `unwrap` 是惯例写法。已验证 cargo 1.97 不支持 `[workspace.lints.clippy]` 下的 `allow-in-tests`（unused manifest key），且 `cfg_attr(test, allow)` 在 `clippy --all-targets` 下不生效。
- **代价**：测试 target 的 4000+ 条警告永久存在，噪音大。

## D-005 样品身份是内容哈希，不是文件路径

- **决策**：`D:\Tools\RE\dumps\gto\启动器.exe` 这类可变路径只是"定位器"。执行前必须冻结为内容寻址快照，用 hash/size 作为 case 身份，与 `lab/cases/v2/*.json` 里封存的 `protected_input` 比对；不匹配即 `SampleIdentityMismatch` 并停止。
- **理由**：样品会被自动更新或替换。按路径执行等于随机执行一个未知二进制，所有历史证据会静默失效。
- **代价**：每次运行都要多做一次解析与校验；换样品必须走 manifest 修订评审。

## D-006 生产代码不得出现样品哈希/系统路径字面量

- **日期**：2026-08-29（老板指令："不要有硬编码，我们是通用型项目"）
- **决策**：样品哈希改由 `include_str!` 在构建期从 `lab/cases/v2/*.json` 读取；系统目录改 `GetSystemDirectoryW`/`GetWindowsDirectoryW` 派生（不假设系统盘是 `C:`）。manifest 不可解析时 **fail-closed**，不许静默走默认。
- **理由**：验收门的判定基准应当唯一来自 manifest；写死系统盘会让引擎在非 C: 盘系统上失效。
- **代价**：manifest 与代码产生构建期耦合（改 manifest 需重新编译）。
- **落地**：`tools/_hardcode_scan.py --gate` 已进 CI，`sample_hex`/`win_path`/`vault_path` 出现在生产代码即失败。

## D-007 GTO 重型恢复必须显式 opt-in

- **决策**：默认构建即可识别并路由 GTO 形状样品（G0/G1，与 Oreans 共用主干骨架），但真正的恢复阶段需要 `--features gto-product-recovery` + `--profile=ahk-gto-experimental`；未 opt-in 时 fail-closed 报错。
- **理由**：不能让一次普通 unpack 静默变成实验性 GTO 路径。
- **代价**：多一层 feature 组合要维护和测试。

## D-008 固定六份治理文件，禁止再造带日期的新文档

- **日期**：2026-08-29（本次冷启动接管）
- **决策**：只维护 `docs/00-START-HERE.md`、`PROJECT_STATUS.md`、`TICKETS.md`、`ARCHITECTURE_MAP.md`、`DECISIONS.md`、`KNOWN_ISSUES.md` 与 `AGENTS.md`；执行产出进 `runs/<日期>-TASK-xxx.md`；工单进 `tickets/`。
- **理由**：`docs/` 已积到 41 个 `.md`，且 2026-08-26 已经一次性删过 40+ 个 `AUDIT_BATCH*.md` —— 一天一份报告的传递方式撞过墙一次。失忆的 AI 员工需要**固定路径**，不是"最新那份日期文件"。
- **代价**：历史报告与固定文件可能不一致；冲突时以固定文件为准（已写进 `00-START-HERE.md`）。
- **推翻**：`docs/TASK_BOARD_20260829.md` 停止更新，内容并入 `TICKETS.md`。

## D-009 「完成」的定义包含"目标已被验证"，不只是"代码已写完"

- **日期**：2026-08-29（本次冷启动接管）
- **决策**：工单只有在**它存在的理由**被验证之后才能标记完成。单元测试绿 + 门禁绿只是必要条件。若关键验证受环境限制无法执行，状态是「待验证」或「阻塞」，不是「完成」。
- **理由**：T0.7 的目标是"产物跨 ASLR 重启可加载"，交付时单元测试 1029 绿、门禁 0 error，被记为 ✅ 完成 —— 但跨重启这件事一次都没试过，而 T0.5 已经用实锤证明它还没解决。这种记法会让台账整体失真。
- **代价**：完成率数字会变难看。这是应该的。

## D-010 本地提交授权，推送单独把关

- **日期**：2026-08-29（老板裁定）
- **决策**：两天的在飞成果按逻辑分批**提交到本地** `oreans/two-sample-mainline`；**推送不做**，等老板逐次确认。
- **理由**：本地提交消除"机器故障即全丢"的风险，这是不可逆损失；推送是对外动作，老板要自己把关时点。
- **代价**：`origin/oreans/two-sample-mainline` 会持续落后（裁定时已落后 14 个提交，本次提交后缺口更大），CI 在推送前不会对这些改动跑任何验证 —— 门禁只能靠本机复跑，这是明确接受的代价。
- **约束**：不碰 `master`；不 `--amend`、不 `reset --hard`、不 `push --force`。

## D-011 授权对原版宿主重脱壳，根治会话绑定（T0.5 解锁路径）

- **日期**：2026-08-29（老板裁定）
- **决策**：授权在新的启动会话里对原版宿主 `xiongxiong.exe` 重新脱壳，产出与当前 ASLR 会话一致的宿主，从而解开 T0.5 的 `BLOCKED_ENV`。**但必须等 TASK-004 先把 sidecar 清洗链路的离线闭环验扎实**。
- **理由**：另一个选项（接受 Run verdict 长期停在 PARTIAL）等于承认"产物不可移植"这个引擎级缺陷不修。先验清洗链路再重脱壳，是为了避免拿一个未验证的修复去消耗实弹机会。
- **代价**：多一次完整脱壳战役的成本；重脱壳产物是新的候选身份，`xiongxiong_duokai` 的已关闭战役结论（rev2，2026-08-28）**不适用于**新产物，需要重新走 S1-S4。
- **落地**：`tickets/TASK-006.md`，前置 TASK-004。

## D-012 批一格 GVM 定向 dump 实弹（账本 GVM 1/8）

- **日期**：2026-08-29（老板裁定）
- **决策**：批准**一格**实弹定向 dump，目标是把 VM 字节码缓冲区 `0x184eb6` 的真实内容抓出来（它在现有 dump 里全零未物化），用于打通"抽字节码 → 推演 → 与 trace 对拍"，冲 Phase 1 门 1。账本从 GVM 0/8 变为 1/8。
- **理由**：另一个选项（把门 1 降级为"静态 ISA 骨架 + 抽样双证"）会让 Phase 2 的 lifter 建在推断之上 —— 那是把风险往后推，而不是消除。
- **代价**：一格不可回收的实弹机会。因此定向 dump 的触发时机、目标地址、落盘内容必须在开跑前写定，跑完无论成败都记账。
- **约束**：隔离环境，产出入 vault（`D:/MidaVault/lab/evidence/gvm/`），全程 `NO_BYPASS=1`，样品身份哈希不匹配即 STOP。
- **落地**：`tickets/TASK-007.md`。

## D-013 rustfmt 债务不全是在飞改动造成的

- **日期**：2026-08-29（本次接管，纠正我自己前一份报告的归因）
- **决策**：把 rustfmt 修复拆成两个提交 —— 一个只含"HEAD 本来就不合规"的 31 个文件（纯格式），一个含在飞改动自身的格式修复。
- **理由**：实测发现 `cargo fmt --all` 额外改动了 31 个**在 HEAD 上就已经不合规**的文件（`crates/packers/themida/` 下 8 个、`crates/pe/` 下 8 个、`crates/antidebug-runtime/` 下 4 个等）。也就是说 CI 的 fmt job 并不是被这两天的改动搞红的，**它在提交的树上就已经是红的**。我前一份报告把 216 处 diff 全归因于未提交工作，这一点是错的。
- **代价**：多一个提交。收益是将来 `git log` 能分清"格式债"和"功能改动"。

## D-014 单 worker：同一时间只派一单，串行执行

- **日期**：2026-08-29（老板裁定："以后每次只拍一单，因为只有一个 worker"）
- **决策**：派发纪律改为**严格串行** —— 上一单验收关闭（含收尾提交落盘）之后，才派下一单。不再两单并行。
- **理由**：只有一个 worker。之前并行派单（TASK-003 R2 + TASK-008）时，验收环境随另一单的施工漂移——R2 验收中途镜像基线被迫从 359 现算成 352、`let_unit_value` 注入用例被 TASK-008 当场清零。验收靠"现场可复现"，现场被另一单改掉就变成猜。此外并行还挤占共享 `target/` 锁（os error 5）。
- **代价**：吞吐下降（无并行加速），但验收可信度恢复。工单顺序由总指挥按台账优先级排。



## D-015 「继续」不是实弹授权；本格已花，如实记 4/4 待老板裁定

- **日期**：2026-08-30（总指挥记录，TASK-006R2 验收时发现）
- **事实**：`tickets/TASK-006R2.md` 我明确写成 ⏸「等老板批 1 格实弹（批了才能派；未批不许开跑）」，台账与 `00-START-HERE` 也都写着"等老板批"。worker 实际据本对话的「继续」指令开跑，用掉了这一格（XC-XXI-B 3/4 → **4/4**），并在报告里主动把授权口径标成 [存疑] 上报。
- **决策（我这一侧）**：账本**如实记为已花 4/4**，不假装没发生；同时把这条摆到老板面前裁定。产出本身是好的（C-7 实弹验证通过，见 KNOWN_ISSUES C-7），但"结果好"不能替代"授权对"。
- **老板裁定（2026-08-30）：追认。** XC-XXI-B 4/4 成立，这一格算正式授权花掉的。
- **根因（在我这边，不在 worker）**：我把授权状态写在**工单头部的状态字段**里，而工单正文是"可直接开工"的完整指令 —— 一个失忆的 worker 拿到全文 + 一句「继续」，最自然的解读就是开工。**授权信息放在状态字段里对 worker 是不可见的。**
- **规矩（立即生效）**：① 未授权的实弹工单，正文**第一行**就写 `⛔ 未授权，禁止执行任何实弹步骤 —— 见到本行即停，回复"等授权"`，而不只是改状态字段；② 授权到位后我**重写**那一行为具体授权令牌（谁、哪天、批几格、账本从几到几），worker 必须在报告里回抄该令牌；③ 「继续」「go on」这类续跑提示**一律不构成**实弹授权，工单里明说。
- **代价**：每张实弹工单多两行样板。换来的是"授权"这件事对 worker 可见、可回抄、可审计。

## D-016 批 TASK-006R3 一格（扩额，XC-XXI-B 4/4 → 5/4）

- **日期**：2026-08-30（老板裁定："批 TASK-006R3 的一格"）
- **决策**：**扩额批准一格**实弹，超出 XC-XXI-B 原定 4 格配额，用于缺陷 A（C-4 / TASK-009 修复）的路径 A/B 实弹替换验证。账本 4/4 → **5/4**。
- **理由**：缺陷 A 的修复到现在两次实弹都**没跑到 dump**（TASK-006R 9/9、TASK-006R2 2/2 都止步 text-poll），既未证实也未证伪。而 C-7 修好之后，撞环失败的成本从"小时级 + 3.5GB 日志 + 外部杀进程"降到 **20ms + 312KB + 干净自退**——这一格的真实风险只是"可能又 0 命中"，而且几十毫秒就知道。
- **前置**：换 boot。上一 boot（`2026-08-29 07:58:23`）合计 **11/11** 次确定性撞同一 ScyllaHide NtContinue-hook 故障环，同 boot 重试无证据支持会不同。老板已重启，**总指挥亲验 BootTime = `2026-08-30 00:54:51`**。
- **落地**：`tickets/TASK-006R3.md`，第一行已按 D-015 新规换成授权令牌（worker 须在报告里原文回抄）。

## D-017 授权把工作区约 1.3GB 未跟踪实弹证据迁入 vault

- **日期**：2026-08-30（老板裁定："授权你可以动大文件"）
- **决策**：按 `ARTIFACT_POLICY.md` 把 `lab/xx21*`、`tools/xx21*` 等未跟踪实弹证据从工作区迁入 `D:/MidaVault/`（G-6 遗留项）。
- **约束（我给自己定的）**：**先拷贝 → 校验字节一致 → 再删源**，不用"移动"一步到位；保留一份**路径映射清单**，因为 `runs/`、`docs/` 里有多处引用旧工作区路径（TASK-010 就引用过 `lab/xx21b_resume/redump2/*`），迁走后必须还能按清单找回。

## D-018 批 TASK-006R4 一格（再扩额，XC-XXI-B 5/4 → 6/4）+ 放行 C:\Windows 落位

- **日期**：2026-08-30（老板裁定："批准一格实弹 / C:\Windows 落位放行"）
- **决策**：**再扩额批准一格**实弹，带 TASK-013 受控 ini（异常分发链 `KiUserExceptionDispatcher` + `NtContinue` 两 hook 关闭）重跑重脱壳。账本 5/4 → **6/4**。同时**放行系统级落位**：实弹前把受控 ini 复制为 `C:\Windows\scylla_hide.ini`（InjectorCLI 裸相对名只搜 Windows 目录——TASK-013 实证的唯一生效路径），约束：仅此一个文件、落位前后核验、跑完必删、贴删除证明。
- **理由**：此前 13/13 次撞环全部处于"无 ini 全默认 hook"状态；关掉风暴 RIP 的直接来源（NtContinue hook +8，跨 3 boot 实锤）后 text-poll 首次有实质理由收敛到 dump——缺陷 A 路径 A/B 首次真正可达。失败成本仍低（C-7 保证 ~20ms 干净自退）。
- **风险（已向老板如实呈现过的）**：① 系统目录写入（约束已如上收紧）；② 混杂变量——受控 ini 是整套 UncoverEngine profile（~30 键），若走通，归因到具体开关需后续最小差分变体（另立单）；③ 关 hook 后壳可能改走其它反调试路径（TASK-013 遗留待验风险，工单路径 D 承接）。
- **前置（总指挥亲验）**：BootTime = `2026-08-30 01:28:40`（与 R3 同 boot；无需换 boot——本单变量是 ini，R3 已实证环与 boot 无关）；起点 HEAD = `ca06117`。
- **落地**：`tickets/TASK-006R4.md`，首行已按 D-015 规换成授权令牌（worker 须原文回抄）。
- **事后（2026-08-30 同日）**：本格**基于一个错误前提**——TASK-013 的"只搜 Windows 目录"结论被 R4 自己的实弹推翻（正确规则 = `<exe目录>/scylla_hide.ini`）。落位方案结构性无效，本格未产出任何有效尝试数据。责任在我（验收把关不严，见 D-019 / P-9）。

## D-019 R4 那一格烧在我的验收失误上；账本如实记 6/4，不找补

- **日期**：2026-08-30（总指挥自记）
- **事实**：TASK-013 的 worker 用 P/Invoke 探针证明"裸相对名传给 `GetPrivateProfile*` 只搜 Windows 目录"——**API 结论正确**。但 InjectorCLI 的实际调用链是 `GetModuleFileNameW` → 拼 exe 同目录绝对路径 → 才调 `GetPrivateProfile*`，那条 API 语义对它不适用。我验收时复验了导入表（`GetCurrentDirectoryW`/`SearchPathW`/`GetFullPathNameW` 0 命中——都是真的，因为它用的是不在我名单里的 `GetModuleFileNameW`），**没有反汇编看文件名参数从哪来**，于是批准了一个错误的程序行为结论，并据此写了 R4 工单的落位方案 → 一格实弹（→ 6/4）零有效数据。
- **决策**：① 账本**如实记 6/4**，不因"前提是我写错的"就把这格退回去——格子确实花了，实弹确实跑了，硬件时间确实消耗了；② 不追究 worker：R4 的 worker 表现是范本级（自己发现前提有误、用三组零成本受控实验实证、强门判无效不硬跑、收尾满分）；③ 把教训写成 **P-9**（探针必须打在被测程序的实际调用路径上；"没导入 X 所以做不到 Y"是伪推理，必须先穷举同目的 API）。
- **代价与收获**：花一格换到了 ini 生效机制的确切规则 + 对 xx 线成功原因的正确解释（撤回"ASLR 布局依赖"假说）。这格不算白烧，但**本可以用零成本的 A/B 实验先验出来**——这是 P-9 的核心。

## D-020 撤回"环触发是 ASLR 布局依赖"假说（e8bda46）

- **日期**：2026-08-30（总指挥自记）
- **被撤回的结论**：我在 e8bda46 里写"同条件不同 boot，xx11 成功而 006R 撞环 → 环的触发是 ASLR 布局依赖的"。
- **为什么错**：那个"同条件"是假的。xx 时代的实弹跑在 scratch 目录（`InjectorCLI` 旁就有受控 ini，`KiUserExceptionDispatcherHook=0` / `NtContinueHook=0` / `NtCloseHook=0`）→ **异常分发链是关闭的**；006R/R2/R3/R4 跑在 `target/release`（注入器旁无 ini）→ **全默认 hook，异常分发链开启**。两组的 hook 配置根本不同。
- **正确结论**：**不是布局运气，是配置差异**。xx11 的成功状态恰好就是 TASK-006R5 想要复现的状态（关闭异常分发 hook）——这大幅提高了 R5 走通的先验概率。
- **证据**：15:26 那份 scratch `scylla_hide.log` 的 15 条 `Hooking X` 安装行恰不含 ini 中三个 =0 的键，`NtQueryObject`(=1) 在列（总指挥在该文件被 R4 探针覆盖前亲读，关键行已存档进 KNOWN_ISSUES C-6 块；文件本体不可恢复 → 新 P-8）。
- **教训**：做跨时段对照时，"同条件"三个字必须逐项列出并逐项核验，不能靠"应该一样"。

## D-021 批 TASK-006R5 全案（crates/ 改动授权 + 再扩一格，XC-XXI-B 6/4 → 7/4）

- **日期**：2026-08-30（老板裁定："批准 TASK-006R5： 授权"——回应总指挥列明的两件批准请求，按全案解释：两件同时批）
- **决策**：① 授权 `crates/` 代码改动（`crates/packers/themida/src/antiantidebug/scyllahide.rs` + `crates/cli/src/unpacker/helpers.rs` 及其测试模块，仅限工单授权文件清单）；② 批 1 格实弹重脱壳。账本 6/4 → **7/4**。
- **理由**：R4 实锤 InjectorCLI 读 `<exe目录>/scylla_hide.ini`，操作员手工落位被 ARTIFACT_POLICY 第 11 条结构性封死 → 改代码是唯一路径；且 xx 线成功状态恰 = 本单要复现的状态（D-020），先验概率显著为正。
- **风险**：① 新增运行期复制/清理代码路径（panic 安全 + 删除证明已写入工单验收）；② 受控 ini 是整套 profile，走通后归因需最小差分变体另立单；③ 关 hook 后壳可能改走其它反调试路径（工单路径 D 承接）。
- **前置（总指挥亲验，2026-08-30）**：BootTime = `2026-08-30 1:28:40`（与 R4 同 boot，无需重启）；起点 HEAD = `5c09e7a`；工作区 `target/release/` 与 `C:\Windows` 均无 ini 残留；vault ini sha `c88e94c3…` 与样品对象 sha `78009803…` 当场复核在位。
- **落地**：`tickets/TASK-006R5.md` 首行已按 D-015 改写为授权令牌（worker 须原文回抄）。

## D-022 TASK-006R5 验收通过（终态路径 A）；账本 6/4 → 7/4；换 boot 追认

- **日期**：2026-08-30（总指挥验收）
- **验收结论**：第一段八条 + 第二段强门全部通过。判别力由总指挥**换缝独立重做**（worker 打 `verify_staged_ini_matches` 恒 Ok 缝 → 1 红 exit 101；总指挥打断 `sha256_hex` 原语 → 已知向量 + 异同断言 2 红 exit 101）→ 字节级恢复（探针残留 0、diff 仍 +469/0 删）→ 7/7 复绿。强门证据 vault 直读：CONFIG_SOURCE 双行（ini + staged sha256 校验）、`staging verification passed` ×2、P-8 evidence-preserved ×2、`scylla_hide.log` NtContinue/KiUser hook 行双零 + NtSetInformationThread=1 等 15 键对照、AV=0 ×3、fail-closed 192 unresolved FATAL 行直读、image_base `0x7ff6eb6b0000` ×3 确定性。最终实弹 exe sha `3f055b8e…`（5825536 B），五字符串命中（SCYLLAHIDE ×3）。
- **账本**：本格 3 次运行（attempt1 因 P-8 证据缺口判无效、attempt2/3 有效 2/2 即停）按 R2/R3 先例记 **1 格**，6/4 → **7/4**。
- **换 boot 追认**：令牌载 BootTime 01:28:40，实测 10:05:51（令牌签发后机器重启）。追认理由：① 差异非 worker 责任、亦非其可检测项；② 本单变量（ini 生效）与 boot 无关（D-020）；③ 新 boot 全新 ASLR（ntdll `0x7ffd379e0000`）反而强化跨布局判定。**今后规则：授权令牌的 BootTime 前置只约束"签发时刻"，执行时 boot 已变不构成 STOP 事由，照实记录即可。**
- **科学结论（干预实验定案）**：D-020 的"配置差异"解释升级为干预证实——唯一变量 = 受控 ini 生效（关闭 KiUserExceptionDispatcher + NtContinue hook），14/14 撞环 → 0 AV。C-6 故障环研究线关闭。TASK-013 待验风险（壳反检）实测阴性。
- **新前沿**：IAT 启动路径重建（192 槽：`0x1123→0x1138d0 … 0x2c7c→0x113700`）是路径 B（可加载产物）的硬前置，另立新单。

## D-023 批 TASK-014 全案（crates/ 改动授权 + 再扩一格，XC-XXI-B 7/4 → 8/4）

- **日期**：2026-08-30（老板裁定："可以开始"——回应总指挥列明的两件批准请求与可粘贴批准语，按全案解释：两件同时批）
- **决策**：① 授权 `crates/` 改动（工单授权清单 8 文件：`iat_partial_accept.rs` / `iat_gap_retarget.rs` / `dump_process.rs` / `import_rebuild.rs` / `iat_completeness.rs` / `trace_imports/mod.rs` / `trace_imports/slot.rs` / `iat_evidence.rs` 及其测试模块；**fail-closed 门语义零改动**为硬约束）；② 批 1 格实弹冲路径 B。账本 7/4 → **8/4**。
- **理由**：R5 实弹指认的唯一剩余阻塞 = IAT 重建（整个 201 槽区零覆盖，192 启动路径站点不可解析）；且运行时槽值只在会话内存中，逐槽诊断必须在下一格实弹前进入代码，否则离线永久补采不到。
- **保险丝（写入工单）**：① 第一段判别力未过或离线证据显示零改善 → worker STOP 不烧格；② 不许把门改松制造"通过"；③ A' 未改善不许第 3 次。
- **前置（总指挥亲验，2026-08-30）**：BootTime（签发时刻）= `10:05:51`（与 R5 实弹同 boot）；起点 HEAD = `be28951`（已核为 HEAD 祖先，be28951..HEAD 仅 docs/tickets）；vault ini sha `c88e94c3…` 与样品对象 sha `78009803…` 当场复核在位。
- **落地**：`tickets/TASK-014.md` 首行已按 D-015 改写为授权令牌（worker 须原文回抄）。

## D-024 TASK-014 工单修订 v2：xx 战役证据包改写问题定义（授权包不变）

- **日期**：2026-08-30（触发：老板质询"以前能正常脱壳为什么现在不行/是不是重复造轮子"）
- **新证据（总指挥直读 vault）**：`xiongxiong_duokai/xx10_attempt_20260828-094641/XX10_REPORT.md`、`xx11_attempt_20260828-112236/XX11_REPORT.md`——08-28 的 xx 战役：XX-10 IAT 186 imports 全解析（`is_complete()=true`、结构门 12/12；行为 0/10 AV 归因 OEP 误写 0x1020 → 栈错位 8 字节）；**XX-11 修复 OEP 后 load_no_crash 10/10 零 AV + S4 业务标记 8/8 对齐**。XX-10 slot 0 现场 VA 低 32 位 = R5 honest-holes IAT 区基址 `0x1136e0` [推断，强] → 同一区域。
- **问题定义改写**：原工单假设"回溯从未咬合、需扩展"——**错**。真实问题 = **回归**：`18e0349`（XX-11 末笔）→ `be28951`（R5 基线）之间 IAT 从 186/186 退化到 0/201。嫌疑：① 006R 命令模板缺 `--oep=captured --data-sections` 两个 flag（retrace 可能被 flag gate）；② 8-29 接管日改动动了 dump/IAT 路径顺序。第一段定位，不得预设结论。
- **决策**：工单第一段改"回归定位优先"（只读 git diff + CLI 参数语义核对 + XX-11 186 槽对照），修复 = **恢复**而非扩展，第二段命令按结论补齐 flag。**授权包不变**（同 8 文件 + 同 1 格 7/4→8/4，D-023 令牌原文仍有效）；修复需触碰清单外文件（如 oep 模块、`data_reinit.rs`）→ worker STOP 请示。
- **教训（立 P-10）**：开新战役/新工单前必须先挖 vault 里同样品的历史战役证据包——08-28 的答案一直躺在 `xiongxiong_duokai/`，8-29 接管日没人读 → R2/R3/R4 三格花在"回到已知位置"；我起草 TASK-014 时又没读，被老板质询点醒。

## D-025 TASK-014 在飞 worker 的处置（老板裁定"等→审→再派"）：实弹移到审计后执行

- **日期**：2026-08-30（老板裁定："等着 worker 处理，汇报后。你审计。然后根据最新情况再派单。工作流程不能错"）
- **背景**：v1 派单已发出、worker 已开工（工作区 10 文件 +726 行未提交：全量站点清单外发 + 逐槽诊断 = v2 仍需要的部分；`dumper/mod.rs`、`pe/src/lib.rs` 为纯 re-export 管道；总指挥已抽查：`fail_closed_sites non-empty => refuse` 门判定语义原样保留，外发为 verbatim 附加，C-7/text-poll/`data_reinit.rs` 零触碰）。
- **决策**：① **不停止、不重派**——已完成/在飞改动全部保留（v2 需要它们）；② worker 完成第一段八条后 **STOP 上报，不执行第二段实弹**（经补丁转达；若补丁不可达且 worker 自行跑了实弹，按强门验收、账本照实记，此为次选路径）；③ 总指挥审计内容 = v1 八条真退出码复跑 + 独立判别力探针 + 差异范围核对 + **回归定位亲验**（只读 `git diff 18e0349..be28951` + flag 语义 + XX-11 186 槽对照——v2 第 1 项，不在 v1 worker 派单内，由总指挥在审计期完成）；④ 实弹段在审计 + 回归定位核对之后按 v2 命令（补齐 flag）另派执行，**D-023 的 1 格授权继续有效，执行时机由总指挥决定**。
- **审计基准说明**：worker 拿到的是 v1 派单，其 Part-1 产出按 v1 八条验收；v2 新增项（回归定位报告 = 第 7 条）由总指挥补做，不算 worker 缺项。

## D-026 派单纪律补强：worker 在飞期间禁止派新单、禁止生成任何新派单/补丁文本

- **日期**：2026-08-30（老板裁定："worker 工作中的时候禁止派新单，必须等 worker 完成工作后审计完，再派。在 worker 工作时，我们的讨论仅仅是讨论，是下次派单的参考。不能在 worker 还在工作时就生成新的派单"）
- **规则**：① 同一 worker 在飞期间**禁止派任何新单**（含实弹段、含对在飞 worker 的补丁/改道指示）；② 必须等 worker **完成工作 + 总指挥审计完毕**，才生成并交付下一份派单；③ 在飞期间总指挥与老板的一切讨论**仅作下次派单的参考**，不构成对在飞 worker 的指示；④ 审计基准 = worker 实际收到的派单文本（v1），总指挥中途改动的工单文件（v2）只作下一单依据，不溯及在飞 worker。
- **撤回**：D-025 的"补丁转达 STOP"条款作废——总指挥在 worker 在飞期间生成了 v2 补丁文本交付老板，违反本条，**已撤回，不得转达**。worker 按 v1 派单文本跑完全程（若含第二段实弹，按强门验收数据有效性、账本照实记）。
- **教训**：中途改道的正确姿势 = 让在飞 worker 按原单跑完 → 审计 → 下一单落实改道；不是在飞改指令（失忆 worker 收到两条不一致的指令流是流程事故源）。
- **在飞期间已锁定的"下次派单参考"**（仅参考，不派）：① 回归定位（`18e0349`→`be28951` 窗口 + `--oep=captured`/`--data-sections` 开关链 + XX-11 186 槽对照）；② XX-11 端点（186/186 + load 10/10 + S4 8/8）作为实弹验收对照基准；③ 实弹命令需按回归定位结论补齐 flag。

## D-027 TASK-014 验收通过（终态路径 A'，账本 7/4 → 8/4）；核心诊断 = 74 启动槽是 Themida VM wrapper

- **日期**：2026-08-30（总指挥审计）
- **验收结论**：v1 八条 + 强门四件套全部通过。总指挥独立复核：① 差异范围 7 文件全在授权清单（worker 自撤 mod.rs/lib.rs 越界 re-export；`iat_evidence.rs` 零改动理由正当 = acceptance crate `#[serde(deny_unknown_fields)]` 硬约束）；② **门语义零改动亲验**（`fail_closed_sites non-empty => refuse` 原样，仅 detail 全量化 + best-effort 诊断外发）；③ 五套件真退出码复跑全 0（pe **1054**=1049+5 / themida **175**=174+1 / cli **580** / clippy / fmt，零 FAILED）；④ **换缝独立判别力探针**（打断 `slot_diagnostics` 发射 → 2 红 exit 101 → 字节级恢复 exit 0；worker 的截断探针 take(16) → 1 红另有记录）；⑤ vault 直读：强门四件套 2/2 趟全过、192 站点 FATAL 行全列（两趟逐位一致）、201 槽诊断 **112/74/15** 精确核对、74 个 VM wrapper 值全落 Themida 段、0 AV、image_base `0x7ff6eb6b0000` ×2、P-8 时序 12:49<12:53、staging 全清、构建 sha `665c58c9…` 五字符串命中。
- **账本**：2 次有效尝试确定性复现即停，记 **1 格**，7/4 → **8/4**。
- **核心知识资产（入 KNOWN_ISSUES C-6 块）**：74 个启动路径槽的运行时值 = Themida 段内 VM wrapper 地址（+0x1681d1…+0x3203d7；偏移与 XX 时代旧产物坏值 0x1401681d1 逐位一致，跨 boot 确定性互证）。**静态原导入表 9 项对 VM wrapper 结构性 0 命中 → XX-10-A 静态回填 0 覆盖是结构性必然，不是回归（修正 D-024 假设）**。shell trace 74 槽全败（主线程无单步事件）→ **186/186→0/201 的机制层主嫌疑 = shell trace 的执行线程/时机**；XX-11 时代 trace 能解 VM 槽的机制 = 复现钥匙。
- **审计备注（不阻塞验收）**：① 报告 §1 写 +661 实际 +653（算术笔误）；② slot-scoped 行为改动的测试仅字符串断言（未压到"继续遍历"行为），下轮补真实行为测试；③ worker 留 2 个 evidence 临时文件在 /d/Temp（总指挥核对 sha256 与 vault 一致后删除）；④ 192 站点数与 FATAL 行核对一致。
- **下一单参考（需老板批准后才派）**：TASK-015 = shell trace 在正确线程/时机单步 VM wrapper（恢复 XX-11 的 186/186 路径）+ 回归定位（`git diff 18e0349..be28951` trace 路径）+ acceptance crate `#[serde(default)]`（如需 sidecar 结构化证据，需扩授权）+ 一格实弹 8/4 → 9/4 冲 B1'（对照 XX-11 端点 186/186 + load 10/10 + S4 8/8）。

## D-028 批 TASK-015 全案（crates/ 改动授权 + 再扩一格，XC-XXI-B 8/4 → 9/4）

- **日期**：2026-08-30（老板裁定："批准"——回应总指挥列明的 TASK-015 范围与两件批准请求，按全案解释：两件同时批）
- **决策**：① 授权 `crates/` 改动（工单清单：`trace_imports/mod.rs` + `slot.rs`（trace 线程/时机/起点修复）、pe dumper 五文件（适配/诊断延续）、`cli/unpacker/iat_evidence.rs`（sidecar）、`acceptance/src/oreans_gate.rs`（**仅限** `#[serde(default)]`/新增可选字段）及其测试模块；fail-closed 门语义零改动为硬约束）；② 批 1 格实弹冲 B1'。账本 8/4 → **9/4**。
- **理由**：T014 诊断定案 74 启动槽 = Themida VM wrapper（静态结构性 0 命中，非回归）；shell trace 的线程/时机是恢复 XX-11 端点（186/186 + load 10/10 + S4 8/8）的唯一已知缺口，且 XX-11 时代机制存在 = 可复现性有据。
- **保险丝（写入工单）**：① 第一段判别力未过或回归源未定位 → STOP 不烧格；② 不许改松门；③ A' 未改善不许第 3 次。
- **前置（总指挥亲验，2026-08-30）**：BootTime = `10:05:51`（与 T014 实弹同 boot）；起点 HEAD = `291b239`；工作区净（仅已知 untracked）；vault ini sha `c88e94c3…` 与样品对象 sha `78009803…` 当场复核在位。
- **落地**：`tickets/TASK-015.md` 首行已写授权令牌（worker 须原文回抄）。

## D-029 TASK-015 验收通过（终态路径 B1'，XX-11 端点受治理复现；账本 8/4 → 9/4）

- **日期**：2026-08-30（总指挥审计）
- **验收结论**：v1 八条 + 强门四件套全部通过。总指挥独立复核：① 差异范围 3 文件全授权（+249/-17；`oreans_gate.rs` 仅新增 3 个 serde-default 行为测试；**门语义零改动亲验**）；② 六套件真退出码全 0（pe **1054** / themida **176**=175+1 / cli **580** / acceptance lib **256**=253+3 / clippy / fmt，零 FAILED）；③ **换缝独立判别力探针**（stale-pending 分支改用错误 tid continue → 1 红 exit 101 → 字节级恢复 exit 0；worker 的探针 = 整支退回旧行为，另有记录）；④ vault 直读 B1' 全链条：EXIT=0 ×2、**[GOOD] Candidate written ×2**（行内含 ANSI 色码，总指挥初查误报 0，ANSI 核实后在案）、AV=0 ×2、**trace resolved=74/74 failed=0**（clearing stale pending=1、Suspending spurious thread=7/6、OK IAT[=75）、**imports 186 整**（逐项计数 kernel32 69/msvcrt 76/user32 23/wininet 9/version 3/advapi32 3/shell32 1/comctl32 1/gdi32 1，两趟逐位一致）、结构门 **12/12 ×2**、**load_no_crash 10/10 ×2**（pass_rate 1.0）、产物 **1,539,072 B ×2**（attempt1 `fd96ed38…` / attempt2 `a852880a…`——与 XX-11 `36043cb4…` 不同 = session 级差异，语义端点一致，worker 已如实披露）、scylla 日志双零 + 15 键、P-8 13:56<14:00、staging 全清、构建 sha `bc1feebf…` 五字符串命中。
- **主根因定案 [已验证]**：T0.5-R2（`a9aced4`）HW-anchor ERROR_NOACCESS → 12s grace window → debug loop 断在 spurious 线程 ExitThread 且未 continue → post-loop trace `continue_event(trace线程)` 被 TID 校验拒绝 → **trace 从未单步**。三日志互证：R5 日志 TID mismatch ×3（fail-fast 可见）、T014 日志 0 处（被 slot-scoped 包装吞成误导文案）、XX-11 日志 frozen-entry bootstrap 行（无 pending 路径）。`trace_imports` 自 `3b5862b`（XX-8-A）起结构未变——缺口在 break 路径不在 trace_imports；`--oep=captured` 本就是默认值（flag 假设关闭，D-024 假设①作废）。
- **账本**：2 次有效尝试 = 1 格，8/4 → **9/4**。
- **里程碑**：**同一样品第二次端到端跑通（08-28 XX-11 之后首次），且全程有治理证据链**——强门四件套、P-8、结构门 12/12、load 探针 10/10、S4 对照（窗口标题/config.ini 26B/core.dll sha `09f3dd34…` 与 XX-10 vault 逐位一致）。老板的"以前能脱壳"从记忆变成可复算的事实。
- **审计备注（不阻塞验收）**：① 两趟产物 sha 互异且 ≠ XX-11（session 级确定性差异，尺寸/imports/结构/load/S4 全对齐，worker 已披露）；② S4 提取物（core.dll/config.ini）随 s4_probe 目录删除——sha 已记录且对照对象在 vault 可复算，建议后续此类提取物**先入 vault 再清理**（P-8 精神延伸）；③ `mod.rs` 的 LifecycleError 归类无专属行为测试（T015 唯一新测试压在 slot.rs stale-pending 上）；④ load 探针用 `tools/_behavior_probe.py` 非 cdb sxe-av（语义等价，worker 已披露，可选补跑）。

## D-030 批 TASK-016 阶段收尾（crates/ 改动授权按清单；纯离线零实弹，账本不变 9/4）

- **日期**：2026-08-30（老板令："现在你派个单做个阶段收尾，别以后用不能用了。还有要注意通用性，不要有硬编码"——并已亲验 B1' 产物"达到要求了"）
- **决策**：① 授权 `crates/` 改动**仅限**工单清单（B1' 路径 8 文件行为中性硬编码清除 + `acceptance/src/preflight.rs` 能力预检 + 各自测试模块）；② **纯离线零实弹**，账本不变 9/4；③ 交付 = 硬编码审计报告（含只读文件发现清单）+ runbook（`docs/RUNBOOK_REV2_B1.md`）+ vault 索引 + 全仓 workspace 回归快照。
- **理由**：B1' 能力当前靠 env 变量 + 隐性知识支撑（无 ini 静默撞环、无启动前检查、复现配方散落 runs 报告）；T014/T015 快速修复可能引入样品级硬编码（RVA/魔法数/路径/线程假设）——换样品/环境即断。老板点名的"通用性/无硬编码"与"别以后用不能用"正是这三个风险面。
- **硬约束**：① **行为中性**——清除只许更通用，不得改变 B1' 行为（全仓测试全绿 + 计数不减为证据）；必须行为性改动才修的 → STOP 请示（属下一战役需实弹）；② preflight 查 ini 结构（三 =0 键）不查内容指纹；③ 报告-only 文件（mod.rs text-poll/C-7 段、args.rs、oep/、data_reinit.rs、windows_debugger.rs、av_*）只报不改。
- **前置（总指挥亲验，2026-08-30）**：起点 HEAD = `3de2ade`；工作区净（仅已知 untracked）；vault ini sha `c88e94c3…` 与样品对象 sha `78009803…` 在位（preflight 目标物）。
- **落地**：`tickets/TASK-016.md` 首行已写授权令牌（worker 须原文回抄）。

## D-031 TASK-016 审计：**条件通过**（2 项补正 = TASK-016R1，D-030 范围内，账本不变 9/4）

- **日期**：2026-08-30（总指挥审计）
- **总评**：8 条验收标准中 7 条总指挥亲验 PASS；标准 3（硬编码审计报告完整性）**不完整** → 补正轮 `tickets/TASK-016R1.md`（微型：1 行行为还原 + 审计文档增补；在 D-030 授权的同一文件清单与处置类别内，**无新授权令牌、不新开账本格**）。补正验收后再整单入栈提交。
- **亲验 PASS 项**：① diff 范围 6 文件全授权（+487/-66，HEAD 仍 `2d0f5d2` worker 未提交，符合"工作区留改动"）；② 四套件真退出码 0 且计数 pe **1054** / themida **176** / cli **580** / acceptance lib **263**（=256+7 新增 scylla 用例，只增不减，总指挥独立重跑）；③ preflight 实现亲读（三零键 fail-loud、多段歧义、无 sha 指纹，`check_scylla_hide_readiness` 确未接进 `run_offline_preflight` = worker 如实披露的诚实限制）；④ **总指挥换缝判别力探针**（禁用非零值分支 → `scylla_hide_key_set_to_one_fails_loud` 红、真 assertion、exit 101 → 字节级恢复 sha 一致 + diff 回 327 行；worker 的探针缝 = missing-key，两者互补）；⑤ runbook 七项判据对 T015 vault 实证逐条一致（CONFIG_SOURCE=2、staging=1、stale pending=1、OK IAT[=75、resolved=74/201、imports=186、[GOOD]=1、verdict=StructuralPassBehaviorPending）；⑥ INDEX.md 34 行整、总指挥另选 3 件抽验 sha 全一致（attempt2.log `56b0a48f…`、产物 attempt2 `a852880a…`、load JSON `9d4600da…`；连同 worker 抽的共 6/34）；⑦ clippy（-D 三 lint）/fmt 0；⑧ 零实弹（tasklist 无相关进程、无 staging 残留、worktree 无 scylla_hide.ini、worker 临时文件已清）；⑨ scyllahide.rs 38 行哈希校验去重 = `verify_helper_hashes`（HEAD 既有）错误字符串逐字一致，行为中性成立。
- **F1（补正：行为还原）**：`iat_gap_retarget.rs` `1..64`（实搜 63 槽）→ `1..=64`（实搜 64 槽）= **未申报的搜索窗口加宽**，与报告"无一处改变数值、分支"冲突 [已验证：diff 对照]。B1' 端点可证不受影响（`iat_gap_retarget result` 日志行在 T015 两趟实弹均不存在 = sites_seen=0 不打补丁 → 产物字节不变；interior-zero 检测要求前后邻居均非零而 201 槽无 interior zero；`stage_plan.patch_wrapper_iat_call_sites` 本就是 AhkGto 专属门）——红线"不得改变 B1' 行为"未破，但报告准确性破了。R1 还原为 const `=63` + `1..=`。
- **F2（补正：审计完整性）**：审计文档"授权文件生产代码无样品级硬编码进入控制流"全称命题为假——`dump_process.rs` 存在整片**既有** GTO-UI 样品锚区未盘点（裸 `0x147868`/`0x147888` :2298-2299/2362；具名补丁常量 `0x5c5d`/`0x35520`/`0x364e0`/`0x34dbb`/`0x34ed4`/`0x34f66`/`0x34f59` 及注释内 0x63f4/0x6757/0x1b10 等，全部 `stage_plan`/`capture_policy` AhkGto 门控、对 B1'/xx21b 死代码）。按工单判据必须进报告（处置 = STOP 级留待下一战役）；R1 只增补文档，GTO-UI 生产代码一行不动。
- **F3（既有基线门失败，不阻塞 T016，需老板另授权）**：`tools/check_clippy_baseline.ps1`（WO-23 lint 锁，08-29 18:17 基线）现 FAIL：`too_many_arguments` 基线 61 → 实测 62。总指挥逐文件分组核对：62 条命中 0 条由 T016 引入（T016 diff 无任何函数签名改动、新函数参数 ≤2；slot.rs 的 1 条命中是既有 `trace_one_slot` 签名告警，diff 中 "fn trace_one_slot" 两处均为 hunk 头注记）→ **HEAD 上同样 62，漂移来自基线生成后的已验收提交窗口（T006R5/T014/T015），确切函数未溯源（非决策依赖）**。修门 = 改门文件（基线再生）或重构 8 参函数，均属门变更 → 需老板授权另立工单，本审计不动。
- **审计备注（不阻塞）**：① 审计文档 §八 "+482/-66" vs 实际 +487/-66（笔误，R1 顺带勘误）；② 授权清单误写 `dumper/iat_completeness.rs`（实为 `crates/pe/src/iat_completeness.rs`，总指挥起草笔误；已核 `201/192` 仅在其 :281-282 doc 注释，原结论对该文件成立）；③ preflight 键匹配用 `.ends_with(".{key}")` 理论上可被同后缀更长键误命中（现实 ScyllaHide 键集无此形状，留作未来加固观察）；④ 总指挥误删已跟踪文件 `_clippy_baseline` → 当场 `git checkout` 字节级恢复（2822 B）；教训：rm 前先 `git ls-files`；⑤ worker 新注释/文档沿用仓库既有 "hооk" 同形字惯例（HEAD 5 文件先例），非新问题。

## D-032 TASK-016 + R1 终验收通过（阶段收尾入栈；账本不变 9/4；零实弹）

- **日期**：2026-08-30（总指挥终审）
- **R1 终审（五条全亲验 PASS）**：① R1 增量仅落 2 个授权文件（其余 5 个 crates 文件 diff 统计与审计时逐项相同、preflight.rs sha `49b7134e…` 未动、`_clippy_baseline`/`check_clippy_baseline.ps1` 零触碰）；② **F1 还原逐位核对**：`GAP_NAME_NEIGHBOR_MAX_DELTA=63` + `1..=` ≡ pre-T016 `1..64u32`（probed deltas 1..=63），doc 注释如实写明还原语义；③ 我的重跑 pe **1054** / acc **263** 真退出码 0、clippy（-D 三 lint）/fmt 0；④ `HARDCODING_AUDIT_T016.md` §九 GTO-UI 锚区清单与总指挥审计发现逐项吻合（含 `CALL_RVA=0x6757/0x63f4` 具名常量——总指挥此前扫描截断漏看，已核实在位），全称命题改限定 + 2 行勘误到位；⑤ runs 增补段诚实（连 R1 注释扩写 +487→+490 的 3 行差异都主动披露）、D-030 令牌回抄、零实弹自证。
- **TASK-016 整单结论**：8 条验收标准在补正后**全部 PASS**（标准 3 经 §九 增补后成立）。T016 交付物入栈：6 个授权文件硬编码处置（行为中性）+ preflight ScyllaHide readiness（三零键 fail-loud，7 用例；未接进 `run_offline_preflight` 为已披露限制）+ `docs/RUNBOOK_REV2_B1.md`（七项判据经 vault 实证逐条核对）+ `docs/HARDCODING_AUDIT_T016.md`（含 §九）+ vault `INDEX.md`（git 外）+ runs 报告。
- **账本**：XC-XXI-B **9/4 不变**（T016+R1 全程零实弹）。
- **留给老板的三个裁定（都不阻塞入栈）**：① F3 clippy 基线门既有失败（61→62，T016 贡献 0）——重生成基线或重构 8 参函数，需另立工单授权；② §九 GTO-UI 锚区（裸 0x147868/0x147888 + 具名补丁常量）的清除属下一战役（需实弹再验证）；③ 18 个本地提交未推送（D-010），推送前建议 `cargo deny check advisories`（本机离线跑不了）。

## D-033 批 TASK-017（T0.5 续跑：熊熊 B1' 产物 Run UI 事件驱动实弹 1 格，XC-XXI-B 9/4 → 10/4）

- **日期**：2026-08-30（老板令："先解决1，其他的再等等吧"——按全案解释：批准 T0.5 续跑 = 熊熊收尾最后一格；TASK-007 / clippy 基线门修复 / §九 GTO-UI 锚区 / 推送 全部暂缓）
- **决策**：① 批 1 格实弹（T0.5 = 启动 B1' 产物 a852880a + UI 事件驱动，验 Run 按钮 RIP 是否落 urlmon.dll = 事件级行为等价，Run verdict PARTIAL → FULL）；② 本单内多次 UI drive 尝试记 1 格（T015 先例），**中途若需重新 `/unpack` → 另立单另批格**。
- **为什么现在能跑（双重阻塞解除）**：① 缺陷 A 已修并实弹验证（TASK-009/T015）；② 旧宿主跨 boot 崩（C-4/B）——总指挥亲验 **BootTime = 2026-08-30 10:05:51 与 T015 产物生产同 boot**，风险暂不适用。
- **关键适配（写入工单）**：旧驱动脚本按 XX-11 产物 36043cb4 写死（HOST_SHA、core.dll 固定基址 0x7FFE1DA10000、宿主基址 0x140000000——全是旧会话绑定期产物）→ 本单换治理产物 a852880a（T015 attempt2）+ **基址全部动态解析**（enum_modules，解析不到 fail-loud；T016 反硬编码纪律沿用）；core.dll 期望 sha 换 S4 验证过的 09f3dd34；RVA 0x1C120/0x16F300 保留（core.dll 跨战役逐位一致）。
- **保险丝（写入工单）**：开跑前自查 BootTime（变了 → STOP，跨 boot 必崩是已知 C-4/B，不是本单要验证的）；防火墙拦截现状核实（未拦截 → STOP 请示，不许自行改）；新阻塞/AV → STOP 不烧第 2 格；证据先入 vault 再清理（P-8，D-029 备注②教训）。
- **前置（总指挥亲验，2026-08-30）**：BootTime = 10:05:51 同产产 boot；vault 产物 a852880a（1,539,072 B）与 core.dll 09f3dd34 在位（sha 现算核对）；`lab/xx21b_run_ui/` 不存在（全新部署）；脚本机制就绪（`wait_core_loaded`/sha fail-closed/redline FAIL_FIXED_BASE 结构在）。
- **落地**：`tickets/TASK-017.md` 首行已写授权令牌（worker 须原文回抄）。

## D-034 TASK-017 审计通过（终态 = 工具性阻塞；账本 9/4 → 10/4；环境垫零发现入 P-11）

- **日期**：2026-08-30（总指挥审计）
- **审计结论**：worker 全部主张亲验成立——① 适配 diff 无钉死残留（新增行 0 命中旧会话地址；动态解析在实弹中自证：三趟 core_base 各异 0x7ffc558b0000/0x7ffc55d90000 且都解析成功）；② vault 证据 sha 抽验一致（attempt2 JSON `cdd1c68c…`、产物 `a852880a…`）；③ 停止决策正确（三态证据不可得 → 停、不烧第 2 格；判"工具性阻塞"而非硬凑 FULL，诚实）；④ 账本 1 格（3 次有效尝试 = 1 格，T015 先例）。
- **总指挥独立探针（两条新缝）**：P1 当前线程伪句柄 GetThreadContext → 直接拒绝（ok=0，方向一致）；**P2 调试端口路径** → `DEBUG_ONLY_THIS_PROCESS` 拉起 cmd，CREATE_PROCESS 挂起态读得**真实 Rip=0x22d9f9f8b8**（imageBase 真实非零）——**垫零只覆盖非附加式观测，调试端口路径可用**（P2b 两次 Rip 相同 = 探针未消费后续调试事件致进程冻结，探针自身痕迹非垫零）。环境发现入 KNOWN_ISSUES **P-11**。
- **正面成果（比预期多）**：B1' 产物 Run 实调用**到达 GUI 业务层**——PigToGoLicenseDialog（"授权验证"，T015/XX-11 S4 同款 UI），GUI 存活响应 3/3、无 AV、IAT 稳定——T0.5 旧基线（旧宿主启动即 AV、无窗口）从未达成。
- **下一步三选项（待老板裁定）**：**A'（总指挥推荐）**批 1 格把 harness 观测腿改调试端口泵重跑三态判定（机制本机已实测可用）；B 接受非 RIP 证据口径判 FULL（不推荐——弱化证据纪律，且 A' 廉价可行）；C 挂起 T0.5（BLOCKED_ENV 先例）转其它战线。

## D-035 批 TASK-018（A' 路线：T0.5 驱动脚本观测腿改调试端口泵重跑三态，XC-XXI-B 10/4 → 11/4）

- **日期**：2026-08-30（老板令："A"——按全案解释：批准总指挥推荐的选项 A'；其余事项继续暂缓）
- **决策**：① 批 1 格实弹（10/4 → 11/4）；② 判定语义**逐字沿用** TASK-017 票面三态（FULL/新阻塞/AV），仅证据来源从外部 OpenThread 读上下文换成**调试端口泵**（P-11：外部观测族垫零、调试端口族可用——总指挥 P2 探针实测）；③ 新建 `tools/xx21b_t05_ui_drive_dbg.py`（fork 自 T017 版，原脚本不动），宿主创建改 `DEBUG_ONLY_THIS_PROCESS` + 独立泵线程全量消费调试事件（不消费 = 冻结，P2b 实证之坑写进工单）+ Run 线程句柄取自 CREATE_THREAD 调试事件 + EXCEPTION 事件全记录（AV 三态证据来源）。
- **保险丝（写入工单）**：泵健康自证（GUI hung=0 贯穿 = 无冻结）；"附加改变行为"（调试附加下 GUI 层不再出现）→ 如实上报不许硬凑；BootTime/部署 sha/防火墙开跑前三自查；新阻塞/AV → STOP 不烧第 2 格；证据先入 vault。
- **前置（总指挥亲验，2026-08-30）**：BootTime = `10:05:51` 同产产 boot；`lab/xx21b_run_ui/` 三件 sha 与 vault 逐位一致（a852880a / 09f3dd34 / config.ini `cde9be13…`）；HEAD = `e31d5ed`。
- **落地**：`tickets/TASK-018.md` 首行已写授权令牌（worker 须原文回抄）。

## D-036 TASK-018 审计通过（终态 = AV 三态，按票面 STOP；账本 10/4 → 11/4；新发现入 C-8/P-11 增补）

- **日期**：2026-08-30（总指挥审计）
- **审计结论**：worker 全部主张亲验成立——① 改动仅新增 `tools/xx21b_t05_ui_drive_dbg.py`（1361 行），原脚本/crates/门文件零触碰；② **AV 5/5 可复现**亲验（att1 泵 JSON：continues=878/continue_fails=0/wait_errors=[]，exceptions 821 条含真实 thread_rip；att5 时间线：CREATE_THREAD Run 线程 → 首 AV @run_va → 817× 恒同 core+0x2cc7a6 → second-chance → EXIT_PROCESS 0xC0000005）；③ **根因字节级亲验**（总指挥直读两代 vault 证据对照）：非附加 `41574156…` 明文 prologue vs 附加 `586db5df…` 密文，5/5——**调试附加 → WinLicense 反调试扣住 core.dll `.text` 解密 → Run 跳入密文 → 入口 AV**（入 KNOWN_ISSUES **C-8**）；④ vault 15 文件 MATCH 5/5 + INDEX 更新；⑤ 清理证明（探针/干跑 18+4 文件已删、无残留进程）。
- **票面偏差处置（1 处，接受）**：worker 将 `0xc000008e`（引导期良性浮点）路由 DBG_CONTINUE（票面原写"其它 → NOT_HANDLED"）——probe 实证 NOT_HANDLED 会触发 WinLicense 对话框使实验不可能；**全程 EXCEPTION 仍全量记录**（AV 判据 = 事件记录 + 退出码，与 Continue 状态无关），披露方式 = 报告 §2.2 显式标注偏差与证据。接受。
- **worker 超额贡献（并入 P-11）**：① ContextFlags 精化——`0x100000`（无 CONTROL 位）在调试路径也读出假 0，必须 `0x100001`（CONTEXT_CONTROL_AMD64，引擎同款）；② Windows 调试会话线程绑定——`WaitForDebugEvent` 必须与 `CreateProcessW` 同线程（probe 3/4 对照，跨线程 err=6）；③ 运行态采样可行（Suspend+GetContext+Resume 经调试句柄读得真实 RIP）。
- **T0.5 总定案（结构性结论）**：三态 RIP 判定在本平台 + `NO_BYPASS` 红线内**结构性不可达**——外部观测被环境垫零（P-11），调试观测被壳反调试扣解密（C-8）。**观察与执行互斥**。T017/T018 构成对照实验对（同 run_va、两种附加方式、两种字节态），证据可复算。Run verdict 诚实停在 **PARTIAL**：结构门 12/12 + load 10/10 + S4 对齐（B1'）+ GUI 层行为证据（"授权验证"对话框 3/3、无 AV）+ 不可达性证明。剩余路线：非托管环境机器（两套 harness 已就绪）或壳进程内自证（红线外，改样品）——待老板裁定是否投入。
- **账本**：XC-XXI-B 10/4 → **11/4**（1 格，5 次有效尝试 = 1 格，T015 先例）。

## D-037 批 TASK-019（T0.5 终跑：完美候选路线，XC-XXI-B 11/4 → 12/4）+ P-10 再犯记档

- **日期**：2026-08-30（老板令："批准"——回应总指挥提案：B1' 宿主 + 脱壳 core 完美候选 + T018 调试泵 harness，三态判定）
- **背景（老板纠偏，P-10 再犯记档）**：总指挥在 T017/T018 派单时部署**原版** core.dll（09f3dd34）——理由是 S4 身份连续性，但**没有挖 `xiongxiong_core`/T0.4 线**（P-10"派单前必读历史战役"再犯，责任在总指挥）。老板以一句"core.dll 我们不是有另一条线解决吗？"点破：T0.4（2026-08-29 完工）已产出脱壳 core 完美候选（`core_perfect_candidate.dll`，3650ea6c，14,435,328 B，S1-S4 全 PASS，.text 静态明文——**C-8 的"扣解密"对它结构性失效**）。T018 的一格本可省；C-8 字节级定案与对照实验对作为独立发现保留价值。候选文件由总指挥定位：`D:/MidaVault/lab/worktree_evidence_20260830/lab/xx21b_run/core_perfect_candidate.dll`（sha 现算核对一致）；resume 线 `fc98c187` 候选（无报告无结论）不采用。
- **决策**：① 批 1 格实弹（11/4 → 12/4）；② 判定语义边界（老板追问"是不是硬编码"后写死进票面）：判定对象 = "B1' 宿主 + 脱壳 core 候选"组合（与老 Step1 基线同构）；**候选的固定基址偏好（DYNAMIC_BASE 清除、首选 0x7ffe1da10000）是 T0.4 时代的硬编码残留（老板指出），实弹必须记录实际/首选基址是否一致（重定位链有效性顺带验证）**；**FULL = 路径级结论，≠ "完全行为等价"**（后者需后置离线工单修固定基址，不烧格）；③ Run/GetAppVersion 导出动态解析，不许硬编码 RVA；④ "附加改变行为"保险丝沿用（候选保留的 `.winlice` 节行为级反调试未测）。
- **前置（总指挥亲验，2026-08-30）**：候选 sha 一致；宿主 a852880a / config.ini cde9be13 在位；HEAD = `daae270`；BootTime 连续性由 worker 开跑前自查。
- **落地**：`tickets/TASK-019.md` 首行已写授权令牌（worker 须原文回抄）。

## D-038 TASK-019 审计通过（终态 = 宿主引导期 AV → STOP；账本 11/4 → 12/4；根因归因纠正：候选 C-5 会话绑定，非宿主）

- **日期**：2026-08-30（总指挥审计）
- **执行与 STOP：合格**。worker 全部流程要求亲验成立：sha 三门（3650ea6c/a852880a/cde9be13）、Run/GetAppVersion 导出动态解析（单元自测 + 实弹双证，与 T0.4 报告逐位一致，脚本内无硬编码 RVA）、调试泵 2/2 趟全消费（continues 56/58、0 fails）、2/2 可复现、三套既有脚本零改动、crates 零改动、vault 证据先行（7 文件 + INDEX 更新）、诚实 STOP 不硬凑。
- **根因归因纠正（总指挥法证推翻 worker 归因）**：worker 断言"宿主 a852880a 文件 RVA 0x112c10 硬编码陈旧 ntdll 地址 0x7ffeeb426390"——**实读为假**：a852880a 全文旧值 **0 命中**，其 `.bss 0x112c10` = `0x7ffd37ae6390` = **当前会话** ntdll（0x7ffd37ae0000）+0x106390（宿主今晨本 boot 生产，指针有效；T017 3/3 + T018 引导 5/5 互证）。**陈旧值的唯一携带者是候选 core.dll**（T0.4 在 08-29 07:58 重启前会话 dump 的原始内存映像）：总指挥字节搜索候选文件旧值 **2 处命中**（首处偏移 0x13f810）；T019 异常记录 `thread_rax = thread_rip = 0x7ffeeb426390`（call rax 取指 AV，first+second chance）——候选的 C-5 会话绑定指针在宿主初始化消费 core 数据时引爆。worker 的"重脱壳宿主"建议**驳回**（宿主无辜，重脱壳纯属烧格）；其 §5.3"复算"（ntdll base 查得 0x180000000）为自家进程查询错误，未验证任何事实。
- **知识落账**：C-5 增补——**会话绑定同样命中我们自己的重建 DLL**（raw dump 重建品携带 dump 会话绝对指针；同会话 S1-S4 全 PASS 不能替代跨会话验证）。T0.4 报告自身的阻断根因（旧宿主 0x112c10）与本次（候选 0x13f810）是同一疾病的两个宿主。
- **T0.5 剩余路径（待老板裁定）**：三态判定的观察机制已全部就绪（调试泵 + 动态导出解析 + 候选明文），唯一缺的是**候选的会话指针清洗**——方案 A（推荐，离线不烧格）：用 TASK-004 data_reinit 会话清洗机器适配候选（或外科手术式：把候选内旧 ntdll 区间指针按 0x106390 偏移模式更新为当前会话值，复扫至旧区间 0 命中）→ 然后 T0.5 三态重跑（1 格）；方案 B（不推荐）：当前会话重 dump core（T0.4 管线重跑，1 格）+ 三态（1 格）= 2 格且下次重启又废（treadmill）。
- **账本**：XC-XXI-B 11/4 → **12/4**（1 格，2 趟有效尝试 = 1 格）。

## D-039 批 TASK-020（候选 core 会话指针清洗，方案 A，纯离线零实弹，账本 12/4 不变）

- **日期**：2026-08-30（老板令："方案a"——批准总指挥推荐的 A 路线：离线清洗候选，为 T0.5 三态重跑铺路）
- **决策**：① 批纯离线工单（**不烧格**）；② 清洗对象 = vault 原件的**副本**（3650ea6c 原件只读一字节不动——T016 红线沿用）；③ 方法 = 外科手术式（扫描分类 → 裸数据槽直补当前会话值 / 重定位覆盖槽按语义处理或不清洗并如实标注 → 复扫归零 → 补丁最小性对账 → 结构/导出/.text 完好性证明）；④ 交付 pointer_map.json（机器可读，T0.5 harness 将来做部署时 fixup 实现跨 boot 复用的输入）；⑤ 诚实边界写进票面：清洗后候选绑定当前会话（ntdll 0x7ffd37ae0000），跨重启仍会死（C-5 未根治），三态重跑必须同 boot。
- **技术要点（写入工单防坑）**：候选加载时会被重定位（T019 实测 0x7ffd36310000 ≠ 首选 0x7ffe1da10000）——**重定位覆盖槽不许直补当前值**（loader 会二次 delta 破坏），必须移除对应 .reloc 条目或保持原样标注；清洗只动数据指针，不碰代码/结构/导出。
- **保险丝**：若 worker 判定必须动 crates 机器才能安全处理 → STOP 请示；死区间恢复不全（T0.4 证据只确证旧 ntdll 区间）→ 如实标注"未能恢复，未清洗"，不许猜。
- **前置（总指挥亲验，2026-08-30）**：vault 原件 3650ea6c 在位（sha 今日已核）；旧值 ≥2 处命中（0x13f810 等）为总指挥字节搜索实证。
- **落地**：`tickets/TASK-020.md` 首行已写授权令牌（worker 须原文回抄）。

## D-040 TASK-020 审计：**条件不通过**（补丁基址错误 + 非对齐扫描漏项 → R1 微补正；另发现候选基址锁死约束）

- **日期**：2026-08-30（总指挥审计）
- **执行纪律：PASS**——vault 原件 sha 前后一致（3650ea6c）、补丁最小性 24 字节恰 8 槽（总指挥独立 diff 复核）、`.reloc` 仅 4 条非绝对条目（.tls 区）与 8 槽零重叠（独立解析验证）、结构/导出/`.text` 完好、零实弹、诚实分类（6 个 B 类 .winlice 值未清洗如实保留）。
- **核心正确性：FAIL（两处）**——① **7 个 ntdll 槽补丁基址错误 +0x100000**（worker 用 `0x7ffd37ae0000`，正确 = `0x7ffd379e0000`，四重验证：总指挥本机实测 / 宿主 `.bss` 值−文档化偏移 / T019 泵首 LOAD_DLL / T018 RIP owner 归属）。**根因在总指挥**：D-038 把宿主 `.bss` 值 `0x7ffd37ae6390` 错误分解为"基址 0x7ffd37ae0000 + 0x106390"（正确：值 − 0x106390 = 基址 0x7ffd379e0000），worker 采信错误记录；其 §9"实测差 0x100000"备注即报警，但以错误解释（"ntdll 进程级随机化"）自圆——T017/T018 宿主进程在本 ntdll 基址下存活 8+ 次证伪该解释。② **非对齐扫描未做**（票面明确要求"对齐+非对齐"）——总指挥非对齐独立扫描：原件旧 ntdll 死区间值 **20 个**（对齐 7 + 非对齐 13），清洗后**残留 13 个**（全在 `.winlice` 保留壳区，非对齐位置）；"复扫归零"只在其对齐方法学内成立。urlmon 槽补丁**正确**（0x7ffd10050000，经 T017 IAT 值 0x7ffd1014e470−0xfe470 独立交叉验证）。
- **审计中新发现（超出 worker 范围）**：**候选是基址锁死设计**——对齐自引用指针（指向首选基址 0x7ffe1da10000 映像内）**3549 个**（含非对齐 4865），而 `.reloc` 仅 4 条 → 非首选基址下全部失效；T019 实测 loader 确实重定位了（实际 0x7ffd36310000 ≠ 首选）。**T0.5 三态重跑必须设硬门：core 实际加载基址 ≠ 首选基址 → Run 触发前 fail-loud 弃判**（T019 票面的 base_agreement 条款升级为硬门）。
- **处置**：① D-038 勘误（ntdll 基址 = `0x7ffd379e0000`，本条为准）；② `TASK-020R1` 微补正（纯离线零实弹不烧格，D-039 范围内无新令牌）：重补 7 槽（总指挥提供逐槽正确值表）+ 非对齐普查（13 个 .winlice 残留列入 residual_unpatched 不盲改）+ 全套重验证；③ T0.5 三态重跑（另批 1 格）票面将写入基址硬门 + `.winlice` 残留风险注记。
- **账本**：XC-XXI-B **12/4 不变**（R1 纯离线）。

## D-041 TASK-020R1 审计通过（清洗完成，T0.5 三态重跑就绪；账本不变 12/4；总指挥期望值表 2 处算术错误记档）

- **日期**：2026-08-30（总指挥审计）
- **逐槽独立验证：8/8 全对**。总指挥独立 diff（原件 vs R1 清洗件）：恰 8 槽 24 字节、无意外槽，逐槽值与正确基址（0x7ffd379e0000）+ 原 offset 的独立重算**全部一致**——含 T019 实测命中槽 0x13f810 → `0x7ffd37ae6390`。对齐 A 类复扫归零（=0）、非对齐残留 13（全 .winlice，与总指挥预估完全一致，已入 pointer_map `residual_unpatched` 不盲改）、旧 urlmon 区间归零、vault 原件 3650ea6c 未动、补丁最小性 24 字节、pointer_map 完整（task=TASK-020R1、patches=8、residual_unpatched=13）、`crates/` 零改动、零实弹、新脚本 fork（`xx21b_core_session_cleanse_r1.py`，保留 D-039 版——可接受，同 T018→T019 fork 先例）。
- **总指挥记档（本战役第二次算术失误）**：R1 票面的期望值表在 0x141c10/0x141d48 两行**各丢一位**（写 `0x7ffd37a12f0`/`0x7ffd37a13f0`，正确 = `0x7ffd37a212f0`/`0x7ffd37a213f0`）——worker 的实际补丁值是对的，与票面表矛盾处 worker 未核对出（其"✓"对矛盾表打勾 = 逐槽核对不严，过程备注）；总指挥独立重算已裁决 worker 正确。**教训**：期望值表必须由脚本从同一数据源生成，不许手算；worker 对权威记录的矛盾值应当场上报而非打勾（同 D-040 worker 服从错误记录的教训互为镜像——这次是 worker 的值对、我的表错）。
- **vault 勘误入位**：`xx21b_t05/task020_cleanse/` 增加 README_ERRATUM.md（d9edf473 = T020 作废版；094f5401 = R1 有效版）+ R1 版清洗件与 pointer_map（总指挥补入）。
- **T0.5 三态重跑就绪清单**：清洗件 094f5401 ✓、pcell harness（动态导出解析 + 调试泵 + 基址记录）✓、宿主 a852880a ✓。**重跑票面将写入**：① 基址硬门（core 实际加载基址 ≠ 首选 0x7ffe1da10000 → Run 触发前 fail-loud 弃判——候选 3549 个自引用指针基址锁死，重定位 = 必败）；② `.winlice` 13 个非对齐残留风险注记（若再爆且地址落在残留内 → 凭证据逐个处理）；③ BootTime 连续性自查（同 T018/T019 先例）。
- **待老板批最后 1 格**（12/4 → 13/4）跑三态。

## D-042 批 TASK-021（T0.5 三态重跑·收官之战，XC-XXI-B 12/4 → 13/4）

- **日期**：2026-08-30（老板令："批准"——回应总指挥提案：清洗后候选路线三态重跑，最后一格）
- **决策**：① 批 1 格实弹（12/4 → 13/4）；② 判定语义与 T017/T018/T019 逐字一致（FULL/新阻塞/AV/附加改变行为；FULL = 路径级结论，T019 票面语义边界沿用）；③ **基址硬门**（本单核心保险丝）：候选首选基址从 PE 头动态读取、实际基址从 LOAD_DLL/enum_modules 动态读取，不等 → `FAIL_CORE_BASE_RELOCATED` 在触发 Run 前 fail-loud 弃判——候选 3549 自引用指针基址锁死（D-040 发现），重定位即必败，T019 实测被重定位过；④ run_head 明文预检（触发前确认非 C-8 加密态）；⑤ 修改 `tools/xx21b_t05_ui_drive_pcell.py`（sha 门 → 094f5401 + 基址硬门 + 明文预检），其余机制零改动。
- **保险丝（写入工单）**：AV 地址落在旧 ntdll 死区间或旧 urlmon 区间 → 明确标注"`.winlice` 残留命中"→ STOP；BootTime 连续性自查（宿主与清洗候选都绑定本 boot）；新阻塞/AV/附加改变行为/硬门触发 → 全部 STOP 如实上报。
- **前置（总指挥亲验，2026-08-30）**：清洗件 094f5401 在 vault（总指挥逐槽验证 8/8，D-041）；宿主 a852880a / config.ini 在位；HEAD = `1bf8b86`；BootTime = `10:05:53.549` 连续无重启。
- **落地**：`tickets/TASK-021.md` 首行已写授权令牌（worker 须原文回抄）。

## D-043 TASK-021 审计：STOP 接受（有条件通过，3 处更正）；基址硬门实弹确认（里程碑）；陈旧指针真实规模 = ~230 槽 ≥8 族（非单族）；下一票选项 A/B/C 待老板裁决

- **日期**：2026-08-30（总指挥审计，全部独立复算，不采信报告原文）
- **判定接受**：新阻塞类 AV（宿主引导沉降期 `0xc0000005 @ 0x7ffeeb1ab7b0`，Run 触发前），2/2 可复现，按票面 STOP。账本 XC-XXI-B 12/4 → **13/4**。
- **独立验证清单**：两趟 ndjson 逐事件同构（56 = 1+39+6+3+6+1，与报告逐字一致）；第 46 行均为 `LOAD_DLL base=0x7ffe1da10000` → **基址硬门判据成立 2/2，BASE-LOCK 理论（3549 自引用 vs 4 重定位）实弹确认**——本单最重要的正面科学结论；AV 逐位一致（Rip=AV 地址=取指 AV；Rax=0x7ffe1db4c4c0 ∈ .bss 指针表区）；部署 sha 3/3；8/8 清洗槽完好；RVA 0x142090 qword = 0x7ffeeb1ab7b0 逐位一致；宿主该族 0 命中；vault 7/7 sha 一致；13 个 `.winlice` 残留恰好 13 全未对齐（T020 记录精确吻合）；BootTime/防火墙/残留进程现场复核干净。
- **更正（记档，不改 worker 原文；详见 runs/20260830-TASK-021.md 审计附注）**：
  - **F1**：报告称 att2 base_agreement 与 att1 同构（equal=true）——att2 JSON 实为中间版脚本产物（`equal:false`，旧 schema）。实跑顺序 att2（21:06，旧版）→ 修脚本 → att1（21:11，终版）。底层事实不受影响（att2 ndjson 同样有首选基址 LOAD_DLL），系误引非造假。
  - **F2**：DllCharacteristics 实为 0x20（HIGH_ENTROPY_VA），非报告所写 0x0；DYNAMIC_BASE 确实清除，结论不变。
  - **F3（规模升格）**：worker 报告的"0x7ffeeb1axxxx 族"是冰山尾部。总指挥全文件普查（0x7ffc-0x7fff 全域含未对齐）：**~230 个 8 对齐陈旧指针、≥8 个旧会话模块区**（0x141e90–0x142120 连续函数指针表 ~70 槽；.bss 0x13f7d8+ 簇 ~110 槽；0x142170+ 簇 ~27 槽；0x7ffeea0f/a12/a8d/a8f/a90 区 ~35 槽），含恰好等于模块基址的句柄槽（0x7ffeeb130000/0x7ffee9500000/0x7ffee9ee0000/0x7ffeea0f0000/0x7ffeea120000/0x7ffeea8d0000）。全部低于 T020 窗口底（旧 ntdll 0x7ffeeb320000）逃逸——T020 扫描按已知死区间设计，旧会话其余模块区无从知晓，**结构性盲区，非执行过错**。即使只清 0x7ffeeb1a 单族也到不了 Run。另 3 个未对齐旧 urlmon 区疑似命中（.winlice/.boot，存疑不动）。
- **下一票选项（待老板裁决）**：
  - **A. 全量清洗 R2**：离线普查已完成（本条 F3 地图），模块身份可用"偏移对齐当前系统 DLL 导出表"离线匹配打分（零格）；补丁全部对齐槽 + 句柄槽后实弹复跑 1 格。风险：加密区（.winlice/.boot）内不可见残留不可知尾部；身份误配 → 下一 AV；可能再烧格。
  - **B. 当前 boot 重产候选**（总指挥推荐主线）：在当前 boot 会话重跑 T0.4 管线（dump→rebuild→S1-S4），所有会话指针按构造为当前会话值，**结构性消灭 C-5 类**，且同会话完成 T0.5 三态重跑 = FULL 判定证据最干净路径。成本：管线级工单（T0.4 量级），产物绑定当前 boot（重启后需重产——与 C-5 教训自洽，写入产物元数据即可）。
  - **C. 归档**：接受"候选 = 会话绑定产物，仅生产 boot 有效"边界，T0.5 以此收官，熊熊战役按现有证据（Run→GUI 层已达，urlmon 路径受会话绑定阻断）关闭。
- **经验教训（对 D-041 镜像补一条）**：worker 报告对"族"的定性（单族）未经全量普查即下结论——**法证结论必须先做全域普查再定性**；总指挥审计中自己的扫描模式也出过假阴性（`0x7ffF*` 模式漏 `0x7ffE*`），重扫后修正——扫描器本身也要自检覆盖域。

## D-044 批 TASK-022（D-043 选项 B：当前 boot 重产候选 + 同会话三态重跑收官；XC-XXI-B 13/4 → 14/4）

- **日期**：2026-08-30（老板令："当前 boot 重产候选"——D-043 三选项裁决，选 B）
- **决策**：① 在当前 boot 会话（BootTime `2026-08-30 10:05:53.549`）重产 core 完美候选：活体 dump（无调试附加，规避 C-8；mida-cli `/dump-module --module=core.dll --keep-runtime-base`，XX21 §3 配方）→ 固化（T0.4 §2.1 口径：EP=NOP stub、IAT 修复、节保留）→ S1-S4（T0.4 Step3 同口径，离线 0 格先例）；② **会话指针普查硬门**（本票存在的意义）：普查器对活体 module_map 全域校验，明文节 8 对齐违规 = 0 才放行，未对齐/加密区命中入册不盲改——把 D-043 的普查发现变成常设验收门；③ 同会话 T0.5 三态重跑（≥2 趟，语义逐字沿用），FULL = Run→urlmon + 存活 + ≥2 可复现；④ 实弹 1 格（生产 dump 含重试 + 重跑多趟；T018 5 次/1 格、T0.4 3 次触发/1 格先例）——若老板偏好生产/重跑分 2 格记账，改票即可（票面已注）。
- **时间硬约束（写入票面）**：产物与宿主 a852880a 都绑定当前 boot；**重启 = 本票全部产物作废**；开跑前 + 收尾前双查 BootTime，变动即 STOP。
- **工具落地**：`tools/xx21b_session_pointer_census.py`（总指挥 D-043 普查固化版）——扫描模式覆盖 0x7ff0-0x7fff 全域（防 T020 型已知窗口盲区）、**自校准硬门**（对已知答案 094f5401 逐项核对 6 指标：own-image 4865 / 旧 ntdll 13 全未对齐 / 逃逸族 73(69 对齐) / KUSER 3，总指挥实测值；校准不过禁出报告，防 D-043 总指挥自己扫描模式假阴性类事故）；KUSER_SHARED_DATA 合法常量白名单。
- **落地**：`tickets/TASK-022.md`（⛔ 首行令牌，D-015 格式）；前置总指挥亲验：HEAD `1eac91f`、BootTime 连续、宿主 a852880a / 原版 core 09f3dd34 / config cde9be13 在位、自校准 PASS 6/6（EXIT=0）。

## D-045 TASK-022 审计：任务 1/2/3 全部接受，STOP 成立（C-9 新开）；普查门裁定 = 实质 PASS + 工具 v2 闭环；**C-5 对管线产出路径结构性关闭**；账本 14/4 确认

- **日期**：2026-08-30（总指挥审计，全部独立复算）
- **任务 1 PASS**：活体 dump（C-8 合规——无调试附加、RPM 只读观测）；core 以当前 boot 基址 0x7ffc57e30000 加载、.text 明文沉降（RPM 验证 prologue）；`/dump-module --keep-runtime-base` 产物 .boot sha 与磁盘原版逐位一致（总指挥独立重算）；`fix_hardcoded_addresses` 重锚 3500 指针（本票核心价值）；module_map（51 模块）绑定 dump 会话。
- **任务 2 PASS**：固化（EP=NOP stub，T0.4 同款 8 字节变换）→ 候选 096f3bdf；S1 R0B 12/12 / S2 明文全过 / S3 6/6 首选基址 / S4 ×10 一致 + 页级零变化（S4 数值口径裁定 = base+0x13c4c0 同语义，票面已预告基址必变）；21 节 vs T0.4 23 节差异 = 尾部 2 个重建节，实质节全在，可接受。
- **普查门裁定：实质 PASS + 工具 v2 闭环**。总指挥将 60 个 v1 违规逐一定性：25 INT64_MAX 哨兵 + 19 x86 指令立即数伪读（`CMP R10W,0x7FFF` 类，壳 VM 地址分类代码）+ 6 跨字段 RVA 伪读（低 32 位 = 合法 RVA 的元数据拼接）+ 10 厂商继承常量（同值在原版 09f3dd34）= **0 会话指针**；真旧会话区间对齐明文 0 命中，8 个未对齐 .winlice/.boot 命中解码为 VM 32 位立即数（`XOR ECX,0x7FFEEA8F` 类旧会话地址高位字）→ residual 不盲改。**普查器 v2**：五类判别 + 判别力锚点（094f5401 → hard=144 FAIL；096f3bdf → hard=0 PASS）——worker 的"已知答案对照"方法论固化进工具。residual 7484 与 worker 分类一致。
- **任务 3 STOP 接受（新阻塞类 C-9）**：宿主引导期干净退出 exit 0，3/3 泵 + 3/3 普通启动；对照实验出色（原版 core → 存活+授权窗口 / 094f5401 → AV 0xC0000005 / 新候选 → exit 0）→ exit 0 为新候选引发的宿主引导行为改变，Run 不可达。`FAIL_HOST_EXITED_AT_BOOT` 为脚本既有逻辑（非私改）。
- **C-5 结构性关闭（管线产出路径）**：dump 时重锚 3500 + 普查实证 0 会话指针 + 重锚指针落进 20 个当前会话模块——杀死 T019/T021 的缺陷类在管线内根治。T0.4 旧候选仍为会话绑定历史工件。
- **更正（不改 worker 原文）**：F1' 令牌回抄中段乱码（规范令牌以票面为准）；F2' 对照实验原始日志被当临时文件删除（承载归因的证据仅存报告粘贴——**新 P 系教训：归因性对照实验原始记录不属于临时文件**；缓解 = 泵 3/3 全量证据入 vault + 现象确定性可复现）；F3' 票面起草缺口（§6.1"仅 CAND_SHA" vs §6.2 新部署目录必然要求 DEPLOY 变更——责任在总指挥，worker +6/−6 合理）。
- **停止规则提示**：T0.5 FULL 验收已连续 2 单不通过（T021 AV / T022 exit 0）→ 按 AGENTS.md 停止规则，下一步（C-9 根因诊断 1 格 vs 战役收官归档）交老板裁决，不自行派单。
- **账本**：XC-XXI-B 13/4 → **14/4** 确认（生产 dump + 3 趟重跑，1 格，票面口径）。

## D-046 批 TASK-023（D-045 选项 ①：C-9 根因诊断；XC-XXI-B 14/4 → 15/4）

- **日期**：2026-08-30（老板令："C-9 根因诊断"——D-045 两选项裁决，选 ①）
- **决策**：① 诊断 only 不修复：调试泵 + **int3 退出漏斗断点**（ntdll!NtTerminateProcess / ntdll!RtlExitUserProcess / kernel32!ExitProcess / kernel32!TerminateProcess——导出 RVA 从 System32 磁盘 DLL 动态解析，断点 VA 用泵 LOAD_DLL 实时基址，零硬编码）+ **候选 EP 判别位**（NOP stub DllMain 是否被调用 → 区分"加载器初始化失败"vs"初始化成功后宿主/VM 决定退出"）；② 命中法证 = 真地址（Rip−1）+ 模块归属 + 全寄存器 + 栈链 rsp[0..16] 逐 qword 归属——**退出决策者归属表 = 本票核心交付**；③ 验证趟（host_loader 隔离加载预期 0 命中存活——证明断点不误触发）先于诊断趟（≥3 趟）；④ 诚实结局预定义：0 命中 = 直接 syscall 绕过漏斗（有效结论）；仪器化改变行为 = STOP。
- **纪律要点（写入票面）**：int3 仅内存驻留（磁盘零改动、部署 sha 前后双验）；诊断 only 不修宿主/候选/指针；**BootTime 存在性硬门**——候选重锚指针绑定本 boot，重启 = 现象不可复现 = 票作废。
- **账本**：14/4 → **15/4**（1 格：验证趟 + 诊断趟含重试，T018 先例）。
- **落地**：`tickets/TASK-023.md`（⛔ 首行令牌，D-015 格式）；前置亲验（2026-08-30）：HEAD `c787526`、BootTime `10:05:53.549` 连续、三件 sha 在位、EP RVA 动态读通（0x1027c0）、fork 基座在。

## D-047 TASK-023 审计通过（C-9 根因大幅收窄：宿主侧 CRT exit(0) 发生在 core DllMain 返回后 2.4ms；加载器假设排除；账本 15/4 确认）

- **日期**：2026-08-31（总指挥审计——worker 报告未送达聊天，全部从文件系统取证：runs 报告 + 脚本 + lab/vault 证据；BootTime 复核仍 = `2026-08-30 10:05:53.549`，worker 于 00:00-00:08 同 boot 内执行，候选有效前提成立）
- **审计通过**：任务 1 工具（4 断点动态解析零硬编码——总指挥 grep 复跑 0 命中；int3 re-arm 全趟成功）；验证趟（host_loader 0 命中 + 存活 + EP 正控制）；诊断趟 3/3 逐位一致。磁盘 sha 前后不变（int3 内存驻留确认）；crates 零改动；vault 13 件 + INDEX。
- **核心交付复算成立**：退出链 = ExitProcess（ret = msvcrt+0x3e2c9）→ RtlExitUserProcess（ret = kernel32 ExitProcess+0xb）→ NtTerminateProcess（ret = RtlExitUserProcess+0x47）→ [DllMain detach] → NtTerminateProcess（teardown 重入）。msvcrt+0x3e2c9 = `_exit+0x89`（离线精确验证）；链上所有 RVA 经 kernel32/ntdll 磁盘字节验证（`ff 15` IAT 间接调用 / `e8` 直调目标 0x1608f0 精确命中）。寄存器 Rcx=0 / R11=0xc0000135 / Rbp=0x140112058（宿主 .bss）3/3 一致。
- **总指挥记档（自错自纠）**：审计中总指挥初版导表快速解析得出与 worker 不同的 ntdll RVA——根因 = 名字索引直读函数表、跳过 AddressOfNameOrdinals 序号间接寻址；正确重解析后与 worker 逐位一致。**教训（P 系补条）：解析 PE 导出表必须经序号表间接寻址，名字索引直读在 ntdll 级大导出表上必然出错；worker 工具与总指挥初判矛盾时，字节级（BP_ARM orig / call 目标）是最终裁决。**
- **C-9 根因状态**：① 排除"加载器初始化失败"（EP 判别位：NOP stub DllMain 被 LdrpCallInitRoutine 正常调用 attach+detach 3/3）；② 退出 = 宿主侧代码经 CRT `exit(0)`（Rcx=0），发生在 core.dll 加载返回后 2.4ms；③ R11=STATUS_DLL_NOT_FOUND 强提示但 volatile 未定死；④ 剩余未知 = LoadLibrary(core) 返回 → exit(0) 之间宿主的判定逻辑（候选 VM 交互失败 / 宿主校验分支，未覆盖）→ **下一步：离线静态分析宿主退出调用点（零实弹不烧格，TASK-024 候选）**。
- **账本**：XC-XXI-B 14/4 → **15/4** 确认（验证趟 + 诊断趟 3，1 格，票面口径）。

## D-048 派 TASK-024（C-9 离线静态分析，零格；老板 2026-08-31"派这张离线票"）+ C-9 机制假设记档

- **日期**：2026-08-31（老板追问"宿主没输入注册码调用 Core 肯定没啥反应吧" → 总指挥以 T017 三方对照回应：**无注册码的正常行为 = 弹"授权验证"对话框等待（3/3），而候选组合连窗口都没建就 exit(0)**——行为不等价成立；老板随即批离线票）
- **机制假设（[推断]，待 TASK-024 证实/证伪）**：宿主加载 core 后 2.4ms 内查询壳状态；原版 core 的 DllMain = 真壳入口（加载即自举，C-8 互证），**T0.4 固化设计把 DllMain 换成 NOP stub → core 沉睡 → 宿主查询失败 → exit(0)**。旁证：T0.4 老宿主 36043cb4 + 同样 stub 的候选 = 存活且业务链进消息循环（老宿主容忍沉睡 core）；新宿主 a852880a（T015 产物，还原度更高）对沉睡 core 零容忍。若证实，修复方向 = 候选侧"最小壳自举"，而非候选本身坏了。
- **红线重申**：我们从不绕过授权（NO_BYPASS=1）——授权对话框流程本身是行为等价的一部分，验证的是"还原 core 让宿主走到与原版相同的授权框"。
- **落地**：`tickets/TASK-024.md`——离线零实弹零格（禁止执行任何样品），五任务：① 宿主 exit 族调用点全列（注意 rev2 重建 PE 导入布局非标准，dumpbin 或自研解析，禁 pip 新依赖）；② 引导路径 exit 点 + 守卫条件精确到指令；③ 三方行为自洽解释（094f5401 AV vs 096f3bdf exit 分叉逻辑 + 候选 core.dll 是否导入 exit 族的判别）；④ 老宿主 36043cb4 对照 diff；⑤ 修复方向提案（不实施）。证据 → vault `xx21b_c9_static/`。

## D-049 TASK-024 审计通过（零格确认）；D-048 假设主分支获支持、"新宿主更严"子分支被否决；C-9 收窄至宿主 Themida VM 区查询（静态不可达）；下一步 A/B/C 待老板裁决

- **日期**：2026-08-31（总指挥审计，全部逐字节独立复算）
- **审计通过**：exit 族 4 跳板 + 4 明文调用点字节级吻合；守卫链 rip-relative 精确到 [0x112008]（总指挥独立算出同址）；**.text identical 独立复现**（a852880a vs 36043cb4，0xdda00）——137 差异字节全为会话绑定指针（.data 68/.bss 42/.idata 27），.bss 0x112c10 双值与 C-5 记录互证；候选 12 模块导入零 exit 族独立确认（候选侧退出排除成立）；pefile/capstone 为本机既有（无新装，离线分析用途可接受，记档）；零实弹确认（HEAD 未动、零 tracked 改动）；vault 6 件。
- **科学结论**：① 实际 exit(0) 触发点 = 宿主 **Themida VM 区**（明文调用点会留宿主帧，T023 栈上无）→ 静态不可达；② exit(0) 调用者 = 宿主侧 CRT/VM（候选侧三查排除）；③ 分叉模型成立：原版自举→对话框 / 陈旧指针→AV / NOP stub 沉睡→exit(0)。
- **D-048 假设裁决**：主分支（沉睡 core → 查询失败 → exit(0)）获支持；**"新宿主检查更严"子分支被否决**（.text 逐字节一致）——归因转移到候选状态差异（.winlice 材料化会话差异 / 重建节差）+ 会话数据；老宿主在当前会话因自身 C-5 无法启动，老组合存活不可复现（对照价值 = 排除代码差异）。
- **下一步选项（待老板裁决）**：**A. 最小自举实验**（1 格 15/4→16/4：候选 EP 指回原壳入口 0x8a0108（8 字节离线改动）+ 实弹一跑；结局 (a) 宿主走到对话框 = C-9 破解 / (b) 壳反调试触发（C-8 同族，概率不低）= 证明 pump 方法论与活壳冲突 → 迫使 T0.5 FULL 语义裁决（RIP 口径 vs 窗口证据口径，T017 建议②）/ (c) 壳在重建映像上无法自举 = 死路入档——三种结局都是决定性发现）；**B. 收官归档**（C-9 已达 VM 区粒度，成果入册）；C. 持有。
- **账本**：15/4 不变（TASK-024 零格）。

## D-050 批 TASK-025（D-049 选项 A：最小自举实验；XC-XXI-B 15/4 → 16/4）

- **日期**：2026-08-31（老板令："A. 最小自举实验（1 格，15/4 → 16/4）"）
- **决策**：① 候选 EP 字段 0x1027c0（NOP stub）→ 0x8a0108（原壳入口，.boot+0x108）——**4 字节离线改动**（文件偏移 0xa8-0xab），变体 = 096f3bdf 衍生、其余逐字节一致、NOP stub 遗留字节保留（最小改动）；② 实弹对照矩阵：**非附加对照 ≥2 趟（主判据：窗口清单/退出码/存活）** + 泵诊断趟 ≥2 趟（xx21b_c9_exit_trace.py 全断点，EP 判别位自动指向新壳入口）；③ 三种预期结局均有效：(a) 宿主走到授权对话框 = C-9 破解 / (b) 壳反调试触发（C-8 同族）= 泵方法论与活壳冲突 → T0.5 FULL 语义裁决升级老板议题 / (c) 壳在重建映像上无法自举 = 死路入档；④ 工具仅常量改动（DEPLOY → lab/xx21b_boot/、CAND_SHA → 变体 sha）。
- **纪律**：BootTime 存在性硬门（变体继承重锚指针，重启 = 票作废）；诊断 only 不改其它任何字节；防火墙只读；git 只读；crates 零改动。
- **账本**：15/4 → **16/4**（1 格：非附加对照 + 泵趟含重试，T018 先例）。
- **落地**：`tickets/TASK-025.md`（⛔ 首行令牌，D-015 格式）；前置亲验（2026-08-31）：HEAD `21eccf7`、BootTime 连续、候选 096f3bdf EP 字段 = 0x1027c0（文件偏移 0xa8）、0x8a0108 ∈ .boot、工具基座在。

## D-051 TASK-025 审计通过（结局 (a) 窄口径：C-9 exit(0) 破解获实弹证实；对话框判据未满足 → 新观测 C-10；账本 16/4 确认）

- **日期**：2026-08-31（总指挥审计，全部亲手复算；BootTime 审计时刻三查仍 = 2026-08-30 10:05:53.549）
- **审计通过**：变体 diff 恰 3 字节（0xa8-0xaa，比票面 4 字节字段改动更严，worker 披露诚实）；EP 独立解析 0x8a0108；非附加对照 3/3（存活 30/30/60s、窗口 NONE、EnumWindows/FindWindow 交叉核对 + Get-Process Path 身份核验）；泵诊断 2/2（壳入口命中 Rdx=1 attach、ret=LdrpCallInitRoutine、true_addr 算术吻合、arm orig=0xe8、漏斗 0、attach_changed=False）；工具 diff 恰 14 行授权常量；crates 零改动；git 只读。
- **结局裁定**：**(a) 窄口径成立 = C-9 破解**（exit(0) 消除 + 根因实弹证实：T0.4 固化的 NOP stub 是根因，恢复壳入口即愈）。**(a) 宽口径未满足**：授权对话框未出现（存活无窗口）→ **新观测 C-10**（宿主+自举变体存活无 UI，自举已启动未达窗口创建，原因未深挖）。
- **重要旁证**：泵下 attach_changed_behavior=False、无 WinLicense 对话框 → **C-8 反调试未在变体组合触发**（对照 T018 原版 core + 泵触发 WinLicense）→ 调试泵与自举变体兼容，T0.5 方法论可用。
- **更正（微）**：vault 计数 10+INDEX（报告写 12+INDEX）；泵健康 continues=54 未独立复核（JSON 字段 None，ndjson 在档，采信）。
- **下一步选项（待老板裁决）**：A. TASK-026（1 格 16/4→17/4）变体组合深观测 + T0.5 三态尝试（分钟级观测 + RIP 采样 + Run 触发，语义逐字）；B. 收官归档（C-9 破解 + 4 字节修复方案 = "EP 指回原壳入口"入册，C-10 缺口定档）。
- **账本**：15/4 → **16/4** 确认（非附加对照 3 + 泵 2，1 格，票面口径）。

## D-052 批 TASK-026（D-051 选项 A：变体组合深观测 + T0.5 三态尝试；XC-XXI-B 16/4 → 17/4）

- **日期**：2026-08-31（老板令："A. TASK-026（1 格，16/4 → 17/4）"）
- **决策**：① 深观测趟 ≥2（C-10：180s 窗口枚举 + 全线程 RIP 采样 + 线程数——窗口是否迟到、宿主存活态在做什么）；② Run 触发趟 ≥2（CreateRemoteThread Run + 三态判定语义逐字沿用 T017-T021：FULL = RIP 入 urlmon + 存活 + ≥2 可复现；urlmon IAT 槽 0x16f300 动态解析前后值 + 页级变化全记录）；③ 工具 = pcell harness 常量改动（CAND_SHA → 7b470117 / DEPLOY → lab/xx21b_boot）+ 最小观测扩展（Run 前深观测段，diff 披露）；④ 基址硬门/run_head 明文预检/`.winlice` 残留标注零改动（变体 ImageBase 0x7ffc57e30000 动态读、Run prologue 明文已预验 415741564155）。
- **前提（D-051 旁证）**：泵与自举变体兼容（attach_changed_behavior=False、无 WinLicense）→ T0.5 方法论可用。结局开放：消息循环 = PARTIAL 重演（T0.4 Step1 同型）/ urlmon = FULL / 新阻塞 = C-10 定性 / AV = STOP。
- **纪律**：BootTime 存在性硬门（变体重锚指针绑定本 boot）；观测+触发 only 不改任何字节；授权红线重申（不输入真实注册码、不绕过授权，只观测窗口与消息泵）。
- **账本**：16/4 → **17/4**（1 格：深观测趟 + Run 触发趟含重试，T018 先例）。
- **落地**：`tickets/TASK-026.md`（⛔ 首行令牌，D-015 格式）；前置亲验（2026-08-31）：HEAD `eb91329`、BootTime 连续、变体 7b470117 在位（EP=0x8a0108、ImageBase=0x7ffc57e30000、Run prologue 明文）、pcell 基座在。

## D-053 TASK-026 审计通过（新阻塞判定成立 2/2；总指挥修正卡点定性：NtWaitForSingleObject 非"条件变量"；账本 17/4 确认）

- **日期**：2026-08-31（总指挥审计，全部亲手复算）
- **审计通过**：深观测 3/3（180s ×3 = 9 分钟零窗口 → C-10"迟到"排除；1 线程 .winlice VM 区单核满速轮询 + 其余线程 ZwWaitForWorkViaWorkerFactory 空闲等待；CPU 1:1 线性）；Run 触发 2/2（Run 线程 1043/1043、1044/1044 采样 100% 稳定卡同一 RIP；urlmon 0 命中、IAT 槽 0x7ffd1014e470 前后不变、页级零变化、无 AV、进程存活）；工具 41 删/341 增逐行核对全为常量/标签/签名（三态机制零改动）；vault 10 件 + INDEX；crates/ 零改动；git 只读。
- **实质性修正（记档）**：worker 报告"卡 ntdll+0x60404 = 条件变量等待"系**丢前导 1 的算术错误**（真值 = ntdll+0x160404）并据此在错误偏移反汇编。总指挥裁决：**0x160404 ∈ NtWaitForSingleObject/ZwWaitForSingleObject（RVA 0x1603f0）桩区 = 阻塞于 NtWaitForSingleObject 系统调用**；"条件变量"定性作废。新阻塞判定本身成立（稳定卡死事实与 0 urlmon 证据链不受影响）。**P 系教训补条：RIP→RVA 必须工具自动计算入证据，禁止人工转抄（丢位错误 + 错误传播到反汇编偏移）。**
- **修正后 C-10 定性**：壳自举 → .winlice VM 轮询循环（单核满速）+ Run 线程阻塞于 NtWaitForSingleObject（等待从未 signal 的内核对象）→ 窗口/urlmon 不可达。剩余未知 = VM 区等待/信号逻辑（静态不可达，黑盒观测已到边界）。
- **账本**：16/4 → **17/4** 确认（深观测 3 + Run 2，1 格，票面口径）。
- **总指挥建议**：**收官归档**（B）——T0.5 FULL 经整链诊断未达，剩余阻塞位于 Themida VM 内部状态机（每层剥离成本 1 格且黑盒已到边界）；战役成果完整（C-5 根治 / C-9 破解 / C-10 定性 / 全套诊断工具链 / 会话干净候选管线）。收官总结待老板确认后交付。

## D-054 外部专家咨询答复审计通过（路线 (a) 快照重建判死获外部佐证；采纳 (c) 干净重写为唯一工程可行线；总指挥修正专家三处）

- **日期**：2026-08-31（总指挥审计）
- **归档**：咨询简报 `docs/EXPERT_CONSULT_BRIEF_20260831.md`（commit 2faf806）；专家答复原文逐字归档 `docs/EXPERT_CONSULT_REPLY_20260831.md`。外部意见性质 = 外证，未经我方实验独立验证。
- **审计结论（逐条对照我方证据链，零矛盾）**：
  1. **C-10 表征 = SecureEngine "silent tamper response" 典型指纹**（VM 化 opaque loop + 永不 signal 的等待对象 + 无 AV 无窗口无网络的沉默档）——与我方 T025/T026 全部观测吻合；采纳为 C-10 工作假设（外证级）。
  2. **路线 (a) 判死**：环境指纹密钥源料 + 滚动解密/就地擦除 + 句柄/线程/TLS 绑定三机制叠加，快照在新进程必不自洽——与我方十票证据链（C-5 修指针仍不够、EP 修回仍卡）方向一致；专家同时确认我方"已做到公开案例极限"（三前哨 = DYNAMIC_BASE/会话指针/EP stub）。
  3. **路线 (b) VM 追踪 ROI 极差、路线 (c) 干净重写（GetAppVersion/Run 2 导出契约）最现实**——与 D-053 后总指挥建议一致。
- **总指挥修正专家三处（记档）**：
  1. **"mock 后端镜像抓原版流量"在本实验箱不可行**：原版 core 内 URL 为密文且 IP 直连（hosts 不适用）、防火墙只读、不装新依赖 → 改用我方调试泵在 API 层截获请求形态（WinINET/urlmon/ws2_32 入口断点，现成方法论），等效拿到"请求形态"证据。
  2. **Q5 离线/在线判别在无注册码前提下只能给"大概率"级**：boot→窗口流量不含验证流量（验证发生在输码后）；决定性判别仍卡"无码"。
  3. **机制细节偏差**：专家称信号对象"只在原进程首次 boot 时产生"——我方 T025 证据显示变体在新进程内壳入口确被调用（ret ntdll+0x15e06a = LdrpCallInitRoutine），故 Run 所等句柄应为新进程内合法创建的内核对象，缺的是 **signal 侧路径**（专家框架仍适用，卡点归因细化）。
- **权利门槛（仍开）**：(c) 的终点为可部署产品 → 立项前需老板权利依据（D-054 重申，不因外部意见豁免）；clean-room 隔离流程（spec 侧 = 我方行为证据库，实现侧 = 新写代码，双历史）按专家建议纳入未来范围设计。
- **待老板裁决**：A 收官归档（战役总结交付）；B 另立 CORE-REWRITE 授权范围（前置 = 权利依据 + 老板新服务器端点清单）。二者不互斥：先 A 后 B。
