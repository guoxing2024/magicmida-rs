# 总指挥审计裁决 — Batch 28

**审计日期**：2026-08-23  
**实际审计 HEAD**：`9589fd13f8e45e7612b212335bcae4c0b1ede23e`（短哈希 `9589fd1`）  
**Batch 28 范围**：`dea085b..9589fd1`  
**git 实测**：1 commit、6 unique paths、+154/-19；全部为 `docs/` 或 `docs/fixtures/`，`crates/` diff lines = 0。  
**性质**：只读审计裁决；不实现 Walker/V2 runtime/CLI，不授权 LIVE-4。

## 1. 总裁决

| 工单 | 裁决 | 原因 |
|---|---|---|
| WO-2801 | **CONDITIONALLY ACCEPTED — evidence valid for dea085b only** | `evidence_head_2801.txt`、范围统计和 workspace 分层可复核，但实际当前 HEAD 已前进到 `9589fd1`。 |
| WO-2802 | **CONDITIONALLY ACCEPTED — raw values correct, two wording/footer defects remain** | 127B 原始字节已补齐，双流偏移正确；但 raw range `0x40..0x7F（63B）`端点错误，文档 footer 仍停在 928047f。 |
| WO-2803 | **CONDITIONALLY ACCEPTED — source facts only** | arithmetic 文档已标 `dea085b`，但不是当前 `9589fd1` 最终树 manifest。 |
| WO-2804 | **CONDITIONALLY ACCEPTED — source facts only** | schema 事实可复用，最终 HEAD 仍需更新。 |
| WO-2805 | **CONDITIONALLY ACCEPTED — source facts only** | caller=0 事实可复用，最终 HEAD 仍需更新。 |
| WO-2806 | **OPEN — stale metadata remains** | 当前树仍存在旧提交 footer/范围措辞；不能称全仓无旧 final-head 残留。 |

**implementation gate：FAIL / HOLD。**  
**Walker runtime/CLI：NOT DISPATCHED。**  
**Windows SEH/VEH live test：NOT AUTHORIZED。**  
**LIVE-4：NOT AUTHORIZED。**  
**自动重试：禁止。**

## 2. 已确认正确的部分

- `D:\Temp\evidence_head_2801.txt` 正确证明 `dea085b`；
- `928047f..dea085b` 统计为 1 commit / 7 paths / +191/-40；
- `crates/` 零修改；
- production/test exact-byte 非连续切片、SHA 和 `PROD == FIXTURE` 结论成立；
- 127B raw bytes 已在 `docs/WO-2601-thunk7-probe-closure_20260823.md:109-118` 完整列出；
- threecheck sentinel、本机 x64 三项检查仍只属于 local/offline evidence。

## 3. 仍未关闭的问题

### 3.1 最终 HEAD 再次前进

实际当前 HEAD：

```text
9589fd13f8e45e7612b212335bcae4c0b1ede23e
```

但当前证据绑定的是：

```text
dea085b
```

`9589fd1` 又修改了 6 个 docs/fixtures 文件，因此不能继续沿用 `dea085b` 作为当前最终树 manifest。

### 3.2 127B raw range 端点错误

```text
docs/WO-2601-thunk7-probe-closure_20260823.md:120
```

当前写法：

```text
0x40..0x7F（63B）
```

正确表达应为二选一：

```text
0x40..0x7E inclusive = 63B
```

或：

```text
obj[0x40..0x7F) = 63B
```

这不是字节值错误，但属于 exact-byte 文档边界错误，必须修正。

### 3.3 exact-byte 文档 footer 仍停在旧树

```text
docs/WO-2601-thunk7-probe-closure_20260823.md:142
```

仍写：

```text
WO-2701 ... 绑定 928047f
```

文档顶部和正文已使用 `dea085b`，footer 未同步。应补充 Batch 28 / `9589fd1` 绑定关系，或明确旧 footer 为历史段落。

## 4. 生产实现门禁

当前生产代码仍未接入：

- `WalkerExecute`
- `MidaAntidebugInitializeV2`
- production 7-arg thunk
- Walker protocol production caller
- V2 attestation/digest consumer
- orphan/unconfirmed acceptance

`D:\Claude project\magicmida-rs\crates\antidebug-runtime\src\exports.rs:239` 仍为：

```text
adr4-foundation-unbound
```

继续保持：

```text
readiness          = ACCEPTED
implemented        = NOT IMPLEMENTED
acceptance allowed = NOT ALLOWED
```

**本裁决状态：Batch 28 HOLD，进入 Batch 29。**
