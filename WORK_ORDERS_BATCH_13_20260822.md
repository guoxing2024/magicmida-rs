# 工作单批次 13 — GTO 完美脱壳主攻(非 dump 路线)· 总指挥签发

**owner 决策(2026-08-22 生效)**:
1. **主攻 `D:\Tools\RE\dumps\gto\启动器.exe` 完美脱壳**;
2. **push 政策变更**:今后不再 push,仅本地仓库;整个项目彻底完成才考虑 push。
   (本批及后续所有工单默认本地提交;A1 违规语境下"禁止 push"升级为"无需 push"。)

---

## 一、战略重构(总指挥裁定)

### 1.1 终局报告地位修正
`GTO_TERMINAL_CHARACTERIZATION` 的结论**对 dump 路线继续成立**,但其结构天花板论证
自带出口条款:*"除非覆盖率=100% 且覆盖所有可达路径"*。主攻方向即此出口:
**用保护器自己的按页解密机制,把覆盖率推向全量,再 dump。**

### 1.2 新战场与账本
- Battlefield: **GTO-PERFECT-2**(非 dump 路线)
- 核心洞察(本战役的资产基础):
  ADR-6 自研 runtime 注入链已在 Oreans 线生产验证——它是往目标体内送
  "自己人"的成熟通道,这是早期 Routes A-H 所不具备的条件;
- Ledger: cap=4 rounds(设计文档不耗轮)。

## 二、Route α 设计方向:在目标体内触发全页解密(设计先行,零代码)

**机制假说**: 按页解密大概率走 PAGE_GUARD/access-violation → 保护器内部 VEH →
解密该页 → 重执行。若成立,**从目标体内读一页 = 合法触发其自身解密**(与正常执行
同路径,非写入、非绕过)。我们的 coverage 测量用 RPM 从调试器侧读——**可能根本
不触发 guard 语义**,这解释了 LIVE-3 的平坦结果。

**Route α 步骤概要**(细节由 WO-1301 设计):
1. 复用 ADR-6 链注入自研 runtime(生产已验证);
2. runtime 内walker 线程:枚举 .rdata0/.rdata2 全部已提交页,逐页**读触碰**;
3. 每轮触碰后复用 coverage 测量口径统计覆盖率;
4. 若覆盖率随触碰上升 → 迭代至稳态;达到高覆盖(≥90%)→ 同轮 dump + smoke;
5. 若熵恒不动 → 假说证伪:解密只沿真实指令流(需要执行流引导,另立路线 β)。

## 三、前置未解问题(必须先诊断,否则 α 无法起跑)

LIVE-3 中窗口 300s 未现、执行近零——**目标在我们宿主下处于怠速**。候选原因:
(a) 裸调试器被检测(SecureEngine 调试端口检测→静默);(b) rev2 样本行为变化;
(c) 网络/license 检查阻塞。α 的触碰线程要跑起来,前提是目标进程本身活着且
VEH 链健康。

---

## 四、工单

### WO-1301(P0)Route α 设计说明(离线,docs-only,0 轮)

产出 `docs/GTO_P2_ROUTE_A_COVERAGE_FORCE_DESIGN.md`:
1. guard-vs-flow 判别实验设计:如何用一次廉价实弹区分两种解密机制
   (如:单页触碰前后差分、异常事件流分析);
2. 触碰 walker 规格:页枚举口径、触碰原语(目标体内读)、节奏限制、
   与保护器 VEH 的共存规则(**我们只读,解密由保护器自己的处理器完成**)；
3. 安全/fail-closed:触碰导致 AV 的处置、覆盖率不动的止损、
   对目标的零写入声明(读触碰除外,论证其等同正常执行);
4. 前置依赖判定:是否必须先解决窗口怠速(WO-1302 先行)?
5. 离线可测部分(覆盖率迭代纯函数等);
6. 明确不做:bypass/语义修复/解密算法逆向。

### WO-1302(P0)窗口怠速诊断设计(离线,docs-only,0 轮)

产出 `docs/GTO_P2_IDLE_DIAGNOSIS_DESIGN.md`:在观察窗内采集
主线程 RIP 分布(只读 get_context)、线程等待原因(NtQueryInformationThread 只读)、
全部顶层窗口枚举(不只 NewClassName)、模块加载事件流——区分假说 a/b/c 的判据表。
**注意**: 本设计与反反调试 Phase 1-3 接线(批次 10 已实现待激活)存在联动:
若判定 (a),则 `MIDA_ANTIDEBUG_MODE=self` 的首次实弹授权成为 α 的前置。

---

## 五、红线(继承 + 澄清)

- 设计阶段零实弹、零代码;实弹需 `GTO-PERFECT-2` 轮次内的新授权(LIVE-4,设计批准后签发);
- 读触碰=借保护器之手,等效正常执行——**除此以外任何目标内存写入仍属红线**;
- 无 bypass/语义修复/解密算法逆向;ADR7/Oreans 门不动;vault 封存证据不动;
- 本地提交即可,**无需 push**(政策变更生效);MSVC 环境;三件套门禁。

**执行顺序**: WO-1301 ‖ WO-1302(并行设计)→ 总指挥联审 → 实施单 → LIVE-4 授权申请。
**签发**: 项目总指挥 · 2026-08-22
