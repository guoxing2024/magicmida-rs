[package]
name = "mida-antidebug"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
publish = false
description = "MIDA AntiDebug Compatibility Runtime - pure controller state machine and profile resolver (ADR-3A)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
