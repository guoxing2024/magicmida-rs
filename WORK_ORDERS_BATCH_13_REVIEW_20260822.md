# 联审裁决 — 批次 13 设计复审(总指挥,2026-08-22)

**审查范围**: WO-1301(Route α)/ WO-1302(怠速诊断)两份设计 + LIVE-4 授权申请草稿。

---

## 一、WO-1301:**REJECTED — 退回修订(WO-1301A)**

### 致命缺陷(必须修正)

**F1 · 红线违规:DR0-DR3 硬件断点**(§2.2 L91、§7)
历批授权与路线图"不做项"均明令禁止 DRx。设计把 DRx 当核心定位手段——全部移除。
x64 下用户态调试器侧 DRx 对抗既越权又脆弱(KiFastSystemCall 模型仅 x86 适用)。

**F2 · 核心机制错误:探针原语不会触发 guard 语义**(§2.2/§3.2.1)
设计的触碰 = 调试器侧 `debugger.read_memory`(RPM)。**RPM 从进程外读取不经过目标的
异常分发——guard 页违例根本不会产生**,Phase 2 预期的
`STATUS_GUARD_PAGE_VIOLATION` 观察不到,假说判别实验自身失效。
这正是批次 13 工单指定 **"runtime 内 walker 线程"**(ADR-6 注入链)的原因:
只有目标上下文内的访问才会触发保护器的按页解密。设计偏离工单走了调试器侧,方向性错误。

**F3 · VEH 位置混淆**(§3.2.1 L208)
`install_veh` 注释为"调试器 vectored exception handler"——调试器进程的 VEH
处理不了目标上下文的异常;目标异常以 debug event 形式到达既有调试循环。

**F4 · 样本身份违规**(LIVE-4 草稿 §8.1)
"样本 = Route U R1 / Route V R0 超时案例"——历史工件不可作为实弹对象;
必须走 `resolve_gto_source_revision` 预检,仅执行 manifest 授权 vault 对象(rev2)。

**F5 · "预算无上限"**
治理要求有界轮次。LIVE-4 重报时给硬上限(建议 cap=2 rounds × 60min)。

### 保留部分(修订时沿用)

三态分类模型(Type A/B/C)、熵+反汇编判据、动态候选扩展策略、节流/动态止损概念、
fail-closed 决策树意图、LIVE-4 分阶段结构——框架合格,机制重写即可。

---

## 二、WO-1301A 修订指令

1. **探针原语重写**:walker = ADR-6 注入链注入的自研 runtime 内线程;
   触碰 = 目标上下文内普通内存读(自然触发保护器自身 VEH 完成解密);
   我们只观测熵差,不拦截、不改写、不设 DRx;
2. 异常流说明:目标内 walker 自身异常由其 SEH 处理;保护器解密行为经
   熵差间接观测;调试循环照常收 debug event;
3. 候选筛选/扩展/节流逻辑保留,数据源从"guard AV 历史"改为
   coverage_measure 既有条带数据(LIVE-3 R1 已产出);
4. LIVE-4 重报:样本=身份预检通过的 rev2 vault 对象;预算硬上限;
5. 其余章节按 F1-F5 对应改写;保留框架重投联审。

## 三、WO-1302:**CONDITIONALLY ACCEPTED**

三维只读观测(RIP 采样 / NtQueryInformationThread 等待原因 / EnumWindows 全枚举)
均为合法调试器侧只读操作,判据表思路可用。附条件:
1. 与 WO-1301A 的执行顺序绑定:LIVE-3 数据支持"先诊断后触碰"(窗口怠速未解,
   触碰实验在死进程上无意义);
2. 诊断结论若指向假说 (a)(反调试检测),则批次 10 Phase 1-3 的自研栈
   (`MIDA_ANTIDEBUG_MODE=self`)首次激活授权进入前置清单——联动关系写入实施单。

---

## 四、下一步

1. worker 按 §二 产出 WO-1301A(预计 60% 内容可复用);
2. WO-1301A 重投联审 → 批准后拆实施单 → LIVE-4 由总指挥签发(cap=2 × 60min 建议);
3. 本地提交即可(push 政策已变更)。

**签发**: 项目总指挥 · 2026-08-22
