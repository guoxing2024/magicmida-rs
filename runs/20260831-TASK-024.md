# TASK-024 执行报告 — C-9 离线静态分析: 宿主 exit(0) 调用点与守卫条件解剖 (零实弹零格)

- 执行者: worker (developer 岗位, 一次性执行)
- 日期: 2026-08-31 (boot 2026-08-30 10:05:53.549 内, 开跑前复核一致)
- 格数: 0 (离线静态, 零实弹)
- 证据库: `D:/MidaVault/lab/evidence/xx21b_c9_static/` (6 件 + INDEX.md, sha 登记)

## 0. 零实弹证明

本票**未执行任何样品**: 未运行 rev2_unpacked.exe / host_loader / core.dll, 未部署未启动, 未附加调试器。
全部工作 = 只读磁盘文件 (PE 解析 + 反汇编 + 字节 diff + 既有证据 JSON 读取)。
工具 = python 3.12 + 已安装 pefile 2024.8.26 + capstone 5.0.7; **无 pip 新依赖**。
`crates/` 零改动; git 只读 (未 commit/push); 未创建临时脚本 (全部为 python - <<'EOF' 内联, 无残留文件)。

## 1. 改动文件 (对照授权清单)

| 文件 | 授权 | 改动 |
|---|---|---|
| `D:/MidaVault/lab/evidence/xx21b_c9_static/` | 票面 §4 (vault 证据) | **新建** 6 件 + INDEX.md |
| `runs/20260831-TASK-024.md` | 票面 §4 (报告) | **新建** 本报告 |
| `crates/` | 零改动 | 未动 |
| 既有脚本/样品 | 零改动 | 未动 (只读) |

## 2. 任务 1: 宿主 exit 调用点全列 (导入表解析 + 调用点)

### 2.1 导入表解析方法 (rev2 重建 PE 非标准布局)

命令: `python` 内联 (pefile + 手工描述符遍历), 无新依赖

原始输出 (节选):
```
NEW_HOST a852880a lab/xx21b_run_pcell2/rev2_unpacked.exe
ImageBase 0x140000000 SizeOfImage 0xd37000  EP RVA 0x1010
Import dir: RVA=0x176000 Size=0xc8
Import Address dir: RVA=0x1136e0 Size=0x618
import descriptor raw: Name=0 全部, FirstThunk=模块名 RVA (指向 .import)
IAT 槽静态预填: hint+name RVA (如 0x113a70 = 0x17683c -> +2 -> "exit")
full IAT name map (slot -> import): total: 186
  0x113998 -> _amsg_exit   0x1139b8 -> _cexit   0x113a70 -> exit   0x113a50 -> abort
  0x1138e8 -> TerminateProcess
```

结论: 导入表 = `.import` (0x176000, 9 模块名 + 186 hint/name 项) + IAT dir (0x1136e0, 186 槽, 静态 name-RVA, 运行时加载器改写) + `.idata` (0x13f000, 9 项运行时绝对地址表)。exit 族 4 槽 + TerminateProcess 1 槽定位完成。→ [已验证]

### 2.2 exit 族跳板表 (0x1e9d0-0x1ebc7)

```
0x1ea98  ff 25 d2 4f 0f 00  -> 0x113a70 (msvcrt.exit)
0x1ea08  ff 25 8a 4f 0f 00  -> 0x113998 (msvcrt._amsg_exit)
0x1ea18  ff 25 9a 4f 0f 00  -> 0x1139b8 (msvcrt._cexit)
0x1ea78  ff 25 d2 4f 0f 00  -> 0x113a50 (msvcrt.abort)
```

### 2.3 明文 .text 对 exit 族跳板的调用点全列 (4 处, E8 rel32)

```
0x13d4  e8 bf d6 01 00  -> 0x1ea98 (exit)       EP 尾部: mov ecx,eax; call exit
0x1306  e8 fd d6 01 00  -> 0x1ea08 (_amsg_exit) EP: mov ecx,8; call _amsg_exit (分配失败路径)
0x13cd  e8 36 d6 01 00  -> 0x1ea08 (_amsg_exit) EP: mov ecx,0x1f; call _amsg_exit (state==1 路径)
0x1314  e8 ff d6 01 00  -> 0x1ea18 (_cexit)     EP: 清理+返回码路径
```

扫描证明: 全 .text (capstone 线性) 对 IAT 槽 0x1136e0-0x113cf8 的 rip-rel 引用仅 2 处 (0x1035 Sleep / 0x1123 SetUnhandledExceptionFilter);
LoadLibraryA/W/GetProcAddress/GetModuleHandle IAT 槽 **0 引用** (全部 API 调用在 VM 区 / .idata 运行时表)。→ [已验证]

## 3. 任务 2: 引导路径 exit 点钉死 + 守卫条件 (指令级)

### 3.1 EP 0x1010 主函数守卫链 (反汇编)

```
0x105c  rbp = [0xe88d0] = &0x112058 (.bss 状态字)
0x1063  cmp [0x112058],1 ; je 0x13c8 -> _amsg_exit(0x1f)
0x106f  test [0x112058],0 ; je 0x1110 (init 路径)
0x1110  [0x112058]=1; call 0x12b80 (C++ 静态初始化器表); SetUnhandledExceptionFilter; fninit;
        PE 头校验 0x116d-0x11c0 (MZ/PE/magic 0x10b|0x20b/DataDir≥0xe/[rax+0xf8] debug dir)
0x11c7  [0x112008] = ecx   <- own-image PE-valid flag
...
0x10c9  mov ecx,[0x112008]; test ecx,ecx; je 0x13d2   <== exit(0) 守卫
0x13d2  mov ecx,eax; 0x13d4 call exit                  (eax = 0xde700 返回值 / _amsg_exit 返回值)
0x10d7  mov edx,[0x112004]; test; je 0x1310 (-> _cexit -> 返回码路径)
```

守卫条件精确到指令: **exit(0) 触发 = `[0x112008]==0`**; 该 flag 仅由 init 路径 0x11c7 置 1 (own-image PE-valid);
init 是否运行由 `[0x112058]` 状态字决定 (0->init, 1->_amsg_exit(0x1f), 2->跳过 init)。
值来源: 0x116d 读取 `.rdata 0xe87c0 = 0x140000000` (自身映像基址) 后校验自身 PE 头。→ [已验证]

### 3.2 实际触发点钉死 (结合 T023 证据)

T023 (att1/2/3 一致): ExitProcess 命中 ret = `msvcrt+0x3e2c9` (`_exit+0x89`), Rcx=0 (exit code 0),
R11=0xc0000135 (STATUS_DLL_NOT_FOUND), Rbp=0x140112058 (宿主 .bss), 
栈链 = msvcrt+0x3e2c9 <- heap onexit (0xf212xx) <- msvcrt+0x3e914 (exit/_cexit 内部)。

判定: 
- 退出 = `exit(0)` (Rcx=0), 与 EP 0x13d4 结构一致; 
- 但栈上无宿主明文帧 (仅 msvcrt + heap onexit) => 调用者位于 CRT 内部路径 / VM 区, 
  非宿主明文 4 调用点中任一 (那些会留下宿主 ret 地址);
- R11=STATUS_DLL_NOT_FOUND 与 CRT 初始化失败类退出 (`_amsg_exit` 家族) 一致;
- 时序: core.dll LOAD (idx47) -> DllMain attach (idx49) -> ExitProcess (idx52) = **2.4ms**;
- 中间仅有 uxtheme.dll 加载 (idx51) — 无任何宿主明文路径痕迹 (init/下载/UI 均未走明文)。
=> **引导路径实际触发的 exit = CRT `_exit(0)` 家族 (宿主 VM 区 app 初始化失败), 
   守卫条件 `[0x112008]==0` 或等价 VM 区失败分支, R11=STATUS_DLL_NOT_FOUND 为强提示**。→ [已验证]/[推断] (R11 语义为推断, 见 §9)

## 4. 任务 3: 三方行为自洽解释

### 4.1 分叉逻辑 (同一查询点, 三种候选)

| 候选 | DllMain 行为 | 宿主查询结果 | 宿主行为 | 证据 |
|---|---|---|---|---|
| 原版 core | 真壳入口, 加载即自举 | 壳状态就绪 | 授权对话框 (PigToGoLicenseDialog) | T0.4/T022 |
| 094f5401 (陈旧指针) | 自举但带 dump 会话陈旧指针 | 宿主消费 core 数据命中 0x7ffeeb426390 | **AV (0xc0000005)** | T021/T022 |
| 096f3bdf (NOP stub) | `xor eax,eax; inc eax; ret` 返回 1, 不执行任何壳逻辑 | 查询返回"无响应/失败" | **exit(0)** (R11=STATUS_DLL_NOT_FOUND) | T023/本票 |

分叉点 = **候选 DllMain 返回后的壳状态查询**: 
- 原版自举 -> 查询成功 -> 进消息循环 (对话框);
- 094f5401 自举但数据损坏 -> 查询返回垃圾指针 -> 宿主消费 AV;
- 096f3bdf 完全沉睡 (NOP stub 不初始化任何状态) -> 查询失败/超时 -> 宿主 VM 区走 CRT `_exit(0)` 干净退出。
本票静态证据: 096f3bdf EP = `31 c0 ff c0 c3` (xor eax,eax; inc eax; ret), 无任何初始化副作用。→ [已验证] (EP 字节) / [推断] (查询语义, 与 D-048 一致)

### 4.2 exit(0) 调用者归属: 宿主 vs 候选

- 候选 096f3bdf 静态导入 12 模块 12 函数, **无 exit/_exit/_amsg_exit/_cexit/abort**;
- 候选 .winlice 动态 API 表 (Themida 导入保护) = {CreateProcessA, ExitProcess, GetCommandLineA, GetStartupInfoA, OpenProcess, Sleep, TerminateProcess}, **无 exit 族**;
- 候选文件内 'exit'/'_exit'/'_amsg' 字符串 **0 命中**;
- T023 退出链: ExitProcess 由 msvcrt `_exit` 调用 (ret=msvcrt+0x3e2c9), 候选映像无帧。
=> **候选侧退出可能性排除; exit(0) 调用者 = 宿主侧 CRT/VM 路径**。→ [已验证]

## 5. 任务 4: 老宿主 36043cb4 对照 diff

命令: `python` 内联字节 diff

原始输出 (节选):
```
.text identical: True
total differing bytes: 137
diff bytes per section: {'.data': 68, '.bss': 42, '.idata': 27}
bss 112c10: new=00007ffd37ae6390 (当前会话 ntdll+0x106390)  old=00007ffeeb426390 (陈旧, C-5)
.idata 新: 0x7ffd3663bc60 (kernel32+0x3bc60=GetModuleHandleA), 0x7ffd35dcf970 (advapi32), ... (当前会话)
.idata 旧: 0x7ffee953bc60, ... (08-29 会话)
```

结论:
1. **宿主 .text 逐字节一致** (137 差异字节全在 .data/.bss/.idata);
2. 差异内容 = **会话绑定绝对指针 (C-5 类)**: .idata 运行时 API 表 9 项 + .bss 0x112c10 (ntdll 指针) + 堆指针族;
3. EP 守卫结构两宿主逐字节一致 (0x11c7/0x10c9/0x13d2 相同);
4. 老宿主 36043cb4 "存活" 证据记录于 08-29 其自身会话 (当时 .idata/.bss 指针有效);
   当前会话下 36043cb4 因 .idata/.bss 陈旧无法启动 (T019/KNOWN_ISSUES 已文档化);
5. **"新宿主多出/改变的检查"无法定位 — 不存在代码级新增检查**; 
   行为差异由 (a) 运行期数据有效性 (会话绑定), (b) VM 区查询结果, (c) 候选状态 决定。→ [已验证] (diff 字节级) / [推断] (行为归因)

## 6. 任务 5: 修复方向提案 (未实施)

见 vault `fix_direction_proposal.txt` 全文。摘要:
- **方向 1 (推荐, 候选侧最小壳自举)**: 候选 EP 0x1027c0 从 NOP stub 改为最小自举
  (DllMain 返回 1 + 使宿主查询通过的初始化, 只碰明文区, 不触 .winlice 壳逻辑);
- 风险: 若宿主查询的是"壳 license 校验完成"而非导出存在性, 最小自举仍不满足 -> 需实弹确认查询内容 (另票);
- 方向 2 (宿主侧): 不推荐 (宿主无辜, 且 VM 区不可静态解码);
- 方向 3 (数据层): 094f5401 (垃圾数据->AV) vs 096f3bdf (无响应->exit) 分叉已由 T021/T022 证据支撑。

## 7. 验收标准逐条对照

### 标准 1: 宿主 exit 族调用点全列 + 导入表解析方法
- 导入表解析: pefile + 手工描述符遍历 (rev2 非标准布局: .import 9 模块 + IAT 186 槽静态 name-RVA + .idata 运行时表), 无新依赖 (§2.1)
- exit 族跳板 4 个 (§2.2) + 明文调用点 4 处 RVA+字节 (§2.3)
→ **PASS [已验证]**

### 标准 2: 引导路径 exit 点钉死 + 守卫条件精确到指令 + 值来源
- EP 0x10c9 `mov ecx,[0x112008]; test ecx,ecx; je 0x13d2 -> exit(eax)` (§3.1)
- 值来源: 0x112008 = own-image PE-valid flag (init 0x11c7 置 1, 由 0x116d 读自身映像基址 0x140000000 校验 PE 头)
- 实际触发: T023 证据 -> CRT `_exit(0)` 家族, R11=STATUS_DLL_NOT_FOUND, 栈无宿主帧 (§3.2)
→ **PASS [已验证]+[推断] (R11 语义)**

### 标准 3: 三方行为自洽解释 (094f5401 AV vs 096f3bdf exit 分叉有证据)
- 分叉 = 候选 DllMain 返回后的壳状态查询: 自举->查询成功->对话框; 陈旧指针->AV; NOP stub 沉睡->查询失败->exit(0) (§4.1)
- 证据: 096f3bdf EP 字节 `31 c0 ff c0 c3`; T021/T022/T023 实弹记录; 本票静态
→ **PASS [已验证]+[推断] (查询语义)**

### 标准 4: 候选 core.dll 导入表结论
- 静态导入 12 模块 12 函数无 exit 族; .winlice 动态表 7 项无 exit 族; 文件无 exit/_exit/_amsg 字符串 (§4.2)
- 候选侧退出可能性 **排除**; exit(0) 调用者 = 宿主侧 CRT/VM
→ **PASS [已验证]**

### 标准 5: 老宿主对照 diff
- .text 逐字节一致; 137 差异字节全在 .data/.bss/.idata (会话绑定指针 C-5 类) (§5)
- "新宿主多出/改变的检查": **不可定位 (不存在代码级差异)**, 如实说明
→ **PASS [已验证]**

### 标准 6: 修复方向提案 + "我没做的事/我不确定的事"
- 修复方向: vault `fix_direction_proposal.txt` (§6); 不确定项见 §9
→ **PASS**

### 标准 7: 零实弹证明 + 零越界 + git 只读 + 无新依赖
- 零实弹: §0 (未执行任何样品); 零越界: crates/ 零改动, 只读样品; git 只读 (HEAD=0e52206, 未 commit/push); 无 pip 新依赖 (pefile/capstone 已安装)
→ **PASS [已验证]**

### 标准 8: vault 证据先行 + INDEX 登记
- `D:/MidaVault/lab/evidence/xx21b_c9_static/`: 6 件 + INDEX.md (sha 登记完整, 复核一致)
→ **PASS [已验证]**

## 8. 零实弹证明 (再确认)

- 全程未执行 rev2_unpacked.exe / host_loader / core.dll / 任何样品;
- 未部署、未启动、未附加调试器;
- 全部工具 = python 内联 (pefile/capstone 只读解析) + 读既有证据 JSON;
- 仓库根目录无新增临时文件; git status 无新增跟踪改动。

## 9. 我没做的事 / 我不确定的事

### 我没做的事
1. **没有执行任何样品** (零实弹零格, 票面红线)。
2. **没有修复任何东西** (宿主/候选/脚本均未动; 修复方向仅书面提案)。
3. **没有解码 VM 区**: 宿主 .text 0x2c00+ / 候选 .winlice/.boot 为 Themida VM 加密区,
   线性反汇编无效 (熵 6.15, 非 x64 明文); 未尝试脱壳 (超出本票授权且需实弹)。
4. **没有运行 dumpbin** (票面建议但非必需; pefile 自研解析已覆盖, 且 dumpbin 需 MSVC 环境, 离线零实弹优先用自研)。
5. **没有验证 R11=0xc0000135 的完整语义** (见下)。
6. **没有对 094f5401/原版 core 做静态导入对比** (票面未要求; 三方解释基于既有实弹记录)。

### 我不确定的事 (诚实边界)
1. **[推断] R11=0xc0000135 (STATUS_DLL_NOT_FOUND) 的精确语义**: R11 是 volatile, 
   命中时值 = CRT `_exit` 内部调用链刚计算的 NTSTATUS。3/3 趟稳定 + 与 Rbp=宿主 .bss 共存支持
   "CRT 初始化失败类" 解释, 但未逐字节确认具体失败 DLL/路径 (VM 区不可静态解码)。
2. **[推断] 宿主 VM 区查询内容**: D-048 假设 (查询壳状态) 与全部证据一致 (原版自举->活,
   stub 沉睡->exit), 但查询的具体对象 (导出存在性? license 校验? .text 明文?) 无法静态确认 —
   需实弹 (另票) 在 VM 区设点确认。
3. **[存疑] EP 守卫 [0x112008]==0 与观察到的 CRT _exit(0) 是否同一路径**: EP 明文守卫在结构上
   与 exit(0) 一致, 但 T023 栈上无宿主明文帧, 无法 100% 断定 exit 来自 EP 0x13d4 还是 VM 区内
   等价调用 (二者都汇到 msvcrt _exit)。已如实标注。
4. **[已验证] 宿主 .text 零差异, 但 .data/.bss/.idata 会话绑定指针的具体运行期影响**:
   新宿主 .idata = 当前会话 (有效), 老宿主 = 旧会话 (C-5 陈旧) — 这解释了老宿主当前会话不可启动,
   但"新宿主+096f3bdf exit vs 老宿主+stub 存活"的差异是否完全由会话/数据决定, 未实弹复验
   (老宿主在当前会话根本无法启动)。
5. **[已验证] 候选 .winlice 动态表 7 项清单**: 序号区 0x207a-0x2000 与名称区对应关系为推断
   (Themida 内部格式), 但名称区明文可读, 7 项确凿无 exit 族。
6. **未做 dumpbin 交叉验证**: pefile 解析结果 (186 槽/名称映射) 与 T022 记录 (imports 186 整) 一致,
   未再跑 dumpbin (零实弹纪律优先)。

## 10. 判定摘要 (给总指挥)

- **任务 1 PASS**: exit 族 IAT 槽 (exit/_amsg_exit/_cexit/abort/TerminateProcess) + 跳板表 4 个 + 明文调用点 4 处全列; 导入表解析方法写明 (rev2 非标准布局, pefile 自研, 无新依赖)。
- **任务 2 PASS**: 守卫条件精确到指令 = EP 0x10c9 `[0x112008]==0 -> exit(eax)`; 值来源 = init 0x11c7 own-image PE-valid flag; 实际触发 = CRT `_exit(0)` (T023: msvcrt+0x3e2c9, R11=STATUS_DLL_NOT_FOUND, 栈无宿主帧, 2.4ms)。
- **任务 3 PASS**: 三方行为分叉 = 候选 DllMain 后壳状态查询 (原版自举->活; 094f5401 陈旧指针->AV; 096f3bdf NOP stub 沉睡->exit(0)); 候选侧退出排除 (导入/动态表/字符串三查零 exit 族)。
- **任务 4 PASS**: 老宿主 .text 逐字节一致; 137 差异字节全为会话绑定指针 (C-5); 无代码级新增检查可定位 — 如实报告。
- **任务 5 PASS**: 修复方向提案 (候选最小壳自举, 不实施) 入 vault。
- **验收 8 条全部 PASS** (逐条附命令/原始输出如上)。
- **C-9 结论 [推断]**: exit(0) = 宿主 VM 区 app 初始化在加载 NOP-stub 候选后查询失败 -> CRT `_exit(0)`;
  D-048 机制假设**获支持** (未证实查询内容); 修复方向 = 候选侧最小壳自举 (需实弹确认查询对象后实施)。

---
*worker · TASK-024 执行完成 · 2026-08-31 · 零实弹零格 · 结论待总指挥审计*

---

## 附注：总指挥审计（2026-08-31，D-049）——验收 8 条全部通过，核心声明逐字节独立复算成立

**复算清单**（全部总指挥亲手）：4 个 `ff 25` 跳板字节逐位吻合；4 个 E8 调用点 rel32 目标算术全对（0x13d4→0x1ea98 等）；**守卫链 rip-relative 精确解析**（0x10c9 `8b 0d 39 0f 11 00` = mov ecx,[rip+0x110f39] → 0x10cf+0x110f39 = **0x112008 精确**；后随 `85 c9` test + `0f 84` je；0x11c7 `89 0d 3b 0e 11 00` 写回同址；0x116d `48 8b 05 4c 76 0e 00` 读 .rdata 0xe87c0 = 0x140000000 后 `66 81 38` MZ 校验）；**.text identical: True（0xdda00）独立复现**；137 差异字节分布 {'.data':68,'.bss':42,'.idata':27} 逐位吻合；.bss 0x112c10 双值（新 0x7ffd37ae6390 / 旧 0x7ffeeb426390）与 C-5 记录精确互证；候选静态导入 12 模块（kernel32!GetModuleHandleA / urlmon!URLDownloadToFileA 等）**零 exit 族**独立确认；pefile 2024.8.26 / capstone 5.0.7 确为本机既有（无新装）；git HEAD 未动（0e52206）、零 tracked 改动（零实弹旁证）；vault 6 件 + INDEX 在位。

**更正（微）**：报告称 vault "6 件 + INDEX.md"，实为 5 件 + INDEX.md（共 6）——计数口径，不涉实质。

**审计结论**：TASK-024 验收 8 条 PASS。零实弹确认。

**对 D-048 假设的裁决（总指挥记档）**：主分支（NOP stub 沉睡 → 宿主查询失败 → exit(0)）**获支持**（未证实查询内容——VM 区不可静态解码）；但"新宿主检查更严"子分支**被否决**（.text 逐字节一致，无代码级差异）——行为差异归因转移到候选状态（.winlice 材料化会话差异 / 21 vs 23 节重建差异）+ 会话数据有效性。T0.4 老宿主在当前会话因自身 .idata/.bss 陈旧根本无法启动（C-5），"老组合存活"不可在今日复现——老宿主对照的价值 = 排除代码差异，非行为对照。
**实际 exit(0) 触发点** = 宿主 Themida VM 区（明文 4 调用点会留宿主 ret 帧，T023 栈上无 → VM/CRT 内部路径）——静态不可达，剩余未知需实弹。
**下一步选项（待老板裁决）**：A. 最小自举实验（1 格：候选 EP 指回原壳入口 0x8a0108，8 字节离线改动 + 实弹一跑；三种结局各自都是决定性发现，但 (b) 结局大概率触发壳反调试 → attach-changed-behavior → 将迫使 T0.5 FULL 语义裁决（RIP 口径 vs 窗口证据口径，T017 建议②）；B. 收官归档（C-9 根因已达"VM 区查询失败"粒度，战役成果入册）；C. 持有。
