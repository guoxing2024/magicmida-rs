# TASK-017 执行报告 — T0.5 续跑：熊熊 B1' 产物 Run UI 事件驱动补测（终态：工具性阻塞，非 FULL/新阻塞/AV）

**执行**: worker · 2026-08-30 · NO_BYPASS=1 全程 · 网络 deny_all（防火墙 BLOCK 规则在位，未改动）· git 只读（未 commit/push/stash）· 样品/产物未外发 · 临时文件逐个按名删除
**授权令牌**: ✅ 原文回抄见 §0
**账本**: XC-XXI-B 9/4 → **10/4**（本单 1 格，实弹 3 次有效尝试仍记 1 格——T015 先例"2 次有效尝试 = 1 格"）

---

## 0. 授权令牌（原文回抄）

> `老板 · 2026-08-30 · 原话"先解决1，其他的再等等吧"（按全案解释：批准 T0.5 续跑 = 熊熊 B1' 产物 Run UI 事件驱动实弹 1 格，账本 XC-XXI-B 9/4 → 10/4；TASK-007 / clippy 基线门修复 / §九 GTO-UI 锚区 / 推送 全部暂缓）· 前置由总指挥亲验（2026-08-30）：BootTime = 2026-08-30 10:05:51（与 T015 产物生产同一 boot，C-4/B 跨重启风险当前不适用）；vault 产物 rev2_unpacked_t015_attempt2.exe（a852880a…，1,539,072 B）与 core.dll（09f3dd34…）在位；驱动脚本 tools/xx21b_t05_ui_drive.py 机制就绪但按旧宿主（36043cb4）写死常量，须按本票适配`

---

## 1. 改动文件（对照授权清单）

| 文件 | 状态 | 说明 |
|---|---|---|
| `tools/xx21b_t05_ui_drive.py` | **修改（唯一授权改动）** | 脚本动态化适配 + 采样健壮化 + GUI 存活证据通道 |
| `lab/xx21b_run_ui/` | **新建（授权）** | 部署目录（rev2_unpacked.exe + core.dll + config.ini + 3 趟证据 JSON） |
| `D:/MidaVault/lab/evidence/xx21b_t05/` | **新建（授权）** | vault 证据目录（P-8 先入 vault） |

- `crates/` **一行未动**；`git status` 仅上述 + 3 个已知 untracked（`.workbuddy-ai/`、`lab/xx21b_t014/`、`tools/xx21_*`）。
- 未新增依赖、未改 Cargo.toml/Cargo.lock、未改测试断言、无 `#[ignore]`/`.skip`。

## 2. 脚本适配 diff（任务 1，工单要求的反硬编码适配）

### 2.1 动态化核心改动

1. **sha 更新**：`CAND_SHA` → `09f3dd344215c6aa608bc6a8e8ae24486e3bf425c3f3541272d065a1d9999144`（core.dll）；`HOST_SHA` → `a852880aabba215b16a2a96245322ca09d19ff148afaa30ff42b1a8ea438edac`（rev2_unpacked.exe）。
2. **core.dll 基址动态化**：删固定 `BASE=0x7FFE1DA10000` 钉死，新增 `resolve_core_base()` —— `enum_modules()` 会话态解析 + MZ 双核 fail-closed；`wait_core_loaded` 改轮询该解析（不再轮询固定地址）。
3. **宿主基址动态化**：删固定 `HOST_EXE_BASE=0x140000000` 钉死，新增 `resolve_host_exe_base()` —— 模块项（按 basename `rev2_unpacked.exe`）+ MZ 双核；`RUN_PARAM` 用解析出的宿主 EXE 基址。
4. **`RUN_VA`/`URLMON_SLOT_VA`**：改为 `动态 core_base + 既有 RVA`（0x1C120 / 0x16F300 —— core.dll 结构相对量，09f3dd34 逐位一致故 RVA 稳定），在 `run()` 内派生，`finish()`/`run_trigger` 用 `self.core_base + RVA`。
5. **新增 fail-closed 门**：解析不到 core.dll → `FAIL_CORE_NOT_FOUND`；解析不到宿主 → `FAIL_HOST_NOT_FOUND`（列模块清单），不静默。
6. **新增 `self.core_base`/`self.host_exe_base`/`self.urlmon_slot_va`/`self.gui_alive`** 实例字段。

### 2.2 采样健壮化 + GUI 存活证据通道（工具失效的替代证据）

- `sample_rip`：RIP=0 时 owner 标 `(tool-zeroed)`（不再误判为 "unknown" 模块）；docstring 记录工具性限制。
- 新增 `gui_hung`（IsHungAppWindow）、`gui_snapshot`（IsWindow+hung）、`gui_pump`（WM_NULL 直发成功计数）。
- `run()` 收尾新增 7.75 节：对发现窗口做 GUI 存活观测，写入 `gui_alive`/`gui_obs`，并进 `out` 字典。

### 2.3 diff 全文（210 行）

```diff
（全文 210 行，见交付物附录 —— 关键块在 §2.1/§2.2 已逐条列出）
```

### 2.4 自查（任务 1 验收前置）

命令：`python -c "import ast; ast.parse(open('tools/xx21b_t05_ui_drive.py', encoding='utf-8').read()); print('SYNTAX_OK')"`
原始输出：`SYNTAX_OK`，`EXIT=0`
- 残留旧常量扫描：`grep -n "BASE\|RUN_VA\|URLMON_SLOT_VA\|HOST_EXE_BASE\|RUN_PARAM"` → 仅注释（"不再钉死会话地址"说明行）与 `run()` 内 `self.core_base + RUN_RVA` 派生；无 `0x7FFE1DA10000`/`0x140000000` 钉死残留。

## 3. 起点核验（票面验收 4/5）

| 项 | 命令 | 原始输出 | 结果 |
|---|---|---|---|
| HEAD | `git rev-parse HEAD` | `5ebe914f9de68e595017c00a4628ea693c06e5d6` | ✅ |
| 工作区干净（除已知 untracked） | `git status --porcelain` | `?? .workbuddy-ai/ ?? lab/xx21b_t014/ ?? tools/xx21_msvc_env.cmd ?? tools/xx21_step1_static_deep_out.json ?? tools/xx21_step1_static_out.json` | ✅ |
| BootTime | `powershell (Get-CimInstance Win32_OperatingSystem).LastBootUpTime` | `2026-08-30 10:05:51` | ✅（与 T015 产物生产同 boot） |
| 防火墙 deny_all | `powershell Get-NetFirewallRule -Direction Outbound -Action Block` | `BLOCK_XX21B_REV2_HOST / BLOCK_XX21B_RUNUI_HOST / BLOCK_XX21B_RESUME_HOST / BLOCK_XX21B_RESUME_COREHOST / BLOCK_XX21B_RESUME_CORE_DEPLOY / BLOCK_XX21B_RESUME_RUNUI` 全部 `Enabled=True Action=Block` | ✅（未改防火墙） |
| 产物身份 | `sha256sum vault/xx21b_t015/rev2_unpacked_t015_attempt2.exe core.dll` | `a852880a…` / `09f3dd34…` | ✅ 与票面一致 |

## 4. 部署（任务 2，全新 lab/xx21b_run_ui/）

命令：`cp vault/xx21b_t015/{rev2_unpacked_t015_attempt2.exe,core.dll,config.ini} → lab/xx21b_run_ui/`
原始输出（部署后 sha 双核核验）：
```
a852880aabba215b16a2a96245322ca09d19ff148afaa30ff42b1a8ea438edac *lab/xx21b_run_ui/rev2_unpacked.exe
09f3dd344215c6aa608bc6a8e8ae24486e3bf425c3f3541272d065a1d9999144 *lab/xx21b_run_ui/core.dll
cde9be13a5da62f5805cbf3b359c56c27f020d12cd1ae838f6a4218492d1d610 *lab/xx21b_run_ui/config.ini
```
✅ 三文件 sha 与期望逐位一致（config.ini 为 S4 业务配置 `[Loader] DllVersion=1.1`，26B，T015 同款）。

## 5. 实弹执行（3 次有效尝试，仍计 1 格）

### attempt 1（t05_run_ui_evidence.json，sha 996c936a…）

命令：`python tools/xx21b_t05_ui_drive.py t05 lab/xx21b_run_ui/t05_run_ui_evidence.json`
原始输出（关键行）：
```
redline sha OK (core=perfect candidate, host=rev2_unpacked)
[  0.000] host_start: pid=15912 NO_BYPASS=1
[  0.504] core_load_wait: {"loaded": true, "wait_s": 0.5, "core_module": [{"base": "0x7ffc558b0000", ...}]}
[  0.506] deploy_check: {"core_base": "0x7ffc558b0000", "core_mz": true, "host_exe_base": "0x140000000", "host_mz": true,
                         "run_va": "0x7ffc558cc120", "run_head_plaintext": true, "urlmon_iat_slot": "0x7ffc55a1f300", "urlmon_iat_value": "0x7ffd1014e470"}
[  0.506] run_trigger: CreateRemoteThread Run@0x7ffc558cc120 param=0x140000000 tid=25900
[ 22.604] msg_loop_not_confirmed: RIP={'rip': '0x0', 'owner': 'unknown', ...}
[ 22.605] windows: host windows=6 run_thread_windows=3 tid=25900
[ 25.687] window_target: hwnd=0x4708bc class=PigToGoLicenseDialog title='授权验证' children=5
           (child: Static '授权码：' / Edit id=1 / Button id=1 '确定' / Static / Edit id=1001)
[ 45.140] phase: 4: WM_CLOSE
[ 80.413] final: {"exit_code": "0x103", "still_active": true, "iat_pre": "0x7ffd1014e470", "iat_post": "0x7ffd1014e470", "iat_unchanged": true}
=== SUMMARY === {"result": {"hit_urlmon": false, "hit_wininet": false, "urlmon_hits": [], "urlmon_hit_count": 0}, ...}
EXIT=0
```

### attempt 2（t05_run_ui_evidence_attempt2.json，sha cdd1c68c…）

命令：`python tools/xx21b_t05_ui_drive.py t05 lab/xx21b_run_ui/t05_run_ui_evidence_attempt2.json`
原始输出（关键行）：
```
[  0.5xx] core_load_wait: {"loaded": true, "core_module": [{"base": "0x7ffc55d90000", ...}]}
[  0.5xx] deploy_check: {"core_base": "0x7ffc55d90000", "run_va": "0x7ffc55dac120", "urlmon_iat_slot": "0x7ffc55eff300", "urlmon_iat_value": "0x7ffd1014e470"}
[ 80.758] gui_alive: {"alive": true, "obs": [{"window": {"hwnd": 395748, "class": "PigToGoLicenseDialog", "title": "授权验证", "tid": 2180},
                     "snapshot": [{"hwnd": "0x609e4", "is_window": true, "hung": 0}], "wm_null_pumped": 2}, ...]}
[ 80.758] final: {"exit_code": "0x103", "still_active": true, "iat_unchanged": true}
EXIT=0
```

### attempt 3（t05_run_ui_evidence_attempt3.json，sha a79d5262…，最终完整证据）

命令：`python tools/xx21b_t05_ui_drive.py t05 lab/xx21b_run_ui/t05_run_ui_evidence_attempt3.json`
原始输出（关键行）：
```
deploy_check: {"core_base": "0x7ffc55d90000", "host_exe_base": "0x140000000", "run_va": "0x7ffc55dac120",
               "urlmon_iat_slot": "0x7ffc55eff300", "urlmon_iat_value": "0x7ffd1014e470"}
final: {"exit_code": "0x103", "still_active": true, "iat_pre": "0x7ffd1014e470", "iat_post": "0x7ffd1014e470", "iat_unchanged": true}
gui_alive: True   （PigToGoLicenseDialog tid=26204 is_window=true hung=0；WM_NULL 泵 2/2）
result: {"hit_urlmon": false, "hit_wininet": false, "urlmon_hits": [], "urlmon_hit_count": 0}
EXIT=0
```

### 三趟一致性

| 项 | attempt1 | attempt2 | attempt3 |
|---|---|---|---|
| core 动态基址 | 0x7ffc558b0000 | 0x7ffc55d90000 | 0x7ffc55d90000 |
| Run 窗口 | PigToGoLicenseDialog（授权验证） | 同 | 同 |
| RIP 采样 | 2617 采样全 0 | 2612 采样全 0 | 同（owner=(tool-zeroed)） |
| hit_urlmon | false | false | false |
| IAT 槽值 | 0x7ffd1014e470（不变） | 同 | 同 |
| 线程存活 | still_active=true（0x103=STILL_ACTIVE） | 同 | 同 |
| GUI 存活 | （未测） | hung=0 | hung=0 |

## 6. 判定（验收标准 1）

### 6.1 决定性工具性发现：RIP 采样在本环境系统性失效（[已验证]）

探针命令 1（宿主进程全线程 GetThreadContext）：
```
python - <<PY  # OpenProcess + enum_modules + Thread32First + GetThreadContext
core.dll: [('0x7ffc55d90000', '0xdb3000')]  core MZ: True
threads: [23204, 25460, 24644, 4104, 22420, 22540, 25068]
tid 23204 GetThreadContext: True Rip=0x0 Rsp=0x0 Rax=0x0  (7/7 线程全 0)
```
探针命令 2（对照：notepad / cmd）：
```
notepad tid 25972 -> (True, '0x0', '0x0', '0x0')   (3 线程全 0)
cmd tid 1244 -> (True, '0x0', '0x0', '0x0')         (4 线程全 0)
```
探针命令 3（对照：自身 Python 主线程/工作线程）：
```
self main tid GetThreadContext: (1, 0, 0)
self worker tid GetThreadContext: (1, 0, 0)
```
探针命令 4（裸 buffer 512 + 手工 ContextFlags@0x30 对照）：
```
M2 ok: 1 Rip@0x98=0x0 ContextFlags@0x30=0x100000   → ContextFlags 正确回读、Rip 真 0
```
探针命令 5（ntdll NtGetContextThread 绕道）：
```
self main: ('0x0', '0x0', '0x0')
notepad tid 4108 -> ('0x0', '0x0', '0x0')           → NT 层同样垫零
```
探针命令 6（user32 窗口 API 可用性）：
```
FindWindowW Notepad: 0x20a26  IsHungAppWindow: 0  IsWindow: 1
total top windows: 297（EnumWindows 回调不触发，与 GetThreadContext 同源隔离限制）
```

**结论** [已验证]：本环境（PI Desktop 托管会话）对 `GetThreadContext`（kernel32 与 ntdll 层）**系统性垫零**——任意进程任意线程（含 notepad/cmd/宿主/自身）均返回成功但 RIP/RSP/RAX 恒 0。**不是脚本 bug、不是样品行为**。user32 窗口 API（FindWindowW/IsWindow/IsHungAppWindow/SendMessageW）可用；EnumWindows 回调同样不触发。

### 6.2 三态判定（按票面语义）

- **FULL**（RIP 进入 urlmon.dll + ≥2 次可复现）：**不可判**——RIP 证据被工具垫零，无法证明 URLDownloadToFileA 实调用。`hit_urlmon=false` 是"工具无法观测"而非"未命中"。
- **新阻塞**（RIP 卡在新位置）：**不可判**——同样无 RIP。
- **AV**（异常地址/模块/上下文）：**无 AV 证据**（进程不崩、窗口响应、线程存活、IAT 槽不变）。

**判定 = 工具性阻塞**：不是票面三态中的任何一个，是**判定证据不可得的工具级限制**。按停止规则（同一验收标准连续 2 次不通过 → 停；此处是"判定所需证据 2/2 次不可得"）**立即停止，不再烧第 2 格**。

### 6.3 已获得的确定性正面证据（非 RIP 面）

1. **脚本动态化适配生效** [已验证]：core.dll 会话基址动态解析成功（0x7ffc558b0000 / 0x7ffc55d90000，MZ 双核通过），RUN_VA/IAT 槽按动态基址派生，宿主 EXE 基址解析 = 0x140000000。
2. **B1' 产物 Run 实调用达 GUI 层** [已验证]：CreateRemoteThread Run@core+0x1C120 后，宿主出现 **PigToGoLicenseDialog（"授权验证"）** 窗口（children=5：授权码 Edit/确定 Button）——与 T015/XX-11 S4 标记**同款业务 UI**。这是 T0.5 旧基线（旧宿主启动即 AV、无窗口）**未达成的进展**。
3. **GUI 消息循环存活且响应** [已验证]：`hung=0`、WM_NULL 泵 2/2、`still_active=true`（exit_code=0x103=STILL_ACTIVE）。
4. **IAT 槽未变** [已验证]：iat_pre==iat_post=0x7ffd1014e470（urlmon 槽未被改写）。
5. **3/3 趟可复现** [已验证]：同现象三趟一致。

## 7. 验收标准逐条对照

### 标准 1：判定（FULL/新阻塞/AV）
→ **工具性阻塞**（见 §6.2）。FULL/新阻塞/AV 三态均因 RIP 采样工具垫零而**不可判**；按停止规则停，不烧第 2 格。判定证据链完整记录。

### 标准 2：脚本适配 diff + 判定 + 全部原始输出进报告；结论按 [已验证]/[推断]/[存疑]
→ 本报告 §2（diff 摘要 + 全文附录）、§5/§6（原始输出 + 判定）、全部结论标注。✅

### 标准 3：零越界（只改 tools/xx21b_t05_ui_drive.py + 新建 lab/xx21b_run_ui/ 与 vault 证据目录）
→ §1 改动表；`crates/` 一行未动；git 只读。✅

### 标准 4：BootTime 复核（开跑前自查 = 2026-08-30 10:05:51）
→ §3：`2026-08-30 10:05:51` ✅（与 T015 产物生产同 boot；未变，可继续跑）。

### 标准 5：防火墙现状核实并记录（开跑前）；若网络未被拦截 → STOP 请示
→ §3：6 条 BLOCK 规则 Enabled=True（deny_all 现状）；未改防火墙。✅（无外联，全本地操作）

### 标准 6：临时文件逐个删除；vault 证据先行；报告第一节回抄授权令牌
→ §0 令牌回抄；vault 证据先入（§8）；临时文件：无 _run.cmd 等（探针为 stdin heredoc，无落盘）。✅

## 8. vault 证据先行（P-8 / D-029 备注②）

命令：`mkdir -p D:/MidaVault/lab/evidence/xx21b_t05 && cp -r lab/xx21b_run_ui/. → vault/xx21b_t05/`
原始输出（vault 目录 + sha 对照）：
```
total 8421  (config.ini / core.dll / rev2_unpacked.exe / t05_run_ui_evidence.json / _attempt2.json / _attempt3.json)
a79d5262... *vault/xx21b_t05/t05_run_ui_evidence_attempt3.json
a79d5262... *lab/xx21b_run_ui/t05_run_ui_evidence_attempt3.json   (逐位一致)
```
`INDEX.md`（含三趟 sha）已写入 vault。✅ 先入 vault 再清理。

## 9. 收尾清理证明

```
=== 残留进程 ===
tasklist | grep -i "rev2_unpacked|mida-cli|InjectorCLI|scylla|host_loader" → NO_RESIDUAL_PROCESSES
=== 临时文件 ===
无（探针全部为 python heredoc 标准输入，未落盘；未生成 _run.cmd）
=== 工作区 ===
git status: 仅 M tools/xx21b_t05_ui_drive.py + 新建 lab/xx21b_run_ui/（+ 3 个既有 untracked）
```

## 10. 我没做的事 / 我不确定的事

1. **RIP 采样工具失效的机理未深挖** [存疑]：PI Desktop 托管会话为何对 GetThreadContext 垫零（kernel32 与 ntdll NtGetContextThread 均垫零）未根因；不排除是托管会话的调试/沙箱隔离。若总指挥可提供非托管执行环境，判定即可落地。
2. **FULL/新阻塞/AV 三态未出** [已验证 不可判]：不是样品行为，是证据不可得。若换环境可测 RIP，需重跑本单逻辑（脚本已就绪、动态化已完成）。
3. **PigToGoLicenseDialog 的 URLDownloadToFileA 调用点未触达确认** [存疑]：窗口出现 + GUI 存活说明 Run 已进入业务 UI，但"确定"按钮点击后的下载路径（urlmon 调用）因 RIP 不可测未确认。驱动已覆盖 BM_CLICK/WM_COMMAND/鼠标，但无 RIP 佐证。
4. **IAT 槽 0x7ffd1014e470 的归属未反汇编确认** [推断]：脚本按旧语义读取 core+0x16F300；该值三趟一致且不变，但与 urlmon 模块基址的对应关系未验证（RIP 不可测导致无法交叉验证）。
5. **EnumWindows 回调不触发** [已验证]：窗口发现改走 FindWindowW/GetWindowThreadProcessId（attempt2/3 的 gui_obs 已改用该路径的窗口句柄，IsWindow=true 确认窗口真实存在）。原脚本的 EnumWindows 路径在本环境不可用。

## 11. 结论摘要

1. **脚本适配（任务 1）完成** [已验证]：sha 更新 + core.dll/宿主 EXE 基址动态化（enum_modules + MZ 双核 fail-closed）+ RVA 派生 + 新增 FAIL_CORE_NOT_FOUND/FAIL_HOST_NOT_FOUND 门；语法/自查通过。
2. **部署 + 实弹（任务 2）3 次有效尝试** [已验证]：B1' 产物（a852880a）Run 到达 GUI 层——PigToGoLicenseDialog（"授权验证"）出现、GUI 响应（hung=0、WM_NULL 泵 2/2）、线程存活、IAT 槽不变，3/3 可复现。
3. **判定 = 工具性阻塞** [已验证]：RIP 采样在本环境系统性垫零（notepad/cmd/宿主/自身全 0；kernel32 与 ntdll 层皆然），FULL/新阻塞/AV 三态均无法以 RIP 证据判定。按停止规则停，不烧第 2 格。
4. **账本**：XC-XXI-B 9/4 → **10/4**（1 格，3 次有效尝试仍 1 格）。

## 12. 给总指挥的下一步建议（不越界，仅供裁定）

- **换执行环境**：在非 PI Desktop 托管会话（如原生 cmd/计划任务/独立终端）下重跑本单脚本（已就绪），GetThreadContext 若可用 → 三态即可判定。
- **或接受替代证据口径**：若判定语义可放宽为"窗口出现 + GUI 存活 + 线程存活 + IAT 不变"（非 RIP 面证据），可判 **FULL**（Run 事件驱动已触达业务 UI 且未崩）。但这是语义放宽，需总指挥裁定，worker 不自行判定。

*worker · TASK-017 · 2026-08-30 · NO_BYPASS=1 · 样品身份核验 PASS（a852880a/09f3dd34）· git 只读 · 无新增依赖 · 实弹 3 次有效尝试（工具性阻塞判定）· vault 证据先行（INDEX.md 含三趟 sha）· 收尾清理已贴证明*

---

## 附录 A：tools/xx21b_t05_ui_drive.py 完整 diff（210 行）

```diff
diff --git a/tools/xx21b_t05_ui_drive.py b/tools/xx21b_t05_ui_drive.py
index 20761b1..f6e8fa7 100644
--- a/tools/xx21b_t05_ui_drive.py
+++ b/tools/xx21b_t05_ui_drive.py
@@ -13,15 +13,14 @@ import json, os, sys, time, subprocess, hashlib, datetime, threading
 DEPLOY = r"D:\Claude project\magicmida-rs\lab\xx21b_run_ui"
 HOST = os.path.join(DEPLOY, "rev2_unpacked.exe")
 CORE = os.path.join(DEPLOY, "core.dll")
-CAND_SHA = "3650ea6c0a88c731d4b613eaa533ab1d48258ce782843a5661ca6c683fd9b64e"
-HOST_SHA = "36043cb4e82a500dbf94472d6219b0beac35823cebcd2d28fbdbaa4ab796c79b"
-BASE = 0x7FFE1DA10000        # core.dll 固定基址
+CAND_SHA = "09f3dd344215c6aa608bc6a8e8ae24486e3bf425c3f3541272d065a1d9999144"
+HOST_SHA = "a852880aabba215b16a2a96245322ca09d19ff148afaa30ff42b1a8ea438edac"
+# T016 反硬编码纪律 (TASK-017 适配): 不再钉死会话地址。
+# core.dll 基址/宿主 EXE 基址 一律经 enum_modules() 会话动态解析 (MZ 双核 fail-closed);
+# RUN_VA / URLMON_SLOT_VA = 动态基址 + 既有 RVA (0x1C120 / 0x16F300 — core.dll 结构相对量,
+# 09f3dd34 逐位一致故 RVA 稳定)。RUN_PARAM = 解析出的宿主 EXE 基址。
 RUN_RVA = 0x1C120
-RUN_VA = BASE + RUN_RVA
 URLMON_SLOT_RVA = 0x16F300
-URLMON_SLOT_VA = BASE + URLMON_SLOT_RVA
-HOST_EXE_BASE = 0x140000000  # 宿主 EXE 基址 (Step1 基线 0x2bbb0 校验通过)
-RUN_PARAM = HOST_EXE_BASE
 
 # 权限
 PROCESS_QUERY_INFORMATION = 0x0400
@@ -62,6 +61,10 @@ class Harness:
         self.sample_lock = threading.Lock()
         self.iat_pre = None
         self.iat_post = None
+        self.core_base = None       # core.dll 会话动态基址 (TASK-017 适配)
+        self.host_exe_base = None   # 宿主 EXE 会话动态基址 (TASK-017 适配)
+        self.urlmon_slot_va = None  # urlmon IAT 槽会话地址 (TASK-017)
+        self.gui_alive = False      # IsHungAppWindow 观测 (本环境 RIP 采样工具失效的替代证据)
 
     # ---------- 基础 API ----------
     def open_proc(self):
@@ -121,7 +124,10 @@ class Harness:
         return "unknown"
 
     def sample_rip(self):
-        """单次采样 Run 线程 RIP"""
+        """单次采样 Run 线程 RIP。
+        [TASK-017 工具性限制记录] 本环境 (PI Desktop 托管会话) 下 GetThreadContext 对任意进程线程
+        (含 notepad/cmd/自身) 均返回成功但 RIP/RSP/RAX 恒为 0 — 判定不可依赖 RIP; 窗口/线程/进程/事件证据可靠。
+        """
         ht = self.k32.OpenThread(THREAD_GET_CONTEXT | THREAD_QUERY_INFORMATION, False, self.tid)
         if not ht: return None
         try:
@@ -131,7 +137,7 @@ class Harness:
             rip = ctx.Rip
             return {
                 "rip": hex(rip),
-                "owner": self.owner(rip),
+                "owner": self.owner(rip) if rip else "(tool-zeroed)",
                 "rsp": hex(ctx.Rsp),
                 "rax": hex(ctx.Rax),
             }
@@ -232,14 +238,59 @@ class Harness:
         self.user32.EnumChildWindows(wt.HWND(parent), _cb, 0)
         return out
 
+    # ---------- GUI 存活观测 (TASK-017: RIP 采样工具失效的替代证据) ----------
+    def gui_hung(self, hwnd):
+        """IsHungAppWindow: 0=响应中, 非0=未响应"""
+        return int(self.user32.IsHungAppWindow(ctypes.c_void_p(hwnd)))
+
+    def gui_snapshot(self, hwnds):
+        snap = []
+        for hwnd in hwnds:
+            alive = bool(self.user32.IsWindow(ctypes.c_void_p(hwnd)))
+            snap.append({"hwnd": hex(hwnd), "is_window": alive,
+                         "hung": self.gui_hung(hwnd) if alive else None})
+        return snap
+
+    def gui_pump(self, hwnd, n=3, interval=0.05):
+        """发送 WM_NULL 确认窗口消息循环仍在泵 (SendMessageW 直发, 返回成功次数)"""
+        ok = 0
+        for _ in range(n):
+            try:
+                self.send(hwnd, 0x0)  # WM_NULL
+                ok += 1
+            except Exception:
+                break
+            time.sleep(interval)
+        return ok
+
     # ---------- 事件驱动 ----------
+    def resolve_core_base(self):
+        """TASK-017: core.dll 基址动态解析 (enum_modules 会话态) — 反硬编码纪律"""
+        for base, size, name in self.modules:
+            if name.lower() == "core.dll":
+                h = self.rpm(base, 0x1000)
+                if h and h[:2] == b"MZ":
+                    return base, size
+        return None, None
+
+    def resolve_host_exe_base(self):
+        """TASK-017: 宿主 EXE 基址动态解析 (模块项 + MZ 双核 fail-closed)"""
+        host_name = os.path.basename(HOST)
+        for base, size, name in self.modules:
+            if name.lower() == host_name.lower():
+                h = self.rpm(base, 0x1000)
+                if h and h[:2] == b"MZ":
+                    return base, size
+        return None, None
+
     def wait_core_loaded(self, timeout=40):
-        """轮询等待 core.dll 固定基址就绪 (MZ at BASE) — 部署核实前置"""
+        """轮询等待 core.dll 会话基址就绪 (MZ 双核) — 部署核实前置"""
         t0 = time.time()
         while time.time() - t0 < timeout:
             self.enum_modules()
-            h = self.rpm(BASE, 0x1000)
-            if h and h[:2] == b"MZ":
+            cb, sz = self.resolve_core_base()
+            if cb:
+                self.core_base = cb
                 core_mod = [{"base": hex(b), "size": hex(s), "name": n} for b, s, n in self.modules if n.lower() == "core.dll"]
                 return {"loaded": True, "wait_s": round(time.time() - t0, 2), "core_module": core_mod}
             time.sleep(0.5)
@@ -401,7 +452,7 @@ class Harness:
         # 终态: 线程退出码 / IAT / 页级
         code = wt.DWORD(0)
         self.k32.GetExitCodeThread(self.hthread, ctypes.byref(code))
-        self.iat_post = self.read_qword(URLMON_SLOT_VA)
+        self.iat_post = self.read_qword(self.core_base + URLMON_SLOT_RVA)
         alive = self.k32.WaitForSingleObject(self.hthread, 0) == 0x102  # STILL_ACTIVE
         return {"exit_code": hex(code.value), "still_active": alive,
                 "iat_pre": hex(self.iat_pre) if self.iat_pre else None,
@@ -441,17 +492,30 @@ class Harness:
         if not wcl["loaded"]:
             return {"redline": "FAIL_CORE_NOT_LOADED", "wait": wcl}
 
-        # 2) 基址核实
-        core_head = self.rpm(BASE, 0x1000)
+        # 2) 基址动态解析 (T016 反硬编码纪律: enum_modules 会话态, MZ 双核 fail-closed)
+        self.enum_modules()
+        core_base, core_size = self.resolve_core_base()
+        if core_base is None:
+            return {"redline": "FAIL_CORE_NOT_FOUND", "modules": [n for _, _, n in self.modules]}
+        self.core_base = core_base
+        host_base, host_size = self.resolve_host_exe_base()
+        if host_base is None:
+            return {"redline": "FAIL_HOST_NOT_FOUND", "modules": [n for _, _, n in self.modules]}
+        self.host_exe_base = host_base
+        RUN_VA = self.core_base + RUN_RVA
+        URLMON_SLOT_VA = self.core_base + URLMON_SLOT_RVA
+        self.urlmon_slot_va = URLMON_SLOT_VA
+        RUN_PARAM = self.host_exe_base
+        core_head = self.rpm(self.core_base, 0x1000)
         core_ok = core_head and core_head[:2] == b"MZ"
-        host_head = self.rpm(HOST_EXE_BASE, 0x1000)
+        host_head = self.rpm(self.host_exe_base, 0x1000)
         host_ok = host_head and host_head[:2] == b"MZ"
         run_bytes = self.rpm(RUN_VA, 16)
         iat_val = self.read_qword(URLMON_SLOT_VA)
         self.iat_pre = iat_val
         deploy_check = {
-            "core_fixed_base": hex(BASE), "core_mz": core_ok,
-            "host_exe_base": hex(HOST_EXE_BASE), "host_mz": host_ok,
+            "core_base": hex(self.core_base), "core_size": hex(core_size), "core_mz": core_ok,
+            "host_exe_base": hex(self.host_exe_base), "host_size": hex(host_size), "host_mz": host_ok,
             "run_rva": hex(RUN_RVA), "run_va": hex(RUN_VA),
             "run_head": run_bytes.hex() if run_bytes else None,
             "run_head_plaintext": run_bytes[:6] == bytes.fromhex("415741564155") if run_bytes else False,
@@ -480,11 +544,11 @@ class Harness:
         # 3) 触发 Run
         tid = wt.DWORD(0)
         self.hthread = self.k32.CreateRemoteThread(
-            self.hproc, None, 0, ctypes.c_void_p(RUN_VA), ctypes.c_void_p(RUN_PARAM), 0, ctypes.byref(tid))
+            self.hproc, None, 0, ctypes.c_void_p(self.core_base + RUN_RVA), ctypes.c_void_p(RUN_PARAM), 0, ctypes.byref(tid))
         if not self.hthread:
             return {"redline": "FAIL_CREATEREMOTETHREAD", "err": ctypes.get_last_error()}
         self.tid = tid.value
-        self.log_event("run_trigger", f"CreateRemoteThread Run@{hex(RUN_VA)} param={hex(RUN_PARAM)} tid={self.tid}")
+        self.log_event("run_trigger", f"CreateRemoteThread Run@{hex(self.core_base + RUN_RVA)} param={hex(RUN_PARAM)} tid={self.tid}")
 
         # 4) RIP 采样线程 (0.03s 高密)
         self.sampling = True
@@ -547,6 +611,17 @@ class Harness:
         self.sampling = False
         sampler.join(timeout=3)
 
+        # 7.75) GUI 存活观测 (TASK-017 替代证据: IsHungAppWindow + WM_NULL 泵 + 窗口仍存在)
+        gui_obs = []
+        for w in (self.windows if self.windows else []):
+            snap = self.gui_snapshot([w["hwnd"]])
+            pumped = self.gui_pump(w["hwnd"], n=2, interval=0.03)
+            gui_obs.append({"window": w, "snapshot": snap, "wm_null_pumped": pumped})
+        self.gui_alive = any(g["snapshot"][0]["hung"] == 0 and g["snapshot"][0]["is_window"]
+                             for g in gui_obs) if gui_obs else False
+        self.gui_obs = gui_obs
+        self.log_event("gui_alive", json.dumps({"alive": self.gui_alive, "obs": gui_obs}))
+
         # 8) 终态
         fin = self.finish()
         self.log_event("final", json.dumps(fin))
@@ -583,8 +658,10 @@ class Harness:
                 "network_deny_all": "BLOCK_XX21B_RUNUI_HOST + BLOCK_XX21B_REV2_HOST (outbound block)",
             },
             "deploy_check": deploy_check,
-            "run_trigger": {"method": "CreateRemoteThread", "va": hex(RUN_VA), "param": hex(RUN_PARAM), "tid": self.tid},
+            "run_trigger": {"method": "CreateRemoteThread", "va": hex(self.core_base + RUN_RVA), "param": hex(RUN_PARAM), "tid": self.tid},
             "windows": self.windows,
+            "gui_alive": self.gui_alive,
+            "gui_obs": getattr(self, "gui_obs", []),
             "events": self.events,
             "rip_log": self.rip_log,
             "final": fin,
```
