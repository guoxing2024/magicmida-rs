# TASK-003 第 1 轮打回记录（2026-08-29，验收人：总指挥）

## 打回理由（1 条致命，1 条违规）

### 致命：E 前缀 rustc 编译错误被放行，软通过洞未堵全

验收探针（总指挥亲自注入并运行）：在 `crates/cli/src/main.rs` 注入
`let probe: u32 = "deliberate type error for TASK-003 acceptance probe";`（E0308 类型错误），
MSVC 环境 + 镜像基线（TOTAL=359）下运行本脚本，输出：

```
Running cargo clippy --workspace --lib --bins (JSON)...
NOTE: cargo clippy exited 101 (deny-level lint present).
OK: clippy warn baseline holds (TOTAL baseline=359).
EXIT=0
```

一个真正的 rustc 编译错误被判成基线通过。原因：分类器用「有无 lint code」区分 deny 命中与
编译/链接失败，但 rustc 编译错误带 E 前缀 code（E0308/E0277/E0432…），`$msg.code.code`
非空，被计入 denyLintErrors 桶放行。工单「具体要求」第 2 条第一种方式原文：
「解析 JSON 输出里有无 "level":"error" 且**不是** clippy lint 的诊断（即 rustc 编译错误 / 链接错误）」
—— rustc 编译错误是工单明文要求识别的形态，未覆盖即打回。

### 违规：具体要求第 4 条未执行

工单第 4 条：「顺手把第 50 行那句会误导人的 `NOTE: ... (deny-level lint present)` 改成不预设原因的措辞。」
该句原样保留（见上方探针输出），并在编译错误场景原形毕露。

### 已验证通过的部分（返工时不要破坏）

1. 负面用例（Git Bash 无 MSVC）：EXIT=3，输出明确「did not complete analysis」✅（总指挥亲自复现）
2. 基线漂移为既有：新旧脚本同环境输出逐字节一致，双双 EXIT=1 ✅（总指挥亲自对照）
3. OK 放行路径：健康树 + 镜像基线（TOTAL=359）→ EXIT=0 ✅（总指挥亲自运行）
4. 链接失败场景（code=null）判定正确：EXIT=3 ✅
5. deny 命中注入（CARGO_CLIPPY_EXTRA_ARGS）未误杀 ✅

## 返工要求

只改 `tools/check_clippy_baseline.ps1`；重新提交 `runs/20260829-TASK-003.md`（覆盖重写，含新验收输出）。
修正方向（二选一，注释里写清理由）：
A. deny 判据从「有 code」收紧为「code 以 `clippy::` 开头」（workspace 唯一 deny 来源是
   [workspace.lints.clippy] 的 print_stdout / dbg_macro，均为 clippy:: 前缀；rustc E 前缀
   编译错误自动落入 rustc 失败桶）—— 但注意 rustc 也有非 clippy 的 deny/warn lint（如
   unused_variables 是 rustc lint，其 deny 形态 code=unused_variables 无前缀），需一并考虑；
B. 任何 error 级诊断：无 code → rustc 失败；有 code 且以 clippy:: 开头 → deny 命中；
   有 code 且以 E 开头（或非 clippy:: 前缀）→ rustc 失败。
两个方向都要重跑完整验收（负面/OK 路径/deny 注入/E0308 注入四条全过才算过）。
E0308 注入方法：临时在 crates/cli/src/main.rs 加一行类型错误，跑完恢复，
git diff --stat 验证恢复干净后粘进报告。

## 遗留（不阻塞本工单，已上报老板）

基线漂移：HEAD 实际 354 警告 > 基线 349，另有 3 条 lint 缺表。修复前脚本同样 EXIT=1，
与本工单无关，但意味着当前 HEAD 若推送，CI clippy 必红。需老板裁定：单独提票修漂移，
或接受“本地门禁先行+基线随修复更新”。
