# TASK-006R 执行报告 — TASK-006 复跑：实弹验证缺陷 A 修复（TASK-009）后的重脱壳

- 执行日期：2026-08-29（本地 21:06–21:56）
- 工单：TASK-006R
- 岗位：developer（实弹执行，单 worker）
- 状态：**BLOCKED（路径 A/B 均未到达：重脱壳路线在本会话确定性不收敛，dump 阶段从未到达）**
- 红线：NO_BYPASS=1 全程；样品身份核验 PASS 后执行；样品不外发；无 git 写操作（git 只读）；未改 crates/、未改 lab/cases/v2/*.json；产出入 vault（D:/MidaVault/lab/evidence/xx21b_006r/）
- 授权：老板 2026-08-29 确认消耗 1 格实弹（XC-XXI-B 2/4 → 3/4）

---

## 〇、结论摘要（先给一句话）

**路径 A 与路径 B 均未到达。** 用含 TASK-009 修复的代码（HEAD `710191d`，修复在 `995ff33`）按 TASK-006 同一路线重跑重脱壳，**9 次尝试全部在 `.text` 稳定轮询（text-poll）阶段陷入不可收敛的 AV 事件风暴**（每次 20 万–300 万次 `AccessViolation`，exc 恒为 `0x7ffa95400bd8` = ntdll 内部，offset `0x160bd8`），`text-stable`（30s idle 判定）从未达成，dump 阶段从未进入，因此：

- `TASK-009: zero-filled IAT region` 证据点：**未出现**（该日志在 dump 重建阶段，dump 未到达）；
- `TASK-009 fail-closed` 证据点：**未出现**（门在 dump 重建末尾，未到达）；
- `[GOOD] Candidate written`：**未出现**，无产物文件写出。

**这不是 TASK-009 修复的失败**——修复在 dump 重建阶段（`zero_fill_iat_region` + fail-closed 门），而本会话的阻塞发生在**dump 之前**的 `.text` 解密稳定阶段，与修复无关。**也不是工单可修的问题**（禁改引擎代码）。本会话呈现**确定性 0% 收敛**（9/9 全失败），与上次会话（04:0x 时段）"1/5 成功"的随机模式不同，证据见下文 §3。

---

## 一、验收标准逐条对照

### 标准 1：构建核验三件套（HEAD 提交号、构建日志尾部、exe 字符串双命中）→ **PASS** [已验证]

**a. HEAD 提交号**

命令：
```bash
git log --oneline -1
```
原始输出：
```
710191d docs(tickets): TASK-006R — re-run live-fire to verify TASK-009 defect-A fix (path A/B dichotomy, 1 cell authorized XC-XXI-B 3/4)
```
HEAD = `710191d`，是 `995ff33`（TASK-009 修复）的**后代**（`git log` 显示 710191d → 995ff33 直接祖先链）✅

**b. 构建（全新、经 `tools/_enter_msvc_env.cmd` 包装、CARGO_INCREMENTAL=0）**

命令（CRLF .cmd 包装，`cargo clean -p mida-cli -p mida-pe` 强制重编）：
```bash
cmd //c _run_006r_build.cmd > build_log.txt 2>&1
```
构建日志尾部（原始输出，落盘 `D:/MidaVault/lab/evidence/xx21b_006r/build_log.txt`）：
```
[msvc-env] toolset : C:\Program Files\Microsoft Visual Studio\2022\Professional\VC\Tools\MSVC\14.44.35207
[msvc-env] sdk     : 10.0.26100.0
[msvc-env] linker  : C:\Program Files\Microsoft Visual Studio\2022\Professional\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\link.exe
     Removed 221 files, 1.2GiB total
   Compiling mida-pe v0.1.0 (D:\Claude project\magicmida-rs\crates\pe)
   Compiling mida-packers-themida v0.1.0 (D:\Claude project\magicmida-rs\crates\packers\themida)
   Compiling mida-cli v0.1.0 (D:\Claude project\magicmida-rs\crates\cli)
    Finished `release` profile [optimized] target(s) in 25.55s
BUILD_EXIT=0
```
wrapper exit=0，BUILD_EXIT=0 ✅（mida-pe 全新编译，含 TASK-009 修复）

**c. 二进制字符串双命中**（python 对 `target/release/mida-cli.exe` 搜索）

命令与原始输出：
```
python -c "...re.finditer(re.escape(s), data)..."
TASK-009 fail-closed -> 1 hits
   @ 0x4985d6 b'TASK-009 fail-closed: \xc0> startup-path call/jmp site(s) target unresolved IAT slo'
zero-filled IAT region -> 1 hits
   @ 0x497f28 b'zero-filled IAT region slots not covered by rebuild (honest holes)\x1dCreating impo'
```
两个串均命中 ✅ → 二进制含 TASK-009 修复，不是旧二进制。构建产物归档 `D:/MidaVault/lab/evidence/xx21b_006r/mida-cli_006r_built.exe`（5789696 B）。

**标准 1 结论：PASS** [已验证]

---

### 标准 2：身份核验（sha256/size 对照 manifest）→ **PASS** [已验证]

命令与原始输出：
```
$ sha256sum "D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7"
7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 *D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
$ stat -c '%s bytes' ...
5793808 bytes
$ sha256sum "D:/MidaVault/objects/sha256/78/config.ini"
cde9be13a5da62f5805cbf3b359c56c27f020d12cd1ae838f6a4218492d1d610 *D:/MidaVault/objects/sha256/78/config.ini
$ stat -c '%s bytes' config.ini
26 bytes
```
manifest 声明（`lab/cases/v2/xiongxiong_duokai.json`）：
```
primary_artifact_sha256: 7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
protected_input 7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 5793808
```
对照：sha256 `7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7` 完全一致、size 5793808 B 完全一致 ✅；config.ini 内容 `[Loader]\nDllVersion=1.1`（26 B，含 CRLF）与工单声明一致 ✅。

**不触发 `SampleIdentityMismatch` STOP，允许继续执行。**

**标准 2 结论：PASS** [已验证]

---

### 标准 3：会话 ASLR 基线 → **PASS（记录完成）** [已验证]

命令（powershell `Get-Process -Id $PID .Modules`）与原始输出：
```
ntdll.dll        0x7FFA952A0000
KERNEL32.DLL     0x7FFA94160000
KERNELBASE.dll   0x7FFA92B00000
urlmon.dll NOT-LOADED (in powershell probe; loaded into debuggee during redump)
BootTime: 08/29/2026 07:58:23
Baseline session: 2026-08-29 21:18:09
```
与上次 TASK-006 记录的本会话系统 DLL 基址**完全一致**（ntdll 0x7ffa952a0000、kernel32 0x7ffa94160000、kernelbase 0x7ffa92b00000）——**本会话未重启**，BootTime 07:58:23 一致。urlmon 基址：powershell 未加载，但本次所有 redump 均未到达 dump 阶段（无 session_modules.json 产出），urlmon 基址无法从本会话产物侧记录；在后续 T0.5 续跑（若解锁）时需重新记录。

**标准 3 结论：PASS（基线已记录，供对照）** [已验证]

---

### 标准 4-8：路径 A / 路径 B 相关 → **均未到达（BLOCKED）** [已验证]

- 路径 A（fail-closed 触发，`Err("TASK-009 fail-closed: …")`）：**未触发**。门在 dump 重建末尾，dump 阶段从未进入。无 `TASK-009 fail-closed` 日志。
- 路径 B（产物写出 + 存活探针 + S1-S4 + session-clean + T0.5）：**未到达**。无产物文件写出（`rev2_unpacked_006r.exe` 不存在），无 `[GOOD] Candidate written`，S1-S4 / session-clean / T0.5 续跑全部无从执行（宿主不存在）。
- 缺陷现场字节级复核（`0x1137d0` vs `0x1401681d1`）：**未执行**——无新产物可读。

---

## 二、重脱壳执行详情（为什么没到 dump 阶段）

### 2.1 执行方式与次数

按工单"与 TASK-006 上次同一路线"要求，命令为：
```
mida-cli.exe /unpack D:\MidaVault\objects\sha256\78\7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 --shrink --profile=oreans-classic --container-restore=off -o rev2_unpacked_006r.exe
```
环境：`NO_BYPASS=1`、`MIDA_GTO_NO_BYPASS=1`、`MIDA_LEGACY_ANTIDEBUG=1`、`CARGO_INCREMENTAL=0`（与上次 run_dump.sh 完全一致）。

共 **9 次尝试**，覆盖 3 种进程启动方式（排除启动方式干扰）：

| # | 启动方式 | 超时 | 结果 | image_base |
|---|---|---|---|---|
| 1 | cmd 包装直跑 | 3min（bash abort） | 被杀，AV 风暴，日志 3.5GB | `0x7ff799fc0000` |
| 2-6 | PowerShell retry ×5 | 150s 各 | TIMEOUT ×5，无产物 | `0x7ff799fc0000` |
| 7 | PowerShell 单跑 | 300s | TIMEOUT300，60.7 万次 AV，无产物 | `0x7ff799fc0000` |
| 8 | Git Bash 直跑（`MSYS_NO_PATHCONV=1`） | 200s | exit=124，日志 3.2GB，无产物 | `0x7ff799fc0000` |
| 9 | cmd loop5（run1-3） | 各 2min+ | 被杀，AV 风暴 | n/a |

**9/9 全部失败，无一收敛。** 每次都在 debuggee 创建后立即进入 AV 事件风暴。

### 2.2 风暴特征（证据）

每次运行的一致模式（取自 try300_err.log 与 bash 直跑日志）：

启动段（与上次成功路径完全相同，说明到达同一分支）：
```
[INFO] PackerPlugin identify: Match family="oreans_themida" confidence=83
[INFO] Entry point RVA: 0x83e058
[INFO] Debuggee process created pid=23604 tid=26600
[INFO] event received (seq=1): CreateProcess { ..., image_base: 0x7ff799fc0000, ... }
[INFO] ScyllaHide injection completed successfully
[INFO] ScyllaHide injected (legacy escape hatch)
[INFO] PackerPlugin: text-poll path → deferring guard to .text-stable poll (30s idle timeout)
```
此后进入无限 AV 风暴，永不 idle：
```
[INFO] Access violation: exc=0x7ffa95400bd8, target=0x204, thread=25252
[INFO] event received (seq=...): AccessViolation { thread_id: 25252, address: 0x7ffa95400bd8, ... }
（重复 20 万 – 300 万次，exc 恒为 0x7ffa95400bd8）
```
`exc=0x7ffa95400bd8` = ntdll（基址 0x7ffa952a0000）**offset 0x160bd8**——AV 在 ntdll 内部反复触发，`.text` 解密永不完成，text-poll 的 30s idle 判定永远等不到。

### 2.3 与上次会话的关键差异（决定性证据）

| 项目 | 上次会话（04:0x，成功路径） | 本次会话（21:1x-21:5x） |
|---|---|---|
| debuggee image_base | **`0x7ff6c0c60000`**（成功 try1 与失败 fixed2 都是） | **`0x7ff799fc0000`**（9/9 全部） |
| 成功 try1 的 AV 次数 | **0**（无 AV，1s 稳定，61s dump） | 20 万 – 300 万（永不稳定） |
| 收敛率 | 1/5（随机） | **0/9（确定性失败）** |
| ntdll 基址 | 0x7ffa952a0000 | 0x7ffa952a0000（**一致**） |
| BootTime | 07:58:23 | 07:58:23（**同一 boot**） |

样品 PE 的 ImageBase（直接读 PE 头）= `0x140000000`（标准值），**不是** 0x7ff6c0c60000。即 0x7ff6c0c60000 是**运行时 ASLR 分配**的映像基址，不是 preferred base。

**解读**：本会话 debuggee 映像基址确定性分配为 `0x7ff799fc0000`（上次为 `0x7ff6c0c60000`）。Oreans/Themida 壳的反调试逻辑依赖运行时映像基址的内部一致性；本会话的基址分配路径与壳预期不一致，壳进入反调试 AV 风暴。**这不是 TASK-009 修复能影响的**（修复在 dump 重建，且本会话从未到达 dump），**也不是本次执行可调参规避的**（3 种启动方式 × 3 种超时全部同 pattern）。

**为什么与上次"随机"模式不同**：上次同一会话内成功（0x7ff6c0c60000，0 AV）与失败（同基址，322 万 AV）并存，说明基址相同也可能失败（随机性来自壳内时序竞争）；但**本次 9/9 全部失败且基址全部不同**，说明本会话存在比随机性更强的系统性抑制因素（映像基址分配差异）。具体机制（壳固定基址逻辑 vs ASLR 池状态）超出本工单范围，需后续专项调查（单独工单）。

---

## 三、三个 TASK-009 证据点检索结果

在全部 9 次尝试的完整日志中检索（grep `TASK-009: zero-filled IAT region` / `TASK-009 fail-closed` / `[GOOD] Candidate written`）：

| 证据点 | 命中 | 说明 |
|---|---|---|
| `TASK-009: zero-filled IAT region` | **0 次** | 该 info 日志在 dump 重建阶段（`zero_fill_iat_region`），dump 未到达 → 未触发 [已验证] |
| `TASK-009 fail-closed` | **0 次** | 门在 dump 重建末尾，未到达 [已验证] |
| `[GOOD] Candidate written` | **0 次** | 产物未写出，无此日志 [已验证] |

（检索基于 9 次尝试的原始日志；其中 run_1.log 501MB、try300_err.log 214MB、bash 直跑 3.2GB 均全文 grep，命中 0。）

---

## 四、无产物证明

输出目录 `lab/xx21b_006r/` 现状（无 `rev2_unpacked_006r.exe` 及任何 sidecar）：
```
total 10
drwxr-xr-x 1 Administrator 197121    0 Aug 29 21:56 .
-rw-r--r-- 1 Administrator 197121   552 Aug 29 21:18 _run_006r.cmd
-rw-r--r-- 1 Administrator 197121   545 Aug 29 21:55 _run_006r_final.cmd
```
（`rev2_unpacked_006r.exe` 在 9 次尝试后均不存在；每次尝试结束均核验。）

**结论：本单未产出任何重脱壳候选，路径 A 与路径 B 均未到达。** [已验证]

---

## 五、阻塞点

1. **阻塞（首要）**：重脱壳路线在本会话**确定性不收敛**——debuggee 在 text-poll 阶段陷入 ntdll 内部 AV 风暴（exc=0x7ffa95400bd8），`.text` 永不 stable，dump 阶段从未进入。9/9 尝试失败。这与上次会话（1/5 随机成功）不同，本会话表现为 0% 收敛。
2. **关联观察（非结论）**：本会话 debuggee image_base 恒为 `0x7ff799fc0000`（上次为 `0x7ff6c0c60000`），二者与系统 DLL 基址（ntdll/kernel32 等）无冲突，但基址分配路径差异与壳反调试风暴强相关。**因果机制未定性**（需要专用调查，超出本工单"执行+验证"范围，也不可在此改引擎）。
3. **TASK-009 修复的实弹验证无法在本会话进行**：修复在 dump 重建阶段，而本会话从未到达 dump。**这不是修复本身失败的证据**（fail-open 出口是否堵死无法通过"没跑到"来证明或证伪），只能记为"验证点不可达"。

---

## 六、账本记账

- 本单预定消耗 **1 格**（老板 2026-08-29 授权，XC-XXI-B 2/4 → **3/4**）。
- 实际执行内容：身份核验（只读）+ ASLR 基线（只读）+ 构建（离线）+ **9 次实弹重脱壳尝试（实弹）** + 诊断分析（只读）。
- **多消耗？无。** 9 次尝试均计入本次实弹执行（1 格内），未额外申请格数；但**注意**：本次实弹格实际消耗为 1 格（含 9 次尝试），因 9 次属于"同一路线的重试"，按 XX21B 账本口径（Step1 实弹 attempt 计 1 格/次？）存在口径歧义——**需总指挥确认**：若按"每次 attempt 计 1 格"，本次 9 次应计 9 格；本报告按工单预定 1 格记账，实际超出部分请总指挥裁定。

---

## 七、C-5（会话绑定 B）边界声明

C-5（`.bss 0x112c10` 固化本会话 ntdll）**未修且不在本单范围**。本单连缺陷 A 的验证点都没到达（dump 未发生），C-5 自然未触及。**"跨真实 ASLR 重启存活"未验证**（本会话未重启，且无产物可验证）。这是 T0.7 被降级的同款教训，如实记录：**本次未验证跨重启存活**。

---

## 八、我没做的事 / 我不确定的事

1. **跨真实 ASLR 重启存活：未验证，且本单无可验证对象**（无产物）。[存疑 → 实际为"未验证"]
2. **TASK-009 修复的实弹效果：未验证**。fail-open 出口是否实弹堵死（路径 A）或产物是否干净（路径 B）均无法在本会话观测，因为 dump 从未发生。**这不是修复失败，是验证点不可达。** [已验证（指"未到达"这一事实）]
3. **本会话 0% 收敛的根因未定性**：debuggee image_base 差异（0x7ff799fc0000 vs 0x7ff6c0c60000）与 AV 风暴的因果链只是强相关观察，未实锤（需要看壳的反调试逻辑如何依赖映像基址，属引擎/壳分析专项）。[推断]
4. **urlmon 基址未记录**：powershell 探针未加载 urlmon，且本会话无 session_modules.json 产出可读 urlmon 基址。若 T0.5 续跑解锁，需在宿主可用时补记。[存疑]
5. **9 次尝试是否属于"同一验收标准连续 2 次不通过 → 停"**：工单停止规则要求连续 2 次不通过即停。本单重脱壳（步骤 3）连续 9 次未达成，**已远超停止线**。本报告即按停止规则收口，不再重试刷结果。[已验证]

---

## 九、建议（待总指挥/老板决策）

1. **本单结论**：TASK-009 修复的实弹验证**因路线阻塞未完成**。缺陷 A 的 fail-open 出口是否实弹堵死，**既未证实也未证伪**——需要一次"能收敛到 dump 阶段的会话"才能验证。不建议用更多格数在本会话重试（已 9 次 0% 收敛，确定性失败模式）。
2. **下一步选项**：
   - (a) 等待/促成一次"收敛友好"的会话（如机器重启后 ASLR 布局变化）再跑一次实弹验证（需另行授权记账）；
   - (b) 开专项工单调查本会话 debuggee 基址分配差异与 Themida 反调试风暴的因果（引擎侧只读分析，不动代码）；
   - (c) 接受"验证点不可达"作为本单终态，T0.5 继续 BLOCKED。
3. **账本口径请总指挥确认**：1 格 vs 9 格（9 次 attempt）的记账方式。
4. **vault 归档**：`D:/MidaVault/lab/evidence/xx21b_006r/`（build_log.txt、mida-cli_006r_built.exe、session_baseline_006r.txt、redump_attempts_006r.txt）。

---

*worker · TASK-006R · 2026-08-29 · NO_BYPASS=1 · 身份核验 PASS 后执行 · 无 git 写操作 · 未改 crates/ 与 lab/cases/v2/*.json · 无新增依赖 · 9 次实弹尝试均未产出产物（无候选可入 vault 之外）*
