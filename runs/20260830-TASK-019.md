# TASK-019 执行报告 — T0.5 终跑（完美候选路线）：B1' 宿主 + 脱壳 core 完美候选 + 调试泵，三态判定（终态：新阻塞类——宿主启动即 AV，core.dll 加载前 → STOP）

## 1. 授权令牌（原文回抄）

> `老板 · 2026-08-30 · 原话"批准"（按全案解释：批准 TASK-019 = T0.5 终跑完美候选路线，实弹 1 格，账本 XC-XXI-B 11/4 → 12/4；判定语义边界（宿主+候选组合、FULL=路径级结论）、固定基址残留条款、"附加改变行为"保险丝均以票面为准）· 前置由总指挥亲验（2026-08-30）：候选文件 D:/MidaVault/lab/worktree_evidence_20260830/lab/xx21b_run/core_perfect_candidate.dll sha 现算 = 3650ea6c… 一致；宿主 a852880a / config.ini cde9be13 在位；HEAD = daae270；BootTime 连续性由 worker 开跑前自查（对照 T018 记录 10:05:53.549）`

（注：HEAD 现状 2f1dc24 = daae270 之后 1 个提交 `docs: TASK-019 authorized`；起点核验符合票面。）

## 2. 改动文件（对照授权清单）

| 文件 | 类型 | 说明 |
|---|---|---|
| `tools/xx21b_t05_ui_drive_pcell.py` | 新增（授权） | T019 驱动脚本，fork 自 T018 dbg 版；原两套脚本（T017 `xx21b_t05_ui_drive.py` / T018 `xx21b_t05_ui_drive_dbg.py` / T018-resume）未改 |
| `lab/xx21b_run_pcell/` | 新建（授权） | 部署目录：rev2_unpacked.exe（a852880a）、core.dll（3650ea6c）、config.ini（cde9be13）+ 2 趟证据 |
| `D:/MidaVault/lab/evidence/xx21b_t05/` | vault 证据（授权） | pcell_att1/att2 各 3 文件 + preflight_selfcheck.md + INDEX.md 更新 |

`crates/` 一行未动；git 只读（未 commit/push）；未新增依赖（仅标准库 + ctypes/subprocess）。

## 3. 任务 1 完成（新脚本，自查）

### 3.1 语法自查

命令：`python -c "import ast; ast.parse(open('tools/xx21b_t05_ui_drive_pcell.py', encoding='utf-8').read()); print('SYNTAX_OK')"`
原始输出：`SYNTAX_OK`，`EXIT=0`

### 3.2 导出动态解析单元自测（不经运行宿主，读 vault 候选文件）

命令（内联 python，monkeypatch 模块 CORE 指向 vault 候选）：
原始输出：
```
err: None
exports: {'GetAppVersion': '0xbb30', 'Run': '0x1c120'}
Run: 0x1c120 GetAppVersion: 0xbb30
urlmon slot rva: 0x16f300
EXPORT_RESOLVE_UNIT_OK
```
结论 [已验证]：Run@0x1c120 / GetAppVersion@0xbb30 / urlmon IAT 槽@0x16f300——与 T0.4 报告（`docs/XX21B_CORE_PERFECT_REPORT_20260829.md`：GetAppVersion@0xBB30 / Run@0x1C120 / IAT 槽 0x16f300=URLDownloadToFileA）逐位一致。任务 1 的"Run 走导出动态解析、不许硬编码 RVA"要求满足（脚本中无 0x1c120/0xbb30/0x16f300 常量，均为运行时导出表解析；grep 核验仅命中注释文本）。

### 3.3 sha 双门更新核验

命令：`grep -n "CAND_SHA\|HOST_SHA\|CONFIG_SHA" tools/xx21b_t05_ui_drive_pcell.py | head -6`
原始输出：
```
30:CAND_SHA = "3650ea6c0a88c731d4b613eaa533ab1d48258ce782843a5661ca6c683fd9b64e"
31:HOST_SHA = "a852880aabba215b16a2a96245322ca09d19ff148afaa30ff42b1a8ea438edac"
32:CONFIG_SHA = "cde9be13a5da62f5805cbf3b359c56c27f020d12cd1ae838f6a4218492d1d610"
```
结论 [已验证]：CORE=3650ea6c（14,435,328 B）/ HOST=a852880a（1,539,072 B）/ CONFIG=cde9be13 三核更新到位，fail-closed 判定在 `run()` 第 0 步。

### 3.4 泵机制与 T018 一致核验（grep）

- `DEBUG_ONLY_THIS_PROCESS` 宿主创建在泵线程内（`create_host_debug` 由 `pump_loop` 调用，T018 实测线程绑定）；
- 泵线程 `WaitForDebugEvent(500ms)` → `_handle_debug_event` → 立即 `ContinueDebugEvent`（全量即时消费，P2b 冻结坑规避）；
- Run 线程句柄取自 `CREATE_THREAD_DEBUG_EVENT`（不用 OpenThread，P-11）；
- EXCEPTION 全记录：0x80000003 首个引导断点 + 0xc000008e 引导期良性浮点 → DBG_CONTINUE，其它（含真实 AV 0xc0000005）→ DBG_EXCEPTION_NOT_HANDLED；
- 泵健康自证（continues/fails/wait_errors/first_breakpoint_seen）+ attach_changed_behavior 上报 + NO_BYPASS=1 + 防火墙只读核实——全部保留。

原脚本未改验证：
命令：`git diff --stat HEAD -- tools/xx21b_t05_ui_drive.py tools/xx21b_t05_ui_drive_dbg.py tools/xx21b_t05_ui_drive_resume.py; echo $?`
原始输出：`0`（无差异，空输出）→ [已验证] 三套既有脚本零改动。

## 4. 开跑前自查（验收标准 5）

### 4.1 BootTime 连续性（对照 T018 记录 10:05:53.549）

命令：`powershell -NoProfile -Command "(Get-CimInstance Win32_OperatingSystem).LastBootUpTime.ToString('yyyy-MM-dd HH:mm:ss.fff')"`
原始输出：`2026-08-30 10:05:53.549`
结论 [已验证]：与 T018 记录**逐字符一致**，同一连续 boot 家族，会话全程无重启（C-4/B 跨重启风险本单不适用）。

### 4.2 部署物三文件 sha 复核（fail-closed）

命令：`sha256sum lab/xx21b_run_pcell/rev2_unpacked.exe lab/xx21b_run_pcell/core.dll lab/xx21b_run_pcell/config.ini`
原始输出：
```
a852880aabba215b16a2a96245322ca09d19ff148afaa30ff42b1a8ea438edac *lab/xx21b_run_pcell/rev2_unpacked.exe
3650ea6c0a88c731d4b613eaa533ab1d48258ce782843a5661ca6c683fd9b64e *lab/xx21b_run_pcell/core.dll
cde9be13a5da62f5805cbf3b359c56c27f020d12cd1ae838f6a4218492d1d610 *lab/xx21b_run_pcell/config.ini
```
结论 [已验证]：宿主 a852880a（1,539,072 B，= T017 实弹同款 rev2_unpacked_t015_attempt2）/ 候选 3650ea6c（14,435,328 B）/ config cde9be13 三核全匹配票面。
部署来源说明：宿主取自 `D:/MidaVault/lab/evidence/xx21b_t015/rev2_unpacked_t015_attempt2.exe`（a852880a，T017 实弹部署同源；vault `xx21b_run/` 内 rev2_unpacked.exe 实为 T0.4 老宿主 36043cb4，票面部署项以 a852880a 为准——已按票面 `vault/xx21b_t015/rev2_unpacked_t015_attempt2.exe` 部署并核验）；候选取自 vault `worktree_evidence_20260830/lab/xx21b_run/core_perfect_candidate.dll`（3650ea6c）；config 取自 vault 同目录（cde9be13，`[Loader] DllVersion=1.1`）。

### 4.3 防火墙 BLOCK 现状核实（只读，不改）

命令：`Get-NetFirewallRule -Direction Outbound -Action Block | Where-Object { $_.Enabled -eq "True" } | Select-Object -ExpandProperty DisplayName`
原始输出：
```
codex_sandbox_offline_block_loopback_udp
codex_sandbox_offline_block_loopback_tcp
codex_sandbox_offline_block_outbound
BLOCK_GTO_LAUNCHER
BLOCK_GTO_TMP
BLOCK_XX21B_REV2_HOST
BLOCK_XX21B_RUNUI_HOST
BLOCK_XX21B_RESUME_HOST
BLOCK_XX21B_RESUME_COREHOST
BLOCK_XX21B_RESUME_CORE_DEPLOY
BLOCK_XX21B_RESUME_RUNUI
```
程序范围核验：`codex_sandbox_offline_block_outbound` = Program=Any, Profile=Any, Outbound, Block, Enabled=True → **全程序出站阻断**，覆盖新部署路径 `lab/xx21b_run_pcell/rev2_unpacked.exe`（与各 BLOCK_XX21B_* 程序规则叠加）。无真实外联可能。未新增/修改任何防火墙规则。

## 5. 实弹执行（2 趟，验收标准 1/2）

### 5.1 第 1 趟（pcell_att1）

命令：`python tools/xx21b_t05_ui_drive_pcell.py pcell_att1 "lab/xx21b_run_pcell/pcell_att1_evidence.json" --no-terminate`
原始输出（关键）：
```
redline sha OK (core=3650ea6c perfect candidate, host=a852880a, config=cde9be13)
[1788088833.239] export_resolve: {"ok": true, "Run_rva": "0x1c120", "GetAppVersion_rva": "0xbb30", "urlmon_slot_rva": "0x16f300", "preferred_base": "0x7ffe1da10000", "resolved_from_disk_export_table": true}
[1788088834.620] firewall_check: {"ok": true, "count": 11, "rules": [...]}
[  0.000] host_start_debug: pid=21288 DEBUG_ONLY_THIS_PROCESS(0x2) NO_BYPASS=1
[  0.101] core_load_pump: core.dll LOAD_DLL base=0x7ffd36310000 (pump event)
[ 12.108] core_load_wait: {"loaded": false, "wait_s": 10.01, "core_module": null}
EXIT=0
```
泵全量事件（ndjson）中 EXCEPTION 序列：
```
seq21 EXCEPTION_DEBUG_EVENT code=0x80000003 addr=0x7ffd37afd78d firstChance=1 -> DBG_CONTINUE_first_breakpoint
seq44 LOAD_DLL_DEBUG_EVENT base=0x7ffd36310000  (core.dll 候选加载事件)
seq47 EXCEPTION_DEBUG_EVENT code=0xc0000005 addr=0x7ffeeb426390 firstChance=1 thread=20720 threadRip=0x7ffeeb426390 handled=DBG_EXCEPTION_NOT_HANDLED
seq48 EXCEPTION_DEBUG_EVENT code=0xc0000005 addr=0x7ffeeb426390 firstChance=0 (second-chance -> 进程终止)
... EXIT_THREAD_DEBUG_EVENT exitCode=3221225477 xN
... EXIT_PROCESS_DEBUG_EVENT exitCode=3221225477 (0xC0000005)
```
判定字段：`redline=FAIL_CORE_NOT_LOADED`（宿主在 core.dll 加载完成前崩溃，Run 未触达）；`exit_code=3221225477`（0xC0000005）；泵 `continues=56, continue_fails=0, wait_errors=[], first_breakpoint_seen=true`。

### 5.2 第 2 趟（pcell_att2）

命令：`python tools/xx21b_t05_ui_drive_pcell.py pcell_att2 "lab/xx21b_run_pcell/pcell_att2_evidence.json" --no-terminate`
原始输出（关键）：
```
redline sha OK (core=3650ea6c perfect candidate, host=a852880a, config=cde9be13)
[1788088891.581] export_resolve: {"ok": true, "Run_rva": "0x1c120", "GetAppVersion_rva": "0xbb30", "urlmon_slot_rva": "0x16f300", ...}
[1788088893.068] firewall_check: {"ok": true, "count": 11, ...}
[  0.000] host_start_debug: pid=27472 DEBUG_ONLY_THIS_PROCESS(0x2) NO_BYPASS=1
[  0.101] core_load_pump: core.dll LOAD_DLL base=0x7ffd354d0000 (pump event)
[ 12.107] core_load_wait: {"loaded": false, "wait_s": 10.01, "core_module": null}
EXIT=0
```
EXCEPTION 序列（ndjson）：
```
EXCEPTION_DEBUG_EVENT code=0x80000003 addr=0x7ffd37afd78d firstChance=1 -> DBG_CONTINUE_first_breakpoint
EXCEPTION_DEBUG_EVENT code=0xc0000005 addr=0x7ffeeb426390 firstChance=1 thread=1604 threadRip=0x7ffeeb426390 handled=DBG_EXCEPTION_NOT_HANDLED
EXCEPTION_DEBUG_EVENT code=0xc0000005 addr=0x7ffeeb426390 firstChance=0 -> 进程终止
```
判定字段：`redline=FAIL_CORE_NOT_LOADED`；`exit_code=3221225477`；泵 `continues=58, continue_fails=0, wait_errors=[]`。

### 5.3 根因定位（跨 boot ASLR，宿主陈旧 ntdll 绝对地址）

AV 地址 `0x7ffeeb426390` 复算：
命令：`python -c "... ntdll base (current process, this boot) ..."`
原始输出：
```
ntdll base (current process, this boot): 0x180000000 size: 0x266000
0x7ffeeb426390 - current ntdll base: 0x7ffd6b426390
0x7ffeeb426390 - 0x106390: 0x7ffeeb320000
```
对照 T0.4 报告 `docs/XX21B_CORE_PERFECT_REPORT_20260829.md` §阻断根因（原文）：
> 机器 07:58:23 重启后系统 DLL ASLR 重随机化（ntdll `0x7ffeeb320000` → `0x7ffa952a0000`）；宿主 `rev2_unpacked.exe` 样品文件 RVA `0x112c10` 硬编码陈旧 ntdll 绝对地址 `0x7ffeeb426390`（= 旧 ntdll+0x106390），启动初始化期 RVA `0x21cc0-0x21cd8` `call rax` 指令取指 AV（c0000005）→ 宿主崩溃于 **core.dll 加载之前**。

本次 att1/att2 AV 地址 `0x7ffeeb426390` = 旧 ntdll `0x7ffeeb320000` + `0x106390`，与 T0.4 记录**逐字节一致** → 同一陈旧绝对地址失配根因。宿主（a852880a 与 T0.4 宿主同源样品）启动初始化期即 AV，崩溃于 core.dll 加载完成前 → Run 不可触达。

### 5.4 附加改变行为自证

宿主崩溃于引导早期（core.dll 加载前、窗口创建前），无任何 GUI 窗口出现；"授权验证"PigToGoLicenseDialog 无从观测。attach_changed_behavior 判定在此路径下不适用（进程未到窗口创建阶段即终止）——如实上报，不硬凑。

## 6. 验收标准逐条对照

### 标准 1：三态判定（证据源 = 调试端口，语义与 T017/T018 逐字一致）
→ **新阻塞类（宿主启动即 AV，core.dll 加载前）[已验证]，2/2 可复现**：宿主（a852880a）启动初始化期 EXCEPTION 0xc0000005 @ `0x7ffeeb426390`（= 旧 ntdll+0x106390，T0.4 字节级证据同一地址），core.dll 加载完成前崩溃 → Run 不可触达 → `FAIL_CORE_NOT_LOADED`。这不是 Run 线程 AV（Run 从未触发）、不是候选 core 问题（候选 Run 字节磁盘静态明文 `415741564155…`，无 C-8 残留）。按票面 STOP。
- FULL：不可达（Run 未触发）。
- AV（Run 线程）：不适用（AV 发生在宿主引导、Run 触发前）。
- 附加改变行为：不适用（进程死于窗口创建前），如实上报。

### 标准 2：调试泵健康自证（事件全消费、无冻结征兆，延续 T018 判据）
→ [已验证] 2/2 趟：continues 56/58、continue_fails 0、wait_errors 空、first_breakpoint_seen=true；引导断点 0x80000003 与 0xc0000005 ×2（first+second chance）全部记录并消费；无泵冻结征兆（进程崩溃退出后泵正常终止，非冻结）。

### 标准 3：脚本关键 diff/全文 + 判定 + 全部原始输出进报告；结论按 [已验证]/[推断]/[存疑]
→ 本报告含关键 diff（§3）、判定（§5/§6）、原始输出（§4/§5）；脚本全文在工作区 `tools/xx21b_t05_ui_drive_pcell.py`（授权新增，未 commit）。

### 标准 4：零越界
→ [已验证] 仅新增 `tools/xx21b_t05_ui_drive_pcell.py` + `lab/xx21b_run_pcell/` + vault 证据；`crates/` 零改动（git status 无 M）；T017/T018/resume 三套脚本零改动（git diff 空）；git 只读（未 commit/push）。

### 标准 5：开跑前自查（BootTime / 部署物 sha / 防火墙）
→ [已验证] §4：BootTime=10:05:53.549 与 T018 一致；部署三文件 sha 三核匹配；防火墙 11 条 BLOCK 在位且 codex 全程序 outbound 覆盖新路径（未拦截 → 未出现，网络被覆盖拦截 → 不真联网；未改防火墙）。

### 标准 6：临时文件逐个删除；vault 证据先行；报告第一节回抄授权令牌
→ 无临时脚本生成（未用 _run.cmd 模式，纯 python 直跑）；vault 证据先行（§7）；令牌回抄见 §1。

## 7. vault 证据（先行，P-8）

命令：`cp lab/xx21b_run_pcell/{pcell_att1_evidence.json,pcell_att1_evidence_pump.json,pcell_att1_dbg_pump_events.ndjson,pcell_att2_evidence.json,pcell_att2_evidence_pump.json,pcell_att2_dbg_pump_events.ndjson,preflight_selfcheck.md} D:/MidaVault/lab/evidence/xx21b_t05/`
结果：7 文件已拷入 `D:/MidaVault/lab/evidence/xx21b_t05/`（ls 核验 pcell_* 在位）；INDEX.md 追加 TASK-019 章节（含两趟 sha 与判定摘要）。

## 8. 阻塞点

1. **宿主（a852880a）跨 boot ASLR 陈旧绝对地址失配（C-4/B 未根治）**：宿主样品文件 RVA 0x112c10 硬编码旧 boot ntdll 绝对地址 `0x7ffeeb426390`，当前 boot 下启动初始化期 RVA 0x21cc0-0x21cd8 `call rax` 取指 AV（0xc0000005）→ 崩溃于 core.dll 加载前 → Run 不可触达。T0.4 报告已记录同因（07:58:23 重启后）；本次 BootTime=10:05:53.549 与 T018 同 boot，但宿主样品绑定的是更早 boot（T0.4 生产期）的 ASLR 布局——**非本单可解**，需总指挥决策（重脱壳宿主 / ASLR 匹配环境），不另立格不自行处理。
2. **基址一致性条款（票面语义边界）未触达**：因宿主在 core.dll 加载完成前崩溃，实弹未能观测"实际加载基址 vs 首选基址 0x7ffe1da10000 是否一致"。脚本已实现该记录（base_agreement 字段），后续若宿主问题解除可复跑直接获得。
3. **attach_changed_behavior 判定不适用**：进程死于窗口创建前，GUI 层无从观测。

## 9. 我没做的事 / 我不确定的事

- **没做**：没有重脱壳宿主（票面红线：需另立单另批格）；没有为规避宿主 AV 修改脚本/宿主/候选；没有新增防火墙规则；没有 commit/push；没有跑 T017/T018 原脚本；`crates/` 未动。
- **不确定**：
  1. [存疑] 宿主 a852880a 的 ASLR 失配在"重新启动后是否恢复"——T0.4 记录 reboot 后 ntdll 换址导致失配；若总指挥在宿主绑定的 boot（T0.4 生产期 10:05:51 之前）下运行，宿主可能正常。但当前 boot 下宿主必崩，本单判定不受影响。
  2. [推断] 0xc000008e 引导期良性浮点在本次未出现（T018 每趟 1 次）——可能因宿主在到达该异常点前已 AV 崩溃；不影响判定。
  3. [存疑] `codex_sandbox_offline_block_outbound` 对本地回环 UDP/TCP 亦有阻断规则，宿主若在崩溃前尝试本地回环也会被环境拒绝——本单宿主未触达任何网络逻辑即崩溃，无外联证据。

## 10. 判定摘要（给总指挥）

- **判定 = 新阻塞类（宿主启动即 AV，core.dll 加载前），2/2 可复现 → STOP（按票面）**。
- 根因 = 宿主（a852880a）跨 boot ASLR 陈旧 ntdll 绝对地址失配（C-4/B），与 T0.4 记录同因；**非候选 core 问题、非 Run 线程 AV、非调试附加行为**。
- 候选 core 完美候选（3650ea6c）本身健康证据：磁盘静态 Run 明文、导出解析与 T0.4 一致、加载事件正常派发（LOAD_DLL 0.101s）。
- 泵机制（T018 全保留）健康：2/2 趟全消费无冻结。
- 建议下一步（不烧本单格）：总指挥决策重脱壳宿主（另立单）或提供宿主绑定 boot 环境后复跑本脚本（脚本已就绪、动态解析已就位）。
