# TASK-016 执行报告 — B1' 能力固化 + 硬编码审计清除 + 复现 runbook（纯离线，零实弹）

> worker · 2026-08-30 · 岗位 developer（纯离线：审计 + 行为中性硬编码清除 + preflight 预检；零实弹）· 账本 XC-XXI-B 9/4（不变）
> 起点核验：HEAD = `2d0f5d2`（TASK-016 起草/授权提交）；`3de2ade`（T015 验收）为 HEAD 祖先 ✅；`3de2ade..HEAD` diff 仅 docs/tickets 文件 ✅

## 0. 授权令牌（原文回抄）

> `老板 · 2026-08-30 · 原话"现在你派个单做个阶段收尾，别以后用不能用了。还有要注意通用性，不要有硬编码"（= 批准阶段收尾工单：授权 crates/ 改动仅限本单授权文件清单，行为中性硬编码清除 + preflight 预检；纯离线零实弹，账本不变）· 前置由总指挥亲验：起点 HEAD = 3de2ade（TASK-015 已验收入栈；worker 须核验 3de2ade 为 HEAD 祖先、且 3de2ade..HEAD 的 diff 仅 docs/tickets 文件）· vault 受控 ini sha256 c88e94c3… 与 `scylla_hide_no_excdispatch.ini` 已当场复核在位（preflight 检查的目标物）`

---

## 1. 改动文件（对照授权清单）

```
$ git diff --name-only HEAD
crates/acceptance/src/preflight.rs              （授权：新增 readiness 检查 + 测试）
crates/packers/themida/src/antiantidebug/scyllahide.rs  （授权：硬编码派生化/命名常量）
crates/packers/themida/src/trace_imports/mod.rs （授权：同上）
crates/packers/themida/src/trace_imports/slot.rs（授权：同上）
crates/pe/src/dumper/dump_process.rs            （授权：同上）
crates/pe/src/dumper/iat_gap_retarget.rs        （授权：同上）

$ git diff --stat HEAD
 crates/acceptance/src/preflight.rs                 | 327 +++++++++++++++++++++
 crates/packers/themida/src/antiantidebug/scyllahide.rs  | 118 +++++---
 crates/packers/themida/src/trace_imports/mod.rs    |  21 +-
 crates/packers/themida/src/trace_imports/slot.rs   |   6 +-
 crates/pe/src/dumper/dump_process.rs               |  45 ++-
 crates/pe/src/dumper/iat_gap_retarget.rs           |  36 ++-
 6 files changed, 487 insertions(+), 66 deletions(-)
```

- 全部 6 个改动文件均在授权清单内；无 `#[ignore]`/`.skip` 新增；无既有断言被放宽（grep 验证为空）。
- `iat_partial_accept.rs`/`import_rebuild.rs`/`iat_completeness.rs` 审计后**零改动**（生产代码已无裸魔法数）。
- 新增文档（授权交付物）：`docs/HARDCODING_AUDIT_T016.md`、`docs/RUNBOOK_REV2_B1.md`（均未提交）。
- vault 新增：`D:/MidaVault/lab/evidence/xx21b_t015/INDEX.md`（34 件清单 + sha256）。

---

## 2. 硬编码审计报告（摘要；全文见 docs/HARDCODING_AUDIT_T016.md）

**结论** [已验证]：T014/T015 快速修复遗留的 `0x1136e0`/`0x1137d0`/`0x1681d1`/201/192/74 等魔法数**全部只在测试夹具与注释里，不进入生产控制流**；生产计算全从 PE 结构/运行时状态/配置得出。

**本次清除（行为中性，仅同值改名 + 注释 + 一处去重）**：

| 文件 | 处置 |
|---|---|
| `trace_imports/mod.rs` | `0x10000`×3 → `MIN_USER_MODE_ADDRESS`；`EFlags\|=0x100` → `X86_EFLAGS_TRAP_FLAG` |
| `trace_imports/slot.rs` | `0x100`×2 → `X86_EFLAGS_TRAP_FLAG`（经 super 导入） |
| `scyllahide.rs` | 新增 12 个 const（staging 目录/证据前缀、ini/注入器/钩子文件名、nowait 参数）；**非 staging 路径 38 行重复哈希校验去重**为 `verify_helper_hashes()` 调用（错误字符串逐字保留） |
| `iat_gap_retarget.rs` | 邻域窗口 `1..64`/`i+24`、MessageBoxW uType `0x0A/0x0E`、SendMessageW imm 列表 → 4 组具名 const |
| `dump_process.rs` | `0x1000` PE 头、`0x100_000` live .text 上限、`0x100000` 巨节日志阈值×2、`0x10000` cmd 计数上限 → 5 组具名 const |
| `preflight.rs` | 新增 ScyllaHide readiness 模块（见 §4） |

**命名常量清单与适用边界**：见 `docs/HARDCODING_AUDIT_T016.md` §3（17 个常量逐一列出值/文件/边界）。

**报告-only 只读发现清单（不改，改动需老板另行授权）**：见 `docs/HARDCODING_AUDIT_T016.md` §5。要点：
- `cli/src/unpacker/mod.rs` text-poll/C-7 段：`+0x10` 双区域采样、prologue 特征字节 `{53,55,56,57}`/`41 54..57`/`48 83|81|8B`、classic8 字节串 —— 壳行为启发式（XX-11-B #17），进控制流，清除需实弹再验证 → STOP 请示（本单不做）。
- `cli/src/unpacker/mod.rs:1515` `Duration::from_secs(12)` grace window —— 时序启发式，进控制流。
- `args.rs:416` `wait_sec=60`、`oep_scan.rs` `WRAPPER_LEN=18`/`CHUNK_OVERLAP=32`/`MAX_WEAK=32` —— 静态扫描启发式（已 const，值源自样品观测）。
- `data_reinit.rs`/`windows_debugger.rs`/`av_handler.rs`/`av_oep_handler.rs` —— 已达标（T0.7 已清 / 阈值已 const 1024）。
- 测试夹具中的 vault 路径字面量（`helpers.rs:451`、`antidebug_controller.rs:2033`）—— 测试文件，gate 扫描跳过。

**扫描证据**：
```
$ python tools/_hardcode_scan.py
===== long_hex: 22 =====   （全部为合法常量：PE 特征/算法常量/签名/通用示例地址，与 T0.10 基线一致）
TOTAL: 22
```
（sample_hex/win_path/vault_path/high_aslr 生产代码 0 命中，与审计前一致——本单清除的是更短的字面量，扫描器不覆盖，逐行人工审。）

---

## 3. 验收标准逐条对照

### 标准 1：`git diff --stat` 只含授权文件；无 `#[ignore]`/`.skip`/既有断言被放宽

命令：`git diff --name-only HEAD` + `git diff HEAD | grep -nE "#\[ignore\]|\.skip\("`
原始输出：见 §1（6 文件全授权）；grep 无输出（无 ignore/skip）。
结论：**PASS**

### 标准 2：`cargo test --workspace --offline` → 真退出码 0，各套件计数 ≥ 基线

命令：`cargo test --workspace --offline`（经 `tools/_enter_msvc_env.cmd`，`_run_final.cmd`）
原始输出（关键行，含 EXIT）：
```
test result: ok. 1054 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out   (mida-pe lib)
test result: ok. 176 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out   (mida-packers-themida lib)
test result: ok. 580 passed; 0 failed; 1 ignored; 0 measured; 0 filtered out   (mida-cli lib)
test result: ok. 263 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out   (mida-acceptance lib, 256+7)
EXIT=0
```
基线对照：pe 1054 ✓（=）· themida 176 ✓（=）· cli 580 ✓（=）· acceptance lib 256 → **263（+7 新增 scylla 用例）**，只增不减。
结论：**PASS**

### 标准 3：硬编码审计报告（逐发现分类 + 修复 diff 对照 + 只读文件发现清单 + 常量清单）

交付：`docs/HARDCODING_AUDIT_T016.md`（§2 摘要在本报告 §2）。
结论：**PASS**

### 标准 4：preflight 判别力（临时移除任一新增检查 → 新用例变红 → 字节级恢复；编译失败不算红）

**探针**：临时禁用 `check_scylla_hide_ini_content` 的 missing-key 分支（注释掉 `reasons.push`）。
命令：`cargo test -p mida-acceptance --lib --offline scylla_hide_missing_key_fails_loud`
原始输出（探针期，非 0 真退出码）：
```
running 1 test
test preflight::tests::scylla_hide_missing_key_fails_loud ... FAILED
---- preflight::tests::scylla_hide_missing_key_fails_loud stdout ----
thread 'preflight::tests::scylla_hide_missing_key_fails_loud' (5896) panicked at crates\acceptance\src\preflight.rs:1755:9:
assertion failed: !verdict.ok
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 262 filtered out
error: test failed, to rerun pass `-p mida-acceptance --lib`
EXIT=101
```
（真 assertion 失败 + exit 101，非编译错误。）

**恢复**：`cp /tmp/preflight_backup_T016.rs → preflight.rs`（字节级恢复，备份已删）。
命令：`cargo test -p mida-acceptance --lib --offline scylla_hide`
原始输出（恢复后 7/7 绿）：
```
running 7 tests
test preflight::tests::scylla_hide_missing_key_fails_loud ... ok
test preflight::tests::scylla_hide_key_set_to_one_fails_loud ... ok
test preflight::tests::scylla_hide_parser_ignores_comments_and_empty_lines ... ok
test preflight::tests::scylla_hide_duplicate_key_is_ambiguous_fail ... ok
test preflight::tests::scylla_hide_good_ini_passes ... ok
test preflight::tests::scylla_hide_helper_missing_files_fail_loud ... ok
test preflight::tests::scylla_hide_readiness_full_check_ok_and_fail ... ok
test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 256 filtered out
EXIT=0
```
恢复后 `git diff --stat crates/acceptance/src/preflight.rs` = `327 ++++++++++`（与探针前一致）。
结论：**PASS**

### 标准 5：runbook 一致性（离线步骤实际执行 + 与文档判据逐条核对）

`docs/RUNBOOK_REV2_B1.md` 的离线步骤 = §1 构建 + §6 preflight 测试。实际执行：

命令：`cargo build --release -p mida-cli --offline`（经 MSVC env）
原始输出：`Finished \`release\` profile [optimized] ... EXIT=0`（25.61s）

五条字符串 HIT 核对（文档 §1 判据，T015 §5 断言）：
```
zero-filled IAT region = 1
TASK-009 fail-closed = 1
guardless constant-AV storm abort = 1
C-7: guardless constant-AV storm = 1
SCYLLAHIDE_HOOK_CONFIG_SOURCE = 3
```
（`SCYLLAHIDE_HOOK_CONFIG_SOURCE` = 3 处，≥1 满足判据）

命令：`cargo test -p mida-acceptance --lib --offline scylla_hide`（runbook §6）
原始输出：`7 passed / 0 failed / EXIT=0`（见标准 4 恢复段）

**与文档逐条核对**：§0 前置条件（ini 在位 sha256=c88e94c3… ✅、样品 sha 匹配 ✅）；§1 构建判据（EXIT=0 + 五字符串全 HIT ✅）；§6 preflight 判据（7 passed ✅）。§2/§3/§4 为实弹步骤，本单**不执行 `/unpack`**（红线），判据数字引自 T015 实弹日志（[已验证] 标注）。
结论：**PASS**

### 标准 6：clippy + fmt → 0

命令：`cargo clippy --workspace --lib --bins --offline -- -D clippy::unwrap_used -D clippy::expect_used -D clippy::manual_let_else`
原始输出：`CLIPPY_EXIT=0`（无 `error[` 行；既有 warn 为基线，非本单新增）
命令：`cargo fmt --all -- --check`
原始输出：`FMT_EXIT=0`
结论：**PASS**

### 标准 7：零实弹自证

命令：`tasklist | grep -iE "mida-cli|rev2|InjectorCLI|scylla|debuggee|xiongxiong|cdb"`
原始输出：`NO_MATCHING_PROCESSES`（grep 无命中）
- 全程未启动 debuggee、未注入、未跑 `/unpack`、未写 `C:\Windows`。
- 临时文件逐个删除：`_run_baseline.cmd`/`_run_build.cmd`/`_run_test.cmd`/`_run_probe.cmd`/`_run_final.cmd`/`_run_clippy.cmd`/`_run_preflight.cmd` 全部 `rm -f`；`/tmp/preflight_backup_T016.rs` 已删；无 `mida-scyllahide-*` staging 残留（StagingGuard 本就未触发）。
结论：**PASS**

### 标准 8：INDEX.md 存在且 sha256 与实物一致（抽 3 个文件复算）

命令：`sha256sum <xx21b_t015 中 3 件>`
原始输出：
```
attempt1.log:  91946bbe14683babb1bd1dced677dc63c856ba485a839b87da8730231289f35d  （INDEX 一致 ✅）
core.dll:      09f3dd344215c6aa608bc6a8e8ae24486e3bf425c3f3541272d065a1d9999144  （INDEX 一致 ✅）
structural_attempt1.json: 71b7c950d01341caadd625fbf9fa951b4a944a08c5b9c3ff9041bff0534fd9e9  （INDEX 一致 ✅）
```
结论：**PASS**

---

## 4. preflight ScyllaHide readiness（新能力说明）

`crates/acceptance/src/preflight.rs` 新增（全部 pub，纯离线可测）：
- `parse_ini_sections(&str) -> BTreeMap<"section.key", value>`：INI 解析（`[Section]`/`Key=Value`/`;` 注释，忽略无段键）
- `check_scylla_hide_ini_content(&str) -> ScyllaHideReadiness`：三零键 `KiUserExceptionDispatcherHook=0`/`NtContinueHook=0`/`NtCloseHook=0` 必须显式在列且=0；缺失→"受控 ini 不完整，将撞 AV 风暴环"（fail-loud），非零→"expected 0"，多段→"ambiguous"
- `check_scylla_hide_helpers(&Path) -> ScyllaHideReadiness`：`scylla_hide.ini`/`InjectorCLIx64.exe`/`HookLibraryx64.dll` 三件套在位
- `check_scylla_hide_readiness(ini, helper_dir) -> ScyllaHideReadiness`：组合入口

**红线遵守**：只查结构，**不写死 ini 的 sha256**（内容可演进）；检查不接进 `run_offline_preflight`（涉及 main.rs 等清单外文件 → 留待授权工单，本单不越界）。

---

## 5. 阻塞点 / STOP 请示情况

- 无阻塞。
- 报告-only 文件发现的 8 处壳行为/时序启发式硬编码（§2），均需**行为性改动**才能清除 → 按工单红线 **STOP 请示**：改动需老板另行授权，属下一战役（需实弹再验证）。本单未触碰。

---

## 6. 我没做的事 / 我不确定的事

1. **preflight readiness 未接进 `run_offline_preflight`**：`PreflightRequest` 加字段会强制改 `main.rs`（清单外）→ 本单只交付独立可测 API + 7 单测，生产接线留待授权。**[推断]**
2. **`scylla_hide.ini` 内容指纹**：按工单明确要求不校验 sha256（内容可能演进）；若未来需要"与 T013 基线逐位一致"的强门，需另立工单。**[存疑]**
3. **`trace_imports` 的 `0x10000` floor 语义**：我把它命名为"Windows 用户/内核地址约定"；严格说这是 64KiB 边界（64KB 以下无用户态映射），非样品锚点。若某壳把 wrapper 放在 0x10000 以下，此判定会把它当"已解析 API"跳过——这是**既有行为**，未改语义，仅改名。**[存疑]**
4. **`dump_process.rs` 的 `0x100000` 巨节日志阈值**：仅日志不参与控制流，改名不影响行为；但其值本身是启发式（T015 之前定的），未做校准。**[存疑]**
5. **runbook 的实弹判据数字（75/74/186/12/10）**：来自 T015 2/2 次实弹，本单未跑 `/unpack`（零实弹红线），未独立重验这些数字；跨 boot 家族内确定性由 T015 §7 对照表支撑。**[已验证 T015 引用 / 本单未复跑]**
6. **INDEX.md 全 34 件 sha256 只抽了 3 件复算**：按验收标准 8 抽 3 件；其余 31 件 sha 由 T015 时计算（`sha256sum` 输出一致性与文件列表核对过）。**[已验证 3/34]**

---

## 7. 收尾证明

- 工作区留改动给总指挥：6 个授权文件改动 + 2 个新文档（`docs/RUNBOOK_REV2_B1.md`、`docs/HARDCODING_AUDIT_T016.md`）+ vault `INDEX.md`；**未 commit/push**。
- 临时文件全部按名删除（§3 标准 7）。
- `tasklist` 无相关进程（§3 标准 7）。
- 全仓回归快照：**EXIT=0**（pe 1054 / themida 176 / cli 580 / acceptance lib 263），clippy/fmt 0，硬编码扫描基线不变（22 合法 long_hex，0 gate 命中）。

*worker · TASK-016 · 2026-08-30 · 纯离线零实弹 · git 只读 · 无新增依赖 · 6 授权文件 +487/-66 · preflight 判别力证明 PASS（红→字节级恢复→绿）· runbook 一致性 PASS（构建 + 五字符串 HIT + preflight 7 用例）· 全仓 EXIT=0 · 账本 XC-XXI-B 9/4 不变*

---

# TASK-016R1 补正

> 本增补段由 TASK-016R1 执行（2026-08-30，微型补正轮，纯离线零实弹）。
> **授权令牌（第一节回抄）**：本单在 D-030 范围内、无新令牌。回抄 `tickets/TASK-016.md` 第一行 D-030 令牌原文如下：
> > `老板 · 2026-08-30 · 原话"现在你派个单做个阶段收尾，别以后用不能用了。还有要注意通用性，不要有硬编码"（= 批准阶段收尾工单：授权 crates/ 改动仅限本单授权文件清单，行为中性硬编码清除 + preflight 预检；纯离线零实弹，账本不变）`
> 引用本票号：**TASK-016R1**（总指挥 D-031 条件通过后派发，D-030 范围内零实弹不耗格）。

## 改动文件（对照授权清单）

本补正轮只做 2 处：`crates/pe/src/dumper/iat_gap_retarget.rs`（F1 行为还原）+ `docs/HARDCODING_AUDIT_T016.md`（F2 审计完整性增补）。
工作区其余 TASK-016 未提交改动（6 crates 文件 + 新文档）原样保留，未还原未提交。

## 验收标准逐条对照

### 标准 1：git diff 相对补正前只含 2 文件；delta 循环还原后与 HEAD（2d0f5d2）语义逐位一致

命令：`git diff --stat`（相对 HEAD）+ 语义对照
原始输出：
```
$ git diff --stat
 crates/acceptance/src/preflight.rs                 | 327 +++
 crates/packers/themida/src/antiantidebug/scyllahide.rs        | 118 +--
 crates/packers/themida/src/trace_imports/mod.rs    |  21 +-
 crates/packers/themida/src/trace_imports/slot.rs   |   6 +-
 crates/pe/src/dumper/dump_process.rs               |  45 +-
 crates/pe/src/dumper/iat_gap_retarget.rs           |  39 +-
 6 files changed, 490 insertions(+), 66 deletions(-)
```
- R1 新增改动仅落在 2 个文件：`iat_gap_retarget.rs`（const 64→63 + 注释如实化）与 `docs/HARDCODING_AUDIT_T016.md`（F2 增补）。[已验证：逐文件 diff 对照，见下]
- delta 循环语义还原：`for delta in 1..=GAP_NAME_NEIGHBOR_MAX_DELTA`（const=63）与 HEAD 版 `for delta in 1..64u32` 同为 delta 1..63（63 值，max=63）。[已验证：range(1,64)==[1..63]]
结论：**PASS**

### 标准 2：mida-pe 1054 / mida-acceptance 263，exit 0

命令：`cargo test -p mida-pe --lib --offline` + `cargo test -p mida-acceptance --lib --offline`（经 `tools/_enter_msvc_env.cmd`）
原始输出：
```
test result: ok. 1054 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 9.28s
PEEXIT=0
test result: ok. 263 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.20s
ACCEXIT=0
```
结论：**PASS**

### 标准 3：clippy/fmt 0；check_clippy_baseline.ps1 = 既有失败（原样记录，不改任何门文件）

命令：`cargo clippy --workspace --lib --bins --offline -- -D clippy::unwrap_used -D clippy::expect_used -D clippy::manual_let_else`、`cargo fmt --all -- --check`、`powershell -ExecutionPolicy Bypass -File tools/check_clippy_baseline.ps1`
原始输出：
```
CLIPPYEXIT=0
FMTEXIT=0
[msvc-env] toolset : ... MSVC.44.35207
[msvc-env] sdk     : 10.0.26100.0
Running cargo clippy --workspace --lib --bins (JSON)...
FAIL: clippy warn baseline exceeded.
  over baseline: clippy::too_many_arguments: baseline=61 got=62
BASELINEEXIT=1
```
- baseline FAIL 与 D-031 F3 记录完全一致（`too_many_arguments` 61→62，0 条由 T016/R1 引入，HEAD 上同为 62）——原样记录，**未改** `_clippy_baseline` / `check_clippy_baseline.ps1` / 任何门文件，未为过门重构任何函数。
结论：**PASS**（含既有失败如实记录）

### 标准 4：报告增补段 ≤1 页附 runs 末尾 + 第一节回抄令牌

本增补段即本段；第一节已回抄 D-030 令牌原文并引用本票号。结论：**PASS**

### 标准 5：零实弹自证 / git 只读 / 临时文件删除

命令：`tasklist` 过滤 + workspace 临时文件检查
原始输出：
```
$ tasklist | grep -icE "injectorcli|hooklibrary|xx21|mida|themida"
0（0 匹配 = 无相关进程）
$ ls _run*.cmd
（无输出 = 临时批处理已删）
```
- git 只读：全程未 commit / push（git status 仍为工作区改动，HEAD 未动）。[已验证]
- 临时文件：`_run_r1.cmd`、`_audit_utf8.txt`、`/tmp/r1diff/`、`/tmp/r1_*.txt` 均已删除。[已验证]
结论：**PASS**

## 阻塞点

无。

## 我没做的事 / 我不确定的事

- **未做**：未改 GTO-UI 区任何生产代码（`dump_process.rs` 等一行未动）；未改 `_clippy_baseline` / `check_clippy_baseline.ps1` 等门文件；未为过门重构任何函数；未 commit / push；未跑 `/unpack`（零实弹）。
- **F1 注释如实化**：const doc 注释改写为"63 reproduces the original 1..64u32 search window exactly"，如实反映还原语义（可信度 [已验证]，diff 对照 + 语义等价计算）。
- **F2 勘误**：顺带在审计文档 §八 把计数笔误 +482 改为 +487（D-031 审计备注①要求）；授权清单路径误写勘误（D-031 备注②）已按票面补一行。
- **`git diff --stat` 显示 +490/-66**（T016 报告记为 +487/-66）：差异来自 R1 对 `iat_gap_retarget.rs` const 注释的改写（新增 3 行注释），非行为性改动；若总指挥要求"相对补正前 diff 严格 2 文件"，则 R1 的注释改写属 `iat_gap_retarget.rs` 文件内的既定增补（该文件本就在授权清单内），未越界。[已验证]
- **全仓回归快照**：`cargo test --workspace --offline` → 65 target 全 ok，WSEXIT=0（1054+263 等计数不变）。[已验证]
