# TASK-006R4 执行报告 — 带受控 ini 重跑重脱壳（落位方案缺陷实锤，STOP 上报）

- 执行日期：2026-08-30（本地 03:40–04:05）
- 工单：TASK-006R4
- 岗位：developer（实弹执行，单 worker）
- 账本：XC-XXI-B **5/4 → 6/4**（老板 2026-08-30 明确扩额授权）
- **终态判定：未达任何有效尝试** —— 落位方案存在根因性缺陷（InjectorCLI 不读 C:\Windows），实弹 attempt1 因 ini 未生效**不算有效尝试**，按停止规则 STOP 上报。**本单核心产出 = 决定性实证：TASK-013 的"只搜 Windows 目录"结论对 InjectorCLI 实际行为错误**
- 红线：NO_BYPASS=1 全程；样品身份核验 PASS；样品未外发；git 只读（未 commit/push/stash、未改 config、未改 crates/ 与 lab/cases/v2/*.json）；C:\Windows 只写 scylla_hide.ini 一个文件且已删；探针环境（C:\Temp\scyllaprobe、C:\Temp\r4_probe*.ps1）已逐名删除；结论按 [已验证]/[推断] 标注；只贴原始输出

---

## 〇、授权令牌原文回抄（必须）

> ✅ **已授权 —— 授权令牌（必须在报告第一节原文回抄）**：
> `老板 · 2026-08-30 · 批 1 格实弹 · XC-XXI-B 5/4 → 6/4（超原配额，老板明确扩额）· 同时放行 C:\Windows 落位（仅限 scylla_hide.ini 一个文件，跑完必删、删后贴证明）· 前置由总指挥亲验：BootTime = 2026-08-30 01:28:40（与 TASK-006R3 同 boot，无需重启——本单实验变量是受控 ini，不是 boot；R3 已实证环与 boot 无关）· 起点 HEAD = ca06117（TASK-013 已验收入栈）`

---

## 一、前置原始输出

### 1. HEAD 与祖先关系（构建核验）

命令：
```bash
git log --oneline -1
```
原始输出：
```
e8bda46 docs: final corrected attribution for the xiongxiong EXE line — boss's memory was right, injection WAS on during the successful xx11 run
```
命令：
```bash
git merge-base --is-ancestor ca06117 HEAD && echo "ca06117 IS ancestor of HEAD"
```
原始输出：
```
ca06117 IS ancestor of HEAD
```
`git diff --stat ca06117..HEAD` = 仅 6 个 docs/tickets 文件（00-START-HERE / DECISIONS / KNOWN_ISSUES / PROJECT_STATUS / TICKETS / TASK-006R4），crates/ 零改动 ✅

### 2. 全新 release 构建（crates/ 自上一格已变，禁止复用旧 exe）

命令（MSVC 环境入口 `tools/_enter_msvc_env.cmd`）：
```bash
cargo build --release --offline -p mida-cli
```
原始输出（尾部）：
```
[msvc-env] toolset : C:\Program Files\Microsoft Visual Studio\2022\Professional\VC\Tools\MSVC\14.44.35207
[msvc-env] sdk     : 10.0.26100.0
[msvc-env] linker  : ...\link.exe
   Compiling mida-cli v0.1.0 (...)
    Finished `release` profile [optimized] target(s) in 21.22s
BUILD_EXIT=0
```

exe 信息：
```
-rwxr-xr-x 2 Administrator 197121 5799424 Aug 30 03:40 target/release/mida-cli.exe
2dd75d91b1da184c1bf90d04fc256faee375fe1ee03e9bff9cb1c84399adcd24 *target/release/mida-cli.exe
```
体积 5799424 B ≠ 上一格 5795328 B（crates/ 确已变化 → 全新构建 ✅）

### 3. 五条字符串命中（binary grep，贴计数）

命令：
```bash
for s in "zero-filled IAT region" "TASK-009 fail-closed" "guardless constant-AV storm abort" "C-7: guardless constant-AV storm" "SCYLLAHIDE_HOOK_CONFIG_SOURCE"; do grep -c -a -F "$s" target/release/mida-cli.exe; done
```
原始输出：
```
[1] zero-filled IAT region
[1] TASK-009 fail-closed
[1] guardless constant-AV storm abort
[1] C-7: guardless constant-AV storm
[2] SCYLLAHIDE_HOOK_CONFIG_SOURCE
```
五条全部命中 ✅

### 4. 样品身份核验（vault 对象路径，P-6）

命令：
```bash
sha256sum D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
```
原始输出：
```
7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 *D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
```
manifest（lab/cases/v2/xiongxiong_duokai.json）：
```
primary_artifact_sha256 = 7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
 artifacts[0]: 7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 (protected_input)
```
**匹配 → PASS**（未触碰 `D:\Tools\RE\dumps\gto\启动器.exe` 定位符）[已验证]

### 5. ini 落位（本单新增，系统级副作用）

**a. 落位前确认 C:\Windows 无 scylla 残留**（命令 + 原始输出）：
```
$ ls /c/Windows/*.ini
/c/Windows/system.ini
/c/Windows/win.ini
$ ls /c/Windows/scylla*.ini
(exit=2, 无)
```
干净 ✅

**b. 落位 + sha256 比对**：
```
$ cp D:/MidaVault/lab/config/scylla_hide_no_excdispatch.ini /c/Windows/scylla_hide.ini
$ sha256sum D:/MidaVault/lab/config/scylla_hide_no_excdispatch.ini /c/Windows/scylla_hide.ini
c88e94c38b8edf36f438449dbd0b62f2967affbf1c6229392cabb7b30be46b5c *D:/MidaVault/lab/config/scylla_hide_no_excdispatch.ini
c88e94c38b8edf36f438449dbd0b62f2967affbf1c6229392cabb7b30be46b5c */c/Windows/scylla_hide.ini
```
两份一致 → 落位步骤完成 ✅

**c. 设环境变量**：`MIDA_SCYLLA_HIDE_INI=D:/MidaVault/lab/config/scylla_hide_no_excdispatch.ini`

---

## 二、会话基线

| 项 | 值 |
|---|---|
| BootTime | **2026-08-30 01:28:40**（与令牌一致，与 R3 同 boot） |
| ntdll.dll | **0x7ff857620000** |
| KERNEL32.DLL | **0x7ff855350000** |
| KERNELBASE.dll | **0x7ff854bb0000** |
| debuggee image_base（attempt1） | **0x7ff729430000** |
| ScyllaHide 注入日志 | `target/release/scylla_hide.log`（尝试前重置；vault 副本 `scylla_hide_livefire_attempt1.log`） |

基线落盘：`D:/MidaVault/lab/evidence/xx21b_006r4/session_baseline_006r4.txt`

---

## 三、实弹执行（1 次尝试，因 ini 未生效不算有效尝试 → STOP）

**执行命令**（与 R2/R3 一致）：
```bash
MSYS_NO_PATHCONV=1 NO_BYPASS=1 MIDA_GTO_NO_BYPASS=1 MIDA_LEGACY_ANTIDEBUG=1 \
  MIDA_SCYLLA_HIDE_INI=D:/MidaVault/lab/config/scylla_hide_no_excdispatch.ini CARGO_INCREMENTAL=0 \
  timeout 300 target/release/mida-cli.exe /unpack \
  D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 \
  --profile=oreans-classic --container-restore=off \
  -o lab/xx21b_006r4/rev2_unpacked_006r4.exe > lab/xx21b_006r4/attempt1.log 2>&1
```
EXIT=1，日志 312352 B，无产物。

**有效尝试判定（工单强门）—— attempt1 判定为【落位失败/ini 未生效】✗**：

- 引擎日志配置来源行（第 14 行）：
```
[2026-08-29T19:41:04.968026Z] [INFO] SCYLLAHIDE_HOOK_CONFIG_SOURCE=ini: D:/MidaVault/lab/config/scylla_hide_no_excdispatch.ini
```
（引擎记录的是 env 提供的 vault 路径 ✅ —— 但这是**引擎自己的记录**，不代表 InjectorCLI 读到）

- **本次 scylla_hide.log 仍出现**（= ini 没被读到 ❌）：
```
Hooking KiUserExceptionDispatcher
Hooking NtContinue
Hooking NtClose
```
（`grep -c` 各 = 1）

**按工单：「若 hооk 行仍在 → ini 没被读到（落位失败）→ 该次不算有效尝试，停下排查落位」。**

### 排查过程（停下排查落位 → 决定性实证）

**实证 1 — P/Invoke 探针（纯离线，验证 TASK-013 的 API 语义）**：
```
GetPrivateProfileSectionNamesW(sb, 4096, "scylla_hide.ini") = ret=23, names='SETTINGS'
```
→ 裸相对名确实能读到 C:\Windows\scylla_hide.ini 的 SETTINGS 段（TASK-013 的 API 语义层面正确）

**实证 2 — 反汇编（InjectorCLIx64.exe，sha256=b902d5ce…，与 target/release 同 sha）**：
```
0x14000ce76: call *0x14006f150   ; IAT = GetModuleFileNameW（拿自身 exe 完整路径）
0x14000cf9a: call *0x14006f158   ; IAT = GetPrivateProfileSectionNamesW, 文件名参数 = GetModuleFileNameW 结果
```
→ **InjectorCLI 用 GetModuleFileNameW 拼的路径读 ini，不是裸相对名**

**实证 3 — 受控 notepad 注入实验（C:\Temp\scyllaprobe\，目标为临时 notepad，注入后杀掉；不碰工作区，符合 ARTIFACT_POLICY）**：

| 实验 | exe 同目录有受控 ini？ | C:\Windows 有 ini？ | cwd | 结果（Hooking NtContinue / KiUserExc / NtClose 计数） |
|---|---|---|---|---|
| A | ✅ | ✅ | 同目录 | **0 / 0 / 0** → 受控 ini 生效 |
| B | ❌ | ✅ | 同目录 | **1 / 1 / 1** → C:\Windows 的 ini 完全不被读 |
| C | ✅ | ✅ | C:\Temp（无 ini） | **0 / 0 / 0** → 读 exe 同目录，与 cwd 无关 |

实验 B 原始输出（关键）：
```
=== scylla_hide.log (同目录无 ini, 但 C:\Windows\scylla_hide.ini 存在) ===
1   ← grep -c "Hooking NtContinue"
1   ← grep -c "Hooking KiUserExceptionDispatcher"
1   ← grep -c "Hooking NtClose"
```
实验 C 原始输出（关键）：
```
$ ls -la /c/Temp/scyllaprobe/scylla_hide.log /c/Temp/scylla_hide.log
-rw-r--r-- 1 Administrator 197121 4443 Aug 30 04:04 /c/Temp/scyllaprobe/scylla_hide.log   ← log 写 exe 目录
$ grep -c "Hooking NtContinue" /c/Temp/scyllaprobe/scylla_hide.log
0
$ grep -c "Hooking KiUserExceptionDispatcher" /c/Temp/scyllaprobe/scylla_hide.log
0
```

**结论 [已验证]：InjectorCLI 用 `GetModuleFileNameW` 拿自身 exe 路径，读 `<exe目录>/scylla_hide.ini`。不读 C:\Windows（即使文件存在），与 cwd 无关。TASK-013 的"只搜 Windows 目录"结论对裸相对名 API 语义正确、但对 InjectorCLI 实际行为错误。引擎 spawn `target/release/InjectorCLIx64.exe` → 它找 `target/release/scylla_hide.ini`（不存在）→ 全默认 hооk。**

### 停止规则判定（严格执行）

- **落位失败第 1 次**（attempt1）：hооk 行仍在。工单："连续 2 次落位失败即 STOP"。
- **本次不是操作失误，是方案根因缺陷**：再次落位 C:\Windows 必然同样失败（InjectorCLI 不读该目录，实证 B）。
- **授权范围内（不改 crates/、不把 scylla_hide.ini 放进工作区 target/）不存在有效路径**：
  - C:\Windows → 不读（实证 B）❌
  - target/release/（exe 旁）→ 会读（实证 A/C），但 ARTIFACT_POLICY 明确禁止工作区出现 scylla_hide.ini（含 target/）❌
  - cwd → 与 ini 读取无关（实证 C）❌
- **结论：有效尝试在授权内不可达成 → 立即 STOP 上报，不硬跑第 2 次实弹**（避免把无效运行当路径证据）。

---

## 四、终态判定

- **路径 A（fail-closed 生效）**：未到达。`TASK-009 fail-closed` 计数 0。
- **路径 B（产物写出）**：未到达。`[GOOD] Candidate written` 计数 0，无产物。
- **路径 C（又撞 C-7 环）**：attempt1 仍 C-7 中止（因 ini 未生效、全默认 hооk），但**该次不算有效尝试**（工单明示不许把 ini 未生效的运行当路径证据）。
- **路径 D（新形态）**：未出现。

三个 TASK-009 证据点计数（attempt1.log）：
```
zero-filled IAT region: 0
TASK-009 fail-closed:   0
[GOOD] Candidate written: 0
guardless constant-AV storm abort: 1  （C-7 中止）
```

attempt1 量化观测（仅供参考，非有效尝试）：
| 项 | 值 |
|---|---|
| AV 事件 | **1024**（恒同元组 `(exc=0x7ff857780bd8, target=0x292, exc_type=0, thread=23316)` ×1024） |
| exc 相对 ntdll 偏移 | **+0x160bd8**（= 0x7ff857780bd8 − 0x7ff857620000，与 R2/R3 完全相同） |
| C-7 中止 | 1（count=1024） |
| image_base | 0x7ff729430000 |
| 事件类型分布 | CreateProcess 1 / LoadDll 18 / AccessViolation 1024 |
| 日志体积 | 312352 B |

---

## 五、收尾（强制，已完成）

### 1. 删除 C:\Windows\scylla_hide.ini + 删后证明

命令 + 原始输出：
```
$ rm -f /c/Windows/scylla_hide.ini && echo deleted
deleted
$ ls /c/Windows/*.ini
/c/Windows/system.ini
/c/Windows/win.ini
$ ls /c/Windows/scylla*.ini
(exit=2, 无)
```
只剩系统自带文件 ✅

### 2. 日志留存（删除前计数已算好 → 有界摘要 + 本体进 vault）

计数（grep -c 原始命令）：
```
AV 总数: 1024
guardless constant-AV storm abort: 1
C-7: 1
TASK-009 zero-filled: 0
TASK-009 fail-closed: 0
[GOOD] Candidate written: 0
SCYLLAHIDE_HOOK_CONFIG_SOURCE: 1
事件类型分布: AccessViolation 1024 / CreateProcess 1 / LoadDll 18
恒同元组: (exc=0x7ff857780bd8, target=0x292, exc_type=0, thread=23316) x1024 (uniq -c 1 行)
```

vault 归档清单（`D:/MidaVault/lab/evidence/xx21b_006r4/`）：
```
attempt1.log                          312352 B  重脱壳完整日志（本体）
attempt1_log_bounded_summary.txt       59452 B  有界摘要（头200+尾200）
scylla_hide_livefire_attempt1.log       3121 B  实弹 scylla_hide.log（ini 未生效证据）
probe_evidence_injectorcl_ini_path.txt  2905 B  决定性探针证据（A/B/C 三实验）
session_baseline_006r4.txt               120 B  会话基线
```
（均 <50MB；scylla_hide.log 已留档进 vault，未只留 target/release）

### 3. 探针环境清理（逐个按名删除）
- `C:\Temp\scyllaprobe\`（InjectorCLI + HookLibrary + 受控 ini + log）→ 已删
- `C:\Temp\r4_probe.ps1`、`C:\Temp\r4_probe2.ps1` → 已删
- 注入目标 notepad（PID 10072 / 11200 / 21452 / 24024 残留）→ 已全部 taskkill
- 全系统复查：无 xiong/rev2/mida/unpack/scylla 进程，无产物文件 ✅

---

## 六、改动文件（对照授权清单）

- **无**。未改 crates/、lab/cases/v2/*.json，未提交、未推送、未 stash、未改 git config。
- 新增 untracked：`lab/xx21b_006r4/`（本单工作目录，按 ARTIFACT_POLICY 不进 Git）。
- vault 新增：`D:/MidaVault/lab/evidence/xx21b_006r4/`（见 §五清单）。
- 既有 untracked（`.workbuddy-ai/`、`tools/xx21_msvc_env.cmd`、`tools/xx21_step1_static*.json`）未触碰。

---

## 七、结论摘要

1. **本单核心发现（决定性）**：**TASK-013 的"InjectorCLI 只搜 Windows 目录"结论错误**。InjectorCLI 实际用 `GetModuleFileNameW` 拿自身 exe 路径、读 **`<exe目录>/scylla_hide.ini`**（实证 A/B/C 三连）。`C:\Windows\scylla_hide.ini` 落位方案**结构上无法让受控 ini 生效**。[已验证]
2. **实弹 attempt1**：ini 未生效（全默认 hооk → C-7 风暴，exc 偏移仍 +0x160bd8），**不算有效尝试**，未达任何路径终态。[已验证]
3. **授权范围内无有效路径**：不改 crates/、不把 scylla_hide.ini 放工作区的前提下，InjectorCLI 永远读不到受控 ini → 有效尝试不可达成 → STOP 上报。[已验证]
4. **账本**：XC-XXI-B 5/4 → **6/4**（本格按 1 格记账，授权令牌已回抄）。

---

## 八、我没做的事 / 我不确定的事

1. **有效尝试 0 次达成**：本单未产生任何有效实弹数据。attempt1 因 ini 未生效被判无效（工单强门），且已实锤继续跑 C:\Windows 方案必然无效，故未跑第 2 次实弹（避免把无效运行当路径证据）。[已验证]
2. **TASK-013 结论错误的根因**：TASK-013 的 P/Invoke 探针测的是"裸相对名"传给 `GetPrivateProfileStringW/GetPrivateProfileSectionNamesW` 的 API 语义（Windows 目录搜索），但 InjectorCLI 实际传的是 `GetModuleFileNameW` 拼的 exe 同目录绝对路径。API 语义结论正确、对 InjectorCLI 实际行为不适用。[已验证；TASK-013 报告 1.1 段标注的"只搜 Windows 目录"需修正]
3. **后续修复方向（另立单，不在本单范围）**：
   - 授权扩展 `crates/packers/themida/src/antiantidebug/scyllahide.rs`：spawn 前把 vault ini 落位到 `exe_dir()/scylla_hide.ini`（或改 InjectorCLI 查找逻辑）。注意：InjectorCLI 的 `GetPrivateProfileStringW` 用的是 exe 同目录绝对路径，落位到 `target/release/` 即可生效（实证 A/C）。
   - 或授权在工作区外（如 C:\Temp\scyllaprobe 模式）维持一套 exe+ini 副本并让引擎指向它（需改 `helpers.rs` 的 `scylla_injector_path()`，同样需授权）。
4. **实弹引擎记录 `SCYLLAHIDE_HOOK_CONFIG_SOURCE=ini: <vault路径>` 行**：这是引擎对 env 的忠实记录（TASK-013 新增），**不代表 InjectorCLI 实际读到 ini**。本单证明二者可能脱节——配置来源记录 ≠ 配置生效。建议后续工单把"生效验证"纳入判定。[已验证（指脱节这一事实）]
5. **注入探针实验的副作用范围**：实证 A/B/C 注入目标为临时 notepad（PID 10072/11200/21452），HookLibrary 短暂注入后随进程 kill 卸载；notepad 是系统自带无害进程，无数据/无网络/无外发。已逐一 taskkill 并复查无残留。[已验证]
6. **C-7 环仍未触及**：本单未产生任何"ini 生效后的实弹"，因此"关掉异常分发链能否让 text-poll 收敛"这一问题**仍未回答**（需先解决 ini 生效路径）。[存疑]

---

*worker · TASK-006R4 · 2026-08-30 · NO_BYPASS=1 · 样品身份核验 PASS（78009803…）· git 只读（未 commit/push/stash、未改 config、未改 crates/ 与 lab/cases/v2/*.json）· C:\Windows 落位已删并贴证明 · 1 次实弹尝试因 ini 未生效不算有效 · 决定性探针（3 实验）已留档 vault · 按停止规则 STOP 上报，未硬跑第 2 次*
