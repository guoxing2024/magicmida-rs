# TASK-006R3 执行报告 — 重启后再验缺陷 A（路径 A/B 实弹替换验证）

- 执行日期：2026-08-30（本地 01:28–01:36）
- 工单：TASK-006R3
- 岗位：developer（实弹执行，单 worker）
- 账本：XC-XXI-B **4/4 → 5/4**（老板 2026-08-30 明确扩额授权）
- **终态判定：路径 C**（2/2 次，C-7 风暴终止生效）—— 缺陷 A 的三个 TASK-009 证据点仍 0 命中，**验证点仍不可达**
- 红线：NO_BYPASS=1 全程；样品身份核验 PASS 后执行；样品未外发；git 只读（未 commit/push/stash、未改 config、未改 crates/ 与 lab/cases/v2/*.json）；结论按 [已验证] 标注；只贴原始输出

---

## 〇、授权令牌原文回抄（必须）

> ✅ **已授权 —— 授权令牌（必须在报告第一节原文回抄本行）**：
> `老板 · 2026-08-30 · 批 1 格实弹 · XC-XXI-B 4/4 → 5/4（超原配额，老板明确扩额）· 前置"机器已重启"由总指挥亲验：BootTime = 2026-08-30 00:54:51（≠ 上一 boot 2026-08-29 07:58:23）`

---

## 一、前置三项原始输出

### 1. 机器已重启（BootTime 复核）

命令：
```bash
powershell -NoProfile -Command "(Get-CimInstance Win32_OperatingSystem).LastBootUpTime.ToString('yyyy-MM-dd HH:mm:ss')"
```
原始输出：
```
2026-08-30 01:28:40
```
**结论**：本会话实测 BootTime = `2026-08-30 01:28:40`，**与令牌中的 `00:54:51` 不一致** —— 说明令牌签发后又重启过一次（总指挥亲验 00:54:51 之后机器再次重启）。按工单指示「照实记录后继续，不用 STOP」。该 boot 与 TASK-006R/006R2 的 boot（`2026-08-29 07:58:23`）不同，跨 boot 目标达成。[已验证]

### 2. 构建核验（省时提示路径：复用已核验 exe）

**a. HEAD 与祖先关系**

命令：
```bash
git log --oneline -1
```
原始输出：
```
1d0c9fe docs: G-6 closed — 1.4GB of untracked live-fire evidence relocated to the vault (D-017)
```
命令：
```bash
git merge-base --is-ancestor e5494c8 HEAD && echo "e5494c8 IS ancestor of HEAD: YES"
```
原始输出：
```
e5494c8 IS ancestor of HEAD: YES
```
HEAD = `1d0c9fe`，含 e5494c8（TASK-006R2 收尾提交）✅

**b. crates/ 零改动证明（①）**

命令：
```bash
git diff --stat 5064363..HEAD -- crates/ Cargo.toml Cargo.lock
```
原始输出：
```
（空）
```
DIFF_EXIT=0。`5064363..HEAD` 仅 3 个 commit，全为 docs（1d0c9fe / 25c7b6b / e5494c8），crates/ Cargo.toml Cargo.lock 零改动 ✅

**c. 复用 exe 的 sha256 复算（②）**

命令：
```bash
sha256sum D:/MidaVault/lab/evidence/xx21b_006r2/mida-cli_006r2_built.exe
```
原始输出：
```
0c407a9722d23ca2796479ffc3f32a553b77ba64d25e82c1d9c28de19dc12829 *D:/MidaVault/lab/evidence/xx21b_006r2/mida-cli_006r2_built.exe
```
- 体积：5795328 B
- 与工单提示的期望 sha256 `0c407a97…` 完全一致 ✅
- 另核验 target/release/mida-cli.exe 与复用 exe 完全同 sha256（`0c407a97…`），确认当前代码产物即此 exe ✅

**d. 四条字符串命中（③）**

命令与原始输出（对复用 exe 做 binary grep）：
```bash
zero-filled IAT region -> 1
TASK-009 fail-closed -> 1
guardless constant-AV storm abort -> 1
C-7: guardless constant-AV storm -> 1
```
四条全部命中（各 1 次）✅

**构建核验结论：PASS**（省时路径，未重新构建；工单明示可复用）。

### 3. 样品身份核验（vault 对象路径，D-015/P-6）

命令：
```bash
sha256sum D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
stat -c '%s bytes' D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
```
原始输出：
```
7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 *D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
5793808 bytes
```
manifest（lab/cases/v2/xiongxiong_duokai.json）：
```
primary_artifact_sha256 = 7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7
artifacts[0].sha256 = 7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7  (role=protected_input, size_bytes=5793808)
```
vault 对象 sha256 与 manifest primary_artifact_sha256 **完全一致** → **不触发 SampleIdentityMismatch，允许继续** ✅ [已验证]
（未触碰 `D:\Tools\RE\dumps\gto\启动器.exe` 定位符，按工单指示直接使用 vault 对象。）

---

## 二、会话基线

| 项 | 值 |
|---|---|
| BootTime | **2026-08-30 01:28:40**（令牌 00:54:51 后再次重启；上一 boot 07:58:23） |
| ntdll.dll | **0x7ff857620000**（上一 boot 0x7ffa952a0000） |
| KERNEL32.DLL | **0x7ff855350000**（上一 boot 0x7ffa94160000） |
| KERNELBASE.dll | **0x7ff854bb0000**（上一 boot 0x7ffa92b00000） |
| debuggee image_base | attempt1 = **0x7ff729430000**；attempt2 = **0x7ff729430000**（上一 boot 恒 0x7ff799fc0000） |
| ScyllaHide 注入日志 | `target/release/scylla_hide.log`（每次尝试前重置；vault 副本 `scylla_hide_006r3.log`） |

原始输出（powershell）：
```
2026-08-30 01:28:40
ntdll.dll        0x140704594657280   # = 0x7ff857620000
KERNEL32.DLL     0x140704558153728   # = 0x7ff855350000
KERNELBASE.dll   0x140704550158336   # = 0x7ff854bb0000
```

**跨 boot 关键对照**：换 boot 后系统 DLL 全部换了 ASLR 基址、debuggee image_base 从 0x7ff799fc0000 变为 0x7ff729430000（新 boot 确定性分配）。但 ScyllaHide NtContinue-hook 故障环的**偏移不变**：exc = `0x7ff857780bd8` = ntdll + **0x160bd8**（上一 boot `0x7ffa95400bd8` = ntdll 0x7ffa952a0000 + 0x160bd8，完全相同）。**换 boot 没有换掉故障环**。[已验证]

基线落盘：`D:/MidaVault/lab/evidence/xx21b_006r3/session_baseline_006r3.txt`

---

## 三、实弹执行（2 次尝试，路径 C 第 2 次即停）

**执行命令（与 TASK-006R/006R2 完全一致）：**
```bash
MSYS_NO_PATHCONV=1 NO_BYPASS=1 MIDA_GTO_NO_BYPASS=1 MIDA_LEGACY_ANTIDEBUG=1 CARGO_INCREMENTAL=0 \
  timeout 300 target/release/mida-cli.exe /unpack \
  D:/MidaVault/objects/sha256/78/7800980301207bf2f851d00a50f7f18e0dcd61a0f2b1581ca609ddcc0f2f1ea7 \
  --shrink --profile=oreans-classic --container-restore=off \
  -o lab/xx21b_006r3/rev2_unpacked_006r3.exe > lab/xx21b_006r3/attemptN.log 2>&1
```

| # | EXIT | image_base | AV 事件数 | C-7 触发行 | 恒同元组 | 首 AV→触发 | 日志体积 | 产物 | 残留进程 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 1 | 0x7ff729430000 | 1024 | 1（count=1024） | (exc=0x7ff857780bd8, target=0x17d, exc_type=0, thread=21840) ×1024 | 0.0194 s | 312225 B | 无 | 无 |
| 2 | 1 | 0x7ff729430000 | 1024 | 1（count=1024） | (exc=0x7ff857780bd8, target=0x1dc, exc_type=0, thread=8724) ×1024 | 0.0215 s | 310154 B | 无 | 无 |

**attempt1 关键日志（原始输出）：**
```
[INFO] PackerPlugin identify: Match family="oreans_themida" confidence=83
[INFO] Debuggee process created pid=23260 tid=21840
[INFO] event received (seq=1): CreateProcess { process_id: 23260, thread_id: 21840, image_base: 0x7ff729430000, ... }
[INFO] ScyllaHide injection completed successfully
[INFO] PackerPlugin: text-poll path → deferring guard to .text-stable poll (30s idle timeout)
[FATAL] C-7: guardless constant-AV storm — aborting fail-closed (tuple=(exc=0x7ff857780bd8, target=0x17d, exc_type=0, thread=21840) x1024, count=1024)
[FATAL] Fatal error: guardless constant-AV storm abort (fail-closed): identical AV tuple (exc=0x7ff857780bd8, target=0x17d, exc_type=0, thread=21840) repeated 1024 times without guard installed; aborting unpack
```

**终态判定：路径 C**（C-7 风暴终止生效）—— 2/2 次确定性，与 TASK-006R2 同 pattern（同偏移 exc、同 count=1024、同 20ms 内触发）。[已验证]

时间线细节（时间戳为 UTC，本地=UTC+8）：
- 首个 AV → C-7 触发行（`C-7: guardless constant-AV storm — aborting`）：0.0194s / 0.0215s —— **20ms 级主动中止**
- 触发行 → Fatal error 行：~5.0s（引擎收尾/清理阶段，非风暴循环耗时）

---

## 四、三个 TASK-009 证据点逐条命中情况

| 证据点 | attempt1 | attempt2 | 说明 |
|---|---|---|---|
| `TASK-009: zero-filled IAT region` | **0** | **0** | 该日志在 dump 重建阶段；C-7 在 text-poll 阶段（dump 之前）中止 → 未到达 [已验证] |
| `TASK-009 fail-closed` | **0** | **0** | 门在 dump 重建末尾；未到达 [已验证] |
| `[GOOD] Candidate written` | **0** | **0** | 无产物写出，无此日志 [已验证] |
| （C-7 观测）`guardless constant-AV storm abort` | 1 | 1 | 引擎主动 fail-closed 中止 [已验证] |

命令与原始输出：
```bash
$ grep -c -a "TASK-009: zero-filled IAT region" lab/xx21b_006r3/attempt1.log   → 0
$ grep -c -a "TASK-009 fail-closed" lab/xx21b_006r3/attempt1.log               → 0
$ grep -c -a "\[GOOD\] Candidate written" lab/xx21b_006r3/attempt1.log         → 0
$ grep -c -a "guardless constant-AV storm abort" lab/xx21b_006r3/attempt1.log  → 1
（attempt2 同：0 / 0 / 0 / 1）
```

**结论**：缺陷 A（TASK-009）的三个证据点仍全部 0 命中。**不是修复失败，是验证点不可达** —— 与本 boot 无关，故障环在换 boot 后仍确定性复现（偏移 0x160bd8 不变）。[已验证]

---

## 五、路径 C 量化观测（四项数字齐全）

| 项 | attempt1 | attempt2 |
|---|---|---|
| AV 事件数（`grep -c "Access violation: exc="`） | **1024** | **1024** |
| 首次 AV → C-7 触发耗时 | **0.0194 s** | **0.0215 s** |
| 日志体积 | **312225 B** | **310154 B** |
| 残留 debuggee 进程 | **无** | **无** |

原始计数命令与输出：
```bash
$ grep -c "Access violation: exc=" lab/xx21b_006r3/attempt1.log   → 1024
$ grep -c "C-7: guardless constant-AV storm" lab/xx21b_006r3/attempt1.log → 1
$ grep -c "Access violation: exc=" lab/xx21b_006r3/attempt2.log   → 1024
$ grep -c "C-7: guardless constant-AV storm" lab/xx21b_006r3/attempt2.log → 1
```
- 事件类型分布：CreateProcess 1 / LoadDll 18 / AccessViolation 1024（两 attempt 相同）
- 恒同元组：attempt1 `(exc=0x7ff857780bd8, target=0x17d, exc_type=0, thread=21840)` ×1024；attempt2 `(exc=0x7ff857780bd8, target=0x1dc, exc_type=0, thread=8724)` ×1024（uniq -c 各 1 行，总数 1024）
- 全系统复查无 xiong/rev2/mida/unpack 进程，无产物文件 ✅

---

## 六、路径 A/B 相关（不适用，如实记录）

- **路径 A（fail-closed 生效）**：未到达 —— dump 阶段从未进入，`TASK-009 fail-closed` 0 命中。
- **路径 B（产物写出）**：未到达 —— 无产物，`[GOOD] Candidate written` 0 命中。P-4 当场存活探针、`.rdata 0x1137d0` 槽字节、`.bss 0x112c10` 均无从执行。

---

## 七、停止规则遵守证明

- 实际尝试次数：**2**（非 9，非 3）。
- 终态判定：路径 C 是工单明示的合法终态 → 不构成"失败"，未触发「连续 2 次失败即停」。
- 走到路径 C **第 2 次即停**，未刷第 3 次 —— 严格按工单「不许为了走通路径 B 反复重跑」。
- 每次尝试均记录 image_base（0x7ff729430000 ×2）✅
- 未改动 crates/ 与 lab/cases/v2/*.json（git status 验证空）✅

---

## 八、日志留存

1. **删除前计数已算好**（见 §五）：AV=1024×2、C-7 abort=1×2、恒同元组 1024×2、事件类型分布已记。
2. **有界摘要进 vault**（头 200 + 尾 200 + 计数）：`attempt1_log_bounded_summary.txt`（400 行 / 59458 B）、`attempt2_log_bounded_summary.txt`（400 行 / 59072 B）✅
3. **日志本体**：均 <50MB（312225 B / 310154 B），**一并留档**进 vault，无需删除。工作区 `lab/xx21b_006r3/` 保留原始副本（untracked、*.log 被 .gitignore 覆盖，不进 Git）。

vault 归档清单（`D:/MidaVault/lab/evidence/xx21b_006r3/`）：
```
attempt1.log                    312225 B   重脱壳完整日志（本体）
attempt1_log_bounded_summary.txt 59458 B   有界摘要（头200+尾200）
attempt2.log                    310154 B   重脱壳完整日志（本体）
attempt2_log_bounded_summary.txt 59072 B   有界摘要（头200+尾200）
scylla_hide_006r3.log            3121 B    ScyllaHide 注入日志（最后一次注入，attempt2）
session_baseline_006r3.txt       1919 B    会话基线
```

---

## 九、改动文件（对照授权清单）

- **无**。未改 crates/、lab/cases/v2/*.json，未提交、未推送、未 stash、未改 git config。
- 新增 untracked：`lab/xx21b_006r3/`（本单工作目录，按 ARTIFACT_POLICY 不进 Git）。
- vault 新增：`D:/MidaVault/lab/evidence/xx21b_006r3/`（见 §八清单）。
- **观察到的既有/外部 untracked（非本单产物，未触碰）**：执行期间发现 `tools/_prepush_gates.cmd`（mtime 2026-08-30 01:36:23，与本单最终核验时刻重合，但本 worker 未执行任何写入 tools/ 的命令）。推断为总指挥/其他并发进程产物（工单明示「工作区留给总指挥」），**未读未改未删**。既有 untracked（`.workbuddy-ai/`、`tools/xx21_msvc_env.cmd`、`tools/xx21_step1_static*.json`）同样未触碰。[存疑]

---

## 十、结论摘要

- **换 boot 没换掉故障环（本单核心新信息）**：机器重启（BootTime 07:58:23 → 01:28:40，系统 DLL 与 debuggee image_base 全部换新）后，ScyllaHide NtContinue-hook 故障环**仍然确定性复现** —— exc 偏移仍 = ntdll+0x160bd8（0x7ff857780bd8），2/2 次在 text-poll 阶段由 C-7 主动 fail-closed 中止（count=1024，20ms 级）。累计 **13/13 次**（TASK-006R 9 + 006R2 2 + 006R3 2）确定性撞同一故障环，跨 3 个 boot。这坐实了 TASK-006R2 的推断：**这不是 ASLR 运气问题，是 ScyllaHide-NtContinue-hook 交互的确定性故障**，需要另立专项（工单路径 C 明示"换 boot 都没换掉，说明不是运气问题，需要另立专项查 ScyllaHide-NtContinue-hook 交互"）。
- **缺陷 A（TASK-009）实弹替换验证：仍未到达（验证点不可达）**。路径 A/B 均未走通，三个证据点 0 命中。这不是修复失败 —— fail-closed 门从未被走到，无法证明或证伪。C-7 让"不可达"以 20ms + 312KB 的干净中止呈现（非 3.5GB / 外部超时）。
- **C-7 风暴终止跨 boot 稳定性**：本 boot 再次实弹验证通过（2/2 主动中止、无残留、无产物、20ms 级）。
- **账本**：XC-XXI-B 4/4 → **5/4**（本格按 1 格记账，授权令牌已回抄）。

---

## 十一、我没做的事 / 我不确定的事

1. **缺陷 A（TASK-009）的实弹效果：仍未验证**。路径 A 与路径 B 均未到达 —— 故障环在换 boot 后仍确定性触发，dump 阶段从未进入。要验缺陷 A，需要先解决 text-poll 阶段的 ScyllaHide NtContinue-hook 故障环（另立专项），或找到能让 text-poll 收敛的执行环境。[已验证（指"未到达"这一事实）]
2. **ScyllaHide NtContinue-hook 故障环根因：未定位**。本单只证明"跨 boot 确定性复现、偏移固定 0x160bd8"，未做 hook 代码路径的深入分析（超本单范围）。[存疑]
3. **BootTime 与令牌不一致**：令牌载 00:54:51，本会话实测 01:28:40 —— 总指挥亲验后又重启过一次。已按工单照实记录、继续。若总指挥认为需要以此判定环境差异，本报告的跨 boot 结论（故障环未消失）不受影响。[存疑]
4. **scylla_hide.log 只留最后一次注入**：每次尝试前 `rm -f target/release/scylla_hide.log` 重置，vault 留档的 scylla_hide_006r3.log 是 attempt2 的；attempt1 的注入成功标记在 attempt1.log 第 14 行（`ScyllaHide injection completed successfully`），两者均验证。[已验证]
5. **C-7 阈值 1024 的误杀侧校准**：延续 TASK-012 的 [存疑] —— 本 boot 未遇到合法恒同 AV >1024 的场景，误杀侧实弹未校准。[存疑]

---

## 十二、建议（待总指挥/老板决策）

1. **本单终态**：路径 C 合法收口。缺陷 A 验证点**结构性不可达**（跨 3 boot 13/13 确定性撞环），继续按原方式开格重跑没有信息增量。
2. **另立专项**：查 ScyllaHide 的 NtContinue-hook 与 Themida 壳在 text-poll 阶段的交互（exc 恒 = ntdll+0x160bd8，guardless 路径无 guard 可装，1024 恒同 AV 环）。这是解锁缺陷 A 实弹验证的前置。
3. **样品定位符**：沿用 vault 对象路径（已按 D-015/P-6 执行），不再触碰 `D:\Tools\RE\dumps\gto\启动器.exe`。
4. **账本**：XC-XXI-B 5/4 记账请总指挥确认（令牌已授权）。

---

*worker · TASK-006R3 · 2026-08-30 · NO_BYPASS=1 · 样品身份核验 PASS（78009803…）后执行 · git 只读（未 commit/push/stash、未改 config、未改 crates/ 与 lab/cases/v2/*.json）· 无新增依赖 · 2 次实弹尝试均走路径 C（C-7 主动中止，20ms 级），无产物，无残留进程，日志 ~312KB/次（有界摘要 + 本体均入 vault）*
