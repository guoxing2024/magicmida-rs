# TASK-004 执行报告 — T0.7 会话绑定修复：补齐可离线验证的闭环

- 执行日期：2026-08-29
- 工单：TASK-004
- 岗位：developer
- 状态：**PASS**（6 条验收标准全部通过）

## 改动文件（对照授权清单）

只改了授权清单内的 3 个代码文件（其余 3 个清单文件 `data_reinit.rs` / `commands.rs` / `args.rs` 本次无需改动，帮助文本写入 `lib.rs`），外加 `runs/` 报告。未做任何 git 写操作，未改测试断言、未加 `#[ignore]`、未改 `_clippy_baseline`、未改 CI。

```
 crates/cli/src/lib.rs                    |  11 ++
 crates/pe/src/dumper/dump_process.rs     |  94 ++++++++++++++++
 crates/pe/src/dumper/sidecar_consumer.rs | 178 +++++++++++++++++++++++++++++++
 3 files changed, 283 insertions(+)
```

## 改动摘要（每个文件 → 改了什么）

| 文件 | 改了什么 |
|---|---|
| `crates/pe/src/dumper/sidecar_consumer.rs` | 测试模块新增 2 个端到端用例 + 合成 2 节 PE64 构造辅助：`synthetic_pe_with_data()`（.text 可执行 + .data 可写，VA==raw offset 使 cleanup 的 buf 切片定位正确）、`e2e_old_table()` / `e2e_new_table()`（新旧 ASLR 会话表）、`read_qword()`。新增用例 `e2e_cleanup_relocates_old_session_pointers_and_leaves_none_behind`（完整链路：serialize_session_table 造新旧表 → parse 回读 → cleanup_artifact → 断言重定位/归零/计数/无残留）与 `e2e_cleanup_scan_only_reports_without_rewrite`（空旧表时 cleanup 是 no-op） |
| `crates/pe/src/dumper/dump_process.rs` | 新增测试模块 `session_sidecar_round_trip_tests`，用例 `persist_sidecar_round_trips_through_parse_session_table`：驱动真实 `persist_session_modules_sidecar` 写临时 sidecar JSON → `parse_session_table` 读回 → 断言 name/base/end 原样 round-trip + schema_version + candidate_sha256 正确 |
| `crates/cli/src/lib.rs` | `print_help()` 的 COMMANDS 段新增 `/rebase-fixed` 与 `/session-clean` 两行（用法字符串与 `args.rs:634`/`args.rs:677` 完全一致） |

未动 `is_stale_absolute_pointer` 判定逻辑（工单第 4 条）；未放宽任何现有断言（工单第 5 条）。

## 起点基线

- `cargo test -p mida-pe --lib --offline` 起点：**1039 passed / 0 failed**（工单所述 1031 为历史数字，实际工作区当前为 1039）。
- `cargo test -p mida-cli --lib --offline` 起点：**572 passed / 0 failed / 1 ignored**。
- `target/release/mida-cli.exe` 起点：**不存在**（工单提示的「当前无输出」是二进制过期/缺失导致，需重新构建后再判断）。

## 验收标准逐条对照

### 标准 1：`cargo test -p mida-pe --lib --offline` → 全绿，0 failed，总数比基线多出新增用例数

命令：`cargo test -p mida-pe --lib --offline`（MSVC 环境，经 `tools/_enter_msvc_env.cmd`）

原始输出（关键行）：

```
running 1042 tests
test apiset::tests::is_apiset_dll_detection ... ok
...
test dumper::sidecar_consumer::tests::e2e_cleanup_relocates_old_session_pointers_and_leaves_none_behind ... ok
test dumper::sidecar_consumer::tests::e2e_cleanup_scan_only_reports_without_rewrite ... ok
test dumper::dump_process::session_sidecar_round_trip_tests::persist_sidecar_round_trips_through_parse_session_table ... ok

test result: ok. 1042 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 9.33s
EXIT=0
```

新增用例函数名（3 个）：
- `dumper::sidecar_consumer::tests::e2e_cleanup_relocates_old_session_pointers_and_leaves_none_behind`
- `dumper::sidecar_consumer::tests::e2e_cleanup_scan_only_reports_without_rewrite`
- `dumper::dump_process::session_sidecar_round_trip_tests::persist_sidecar_round_trips_through_parse_session_table`

总数 1039 → **1042**（+3），0 failed。**PASS**

### 标准 2：`cargo test -p mida-cli --lib --offline` → 全绿，0 failed

命令：`cargo test -p mida-cli --lib --offline`（MSVC 环境）

原始输出（关键行）：

```
test result: ok. 572 passed; 0 failed; 1 ignored; 0 measured; 0 filtered out; finished in 21.88s
CLI_EXIT=0
```

**PASS**

### 标准 3：`cargo clippy --workspace --lib --bins --offline -- -D clippy::unwrap_used -D clippy::expect_used -D clippy::manual_let_else` → exit 0

命令：`cargo clippy --workspace --lib --bins --offline -- -D clippy::unwrap_used -D clippy::expect_used -D clippy::manual_let_else`（MSVC 环境）

原始输出（关键行）：

```
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 11.30s
EXIT=0
```

**PASS**（exit 0；mida-cli lib 有 57 个既有 warning，非本次引入，且均非 deny 的三个 lint）

### 标准 4：`cargo fmt --all -- --check` → exit 0

命令：`cargo fmt --all -- --check`

原始输出：首次运行 exit 1（仅 lib.rs 新增 help 长行与 dump_process.rs 测试元组的格式差异），执行 `cargo fmt --all` 后重跑：

```
$ cargo fmt --all -- --check
$ echo $?
0
```

**PASS**

### 标准 5：重新构建后 `target/release/mida-cli.exe --help` 能看到 `/session-clean` 和 `/rebase-fixed`

命令：`cargo build --release -p mida-cli`（MSVC 环境）→ `./target/release/mida-cli.exe --help`

原始输出（构建）：

```
   Compiling mida-packers-themida v0.1.0 ...
   Compiling mida-cli v0.1.0 ...
    Finished `release` profile [optimized] target(s) in 49.82s
EXIT=0
```

原始输出（--help COMMANDS 段）：

```
COMMANDS:
  /unpack <file> [options]           Themida-oriented unpack
  /generic-unpack <file> [options]   Packer-agnostic full dump (no shrink)
  /dump-process <pid> <file> [--module=<name>]  Dump .text from running process (optionally a loaded module)
  /rebase-fixed <input> <output> --old-base=<0x..> --new-base=<0x..>
                                  Rebase absolute pointers frozen to an old image base
  /session-clean <input> --old-table=<path> --new-table=<path> [--output=<path>] [--report=<path>]
                                  Rewrite an old-session PE artifact against the current
                                  session's module table (consume .session_modules.json)
  /verify <unpacked> <ref>           Verify against reference
  /offline-preflight <dir> [options] Emit runner-config envelope and run the
                                    independent offline preflight gate
```

`grep -iE "session|rebase"` 命中 2 行，退出码 0。**PASS**

### 标准 6：端到端用例必须真的会失败（判别力证明）

**临时改坏**：注释掉 `sidecar_consumer.rs` 中 `rewrite_section_pointers` 的两处实际写入（`copy_from_slice(&nv.to_le_bytes())` 与 `fill(0)`），保留计数逻辑。

**故意改坏 → 变红**（输出）：

```
test dumper::sidecar_consumer::tests::e2e_cleanup_relocates_old_session_pointers_and_leaves_none_behind ... FAILED
thread 'dumper::sidecar_consumer::tests::e2e_cleanup_relocates_old_session_pointers_and_leaves_none_behind' (19472) panicked at crates\pe\src\dumper\sidecar_consumer.rs:665:9:
  left: 140732845417360
 right: 140714222183312
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 1041 filtered out; finished in 0.00s
error: test failed, to rerun pass `-p mida-pe --lib`
EXIT=101
```

（left = 0x7FFEEB426390 旧 ntdll 指针未被重写；right = 0x7FFA952A6390 期望的新 ntdll 基址+偏移。）

**恢复 → 变绿**（输出）：

```
test dumper::sidecar_consumer::tests::e2e_cleanup_relocates_old_session_pointers_and_leaves_none_behind ... ok
test dumper::sidecar_consumer::tests::e2e_cleanup_scan_only_reports_without_rewrite ... ok
test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 1040 filtered out; finished in 0.00s
EXIT=0
```

**恢复后的 `git diff --stat`**（证明临时改动已恢复，无残留）：

```
 crates/cli/src/lib.rs                    |  11 ++
 crates/pe/src/dumper/dump_process.rs     |  94 ++++++++++++++++
 crates/pe/src/dumper/sidecar_consumer.rs | 178 +++++++++++++++++++++++++++++++
 3 files changed, 283 insertions(+)
```

（全部为新增代码，无删除/修改既有逻辑；`grep -c "TEMP-BREAK" sidecar_consumer.rs` = 0。）**PASS**

## 我不确定的事

1. **我验证了什么**：
   - 端到端离线闭环：合成 PE（.data 可写节）+ 新旧会话模块表（旧 ntdll/kernel32 基址 vs 新基址，模拟 ASLR 变化）→ `serialize_session_table` 造表 → `parse_session_table` 读回 → `cleanup_artifact` 重写 → 断言「按模块名+模块内偏移重定位到新基址」「无法映射的归零」「CleanupStats 计数与实际改写字节一致」「重写后产物不再存在任何落在旧会话模块区间内的绝对指针」。该用例在注释掉重写步骤后确实变红，证明有判别力。
   - schema 一致性：`persist_session_modules_sidecar`（dump 管道写端）写出的 `mida.session-modules/v1` JSON 能被 `parse_session_table`（消费读端）原样读回（name/base/end 逐字段等价），两侧 schema 无漂移。
   - `--help` 可见性：重新构建 release 后 `/session-clean` 与 `/rebase-fixed` 出现在 COMMANDS 段，用法与 `args.rs` 内字符串一致。
2. **跨 ASLR 重启这件事我仍然没有验证**：本工单是纯离线验证。我**没有**做过以下任何一项：
   - 没有在真实 Windows 上跑一次实弹 dump → 重启 → `/session-clean` → 启动宿主，来证明重写后的产物在下次开机真能通过 `call rax` 取指。
   - 没有验证真实 dump 产物的 `module_map`（`persist_session_modules_sidecar` 的输入）与真实会话表 JSON 在实战中的字节级一致性——我的 round-trip 用例用的是合成模块表驱动真实写端。
   - 没有验证 `rewrite_section_pointers` 对真实 PE 里 `VA != raw offset` 的数据节定位是否正确（我的合成 PE 特意让 VA==raw offset 以便 cleanup 的 buf 切片直接命中；真实 dump 的 .data 节 RVA 通常与 raw offset 不同，cleanup_artifact 用 `section.virtual_address` 直接当 buf 偏移的假定，需要实弹 dump 产物验证）。
   - 实弹验证在等老板决策，不属于本工单（按停止规则未自行制造重启）。
3. **合成 PE 的局限**：`synthetic_pe_with_data` 是字节级最小 PE，仅含 .text + .data 两节，VA 与 raw offset 对齐；它覆盖了 cleanup 的判定/重写路径，但不覆盖真实 PE 的节对齐、导入表、重定位表等结构。`pe_structure_bytes_untouched_by_cleanup`（既有用例）覆盖了非数据节不被触碰的保证。
4. **既有 warning**：`mida-pe` lib test 有 2 个既有 unused import warning（`raw_slab_coherence_tests_route_r0.rs:275`、`exception_final.rs:935`），`mida-cli` lib 有 57 个既有 clippy warning。均为工作区既有状态，非本次改动引入，且不在验收 3 的 deny 清单内（该命令 exit 0）。
5. **测试总数差异**：工单写「当前 1031」，实测起点为 1039——工作区在工单编写后已有其他提交增加过测试。本报告以实测起点（1039 → 1042）为准。
