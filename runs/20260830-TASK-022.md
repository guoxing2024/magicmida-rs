# TASK-022 执行报告 — 当前 boot 重产 core 完美候选（D-043 选项 B）+ 同会话 T0.5 三态重跑收官

- 执行者：worker（developer 岗位，一次性执行）
- 日期：2026-08-30（UTC）
- 账本：XC-XXI-B 13/4 → **14/4**（本单 1 格：生产 dump 1 次 + T0.5 三趟，按票面计 1 格）
- 证据库：`D:/MidaVault/lab/evidence/xx21b_repro/`（INDEX.md 已登记）

## 0. 授权令牌（原文回抄）

> `老板 · 2026-08-30 · 原话"当前 boot 重产候选"（按全案解释：批准 D-043 选项 B 全案 = 在当前 boot 会话重产 core 完美候选（活体 dump → 固化 → S1-S4 + 会话指针普查硬门），并同会话完成 T0.5 三态重跑收官；实弹 1 格（生产 dump 含重试尝试 + 重跑多趟，按 T018 5 次/1 格、T0.4 3 次触发/1 格先例），账本 XC-XXI-B 13/4 → 14/4；三态判定语义与 T017/T018/T019/T021 逐字一致；BootTime 硬门与普查硬门以票面为准）· 前置由总指挥`tools/xx21b_session_pointer_census.py`�BootTime = 2026-08-30 10:05:53.549 连续无重启；宿主 a852880a / 原版 core 09f3dd34 / config cde9be13 在位（vault）；普查器 xx21b_session_pointer_census.py `2026-08-30 10:05:53.549`��，对已知答案 094f5401）`

## 1. 改动文件（对照授权清单）

| 文件 | 授权 | 改动 |
|---|---|---|
| `tools/xx21b_t05_ui_drive_pcell.py` | 票面 §6.1 唯一授权脚本改动 | `CAND_SHA` → `096f3bdf...`（新候选 R2）+ `DEPLOY` → `lab/xx21b_run_pcell2` + 打印标签（+6/−6，最小 diff；死区间标注常量保留不动） |
| `lab/xx21b_repro/` | **新建（授权）** | 部署（原版三件）+ 活体 dump + 固化候选 + S1-S4 证据 + 普查 |
| `lab/xx21b_run_pcell2/` | **新建（授权）** | T0.5 部署（宿主 a852880a + 新候选 + config cde9be13）+ 3 趟证据 |
| `D:/MidaVault/lab/evidence/xx21b_repro/` | **新建（授权）** | 证据 24 件 + INDEX.md |
| `crates/` | 零改动 | 未动（git status 无 M） |
| 既有脚本（T017/T018/T019/T020 等） | 零改动 | 未动（git diff 仅本脚本） |

`git diff --stat`：
```
 tools/xx21b_t05_ui_drive_pcell.py | 12 ++++++------
 1 file changed, 6 insertions(+), 6 deletions(-)
```

## 2. 任务 0：三份权威报告 + 普查器自校准

- 通读 `docs/XX21_CORE_PERFECT_REPORT_20260829.md`（Step 3 dump 配方权威：`mida-cli /dump-module <pid> out.dll --module=core.dll --keep-runtime-base`，23 节完整映像，.winlice/.boot 内容寻址对照）、`docs/XX21B_CORE_PERFECT_REPORT_20260829.md`（Step 2 固化：keep_runtime_base + EP=NOP stub `31c0ffc0c3` DLL 语义 + IAT 修复 + 23 节保留 + .winlice 明文不剥离 + .boot 加密保留；Step 3 S1-S4 判据权威）、`runs/20260830-TASK-021.md`（含总指挥审计附注 D-043：F1 att2 误引 / F2 DllCharacteristics=0x20 / F3 陈旧指针规模升格 ~230 槽，理解为什么重产）。
- 普查器自校准（硬门，不过即 STOP）：

命令：`python tools/xx21b_session_pointer_census.py --selftest --image lab/xx21b_run_pcell/core.dll`
原始输出：
```
  own_image_refs             got=4865   expect=4865   OK
  old_ntdll_hits             got=13     expect=13     OK
  old_ntdll_all_unaligned    got=True   expect=True   OK
  family_b14_b32_hits        got=73     expect=73     OK
  family_b14_b32_aligned     got=69     expect=69     OK
  kuser_hits                 got=3      expect=3      OK
SELFTEST PASS (lab/xx21b_run_pcell/core.dll)
SELFTEST_EXIT=0
```
结论：6/6 指标 OK，SELFTEST PASS。→ 工具/环境正常 [已验证]

## 3. 开跑前自查（验收标准 6）

### 3.1 BootTime 硬门（开跑前）
命令：`powershell -NoProfile -Command "(Get-CimInstance Win32_OperatingSystem).LastBootUpTime.ToString('yyyy-MM-dd HH:mm:ss.fff')"`
原始输出：`2026-08-30 10:05:53.549`
结论：与票面/T021 记录逐字符一致。→ PASS [已验证]

### 3.2 HEAD + 工作区
命令：`git log --oneline -1`
原始输出：`14c8d56 docs: TASK-022 authorized (D-043 option B per boss ruling; ...)`
结论：TASK-022 授权提交在位。→ PASS [已验证]

### 3.3 部署三件 sha（lab/xx21b_repro，vault 原版）
命令：`sha256sum lab/xx21b_repro/core.dll lab/xx21b_repro/rev2_unpacked.exe lab/xx21b_repro/config.ini`
原始输出：
```
09f3dd344215c6aa608bc6a8e8ae24486e3bf425c3f3541272d065a1d9999144 *lab/xx21b_repro/core.dll
a852880aabba215b16a2a96245322ca09d19ff148afaa30ff42b1a8ea438edac *lab/xx21b_repro/rev2_unpacked.exe
cde9be13a5da62f5805cbf3b359c56c27f020d12cd1ae838f6a4218492d1d610 *lab/xx21b_repro/config.ini
```
结论：3/3 匹配票面（原版受保护 core 09f3dd34 / 宿主 a852880a / config cde9be13），vault 逐位一致。→ PASS [已验证]

### 3.4 防火墙 BLOCK 只读清单
命令：`Get-NetFirewallRule -Direction Outbound -Action Block -Enabled True | Select-Object -ExpandProperty DisplayName`
原始输出：
```
codex_sandbox_offline_block_loopback_udp
codex_sandbox_offline_block_loopback_tcp
codex_sandbox_offline_block_outbound
BLOCK_GTO_LAUNCHER
BLOCK_GTO_TMP
BLOCK_XX21B_REV2_HOST
BLOCK_XX21B_RUNUI_HOST
BLOCK_XX21B_RESUME_HOST
BLOCK_XX21B_RESUME_COREHOST
BLOCK_XX21B_RESUME_CORE_DEPLOY
BLOCK_XX21B_RESUME_RUNUI
```
结论：11 条 BLOCK Enabled=True 在位；`codex_sandbox_offline_block_outbound`（Program=Any）覆盖部署路径；未增删改。→ PASS [已验证]

### 3.5 残留进程
命令：`Get-Process | Where-Object { $_.ProcessName -match 'rev2|mida|host_loader|xx21' }`
原始输出：（空）
结论：无残留进程。→ PASS [已验证]

## 4. 任务 1：活体 dump（生产，C-8 无调试附加）

### 4.1 宿主启动（普通 CreateProcessW，NO_BYPASS=1，无 DEBUG_ONLY_THIS_PROCESS）
命令：`python lab/xx21b_repro/launch_host_prod.py`（本票任务 1 新脚本）
原始输出（`repro_observe.json` 关键字段）：
```
redline sha OK (core=09f3dd34 protected original, host=a852880a, config=cde9be13)
host started pid=25468 (CreateProcessW, NO_BYPASS=1, no debug attach)
module_map written: ... (51 modules)
{
 "pid": 25468,
 "core_base": "0x7ffc57e30000",
 "core_size": 14364672,
 "core_mz": true,
 "exports_dynamic": {"GetAppVersion": "0xbb30", "Run": "0x1c120"},
 "GetAppVersion_bytes": "41565756534881ec58010000803d7d09",
 "GetAppVersion_plaintext": true,
 "Run_bytes": "4157415641554154555756534881eca8",
 "Run_plaintext": true,
 "module_count": 51
}
READY_FOR_DUMP pid=25468 core_base=0x7ffc57e30000
```
结论：
- 宿主普通启动成功（非调试附加，满足 C-8）；引导沉降后 core.dll 以**当前 boot ASLR 基址 `0x7ffc57e30000`** 加载（票面 §5.1：新候选时基址不会 0x7ffe1da10000，属预期，不许修）。
- RPM 验证 core .text 已明文：GetAppVersion 本址 prologue `41565756534881ec58010000...`（标准 x64 prologue，非加密态）；Run prologue `4157415641554154555756534881eca8`。
- 导出 RVA 从内存映像导出表**动态解析**（不许硬编码）：GetAppVersion@0xBB30 / Run@0x1C120。
- `module_map.json` 写入（51 模块，`boot_time=2026-08-30 10:05:53.549`，census 格式）。→ PASS [已验证]

### 4.2 生产 dump
命令：`MSYS_NO_PATHCONV=1 ./target/release/mida-cli.exe /dump-module 25468 lab/xx21b_repro/dump_module_core_r2.dll --module=core.dll --keep-runtime-base`
原始输出（关键行）：
```
[INFO] Derived IAT region from import descriptors: va=0x7ffc57f9f280 size=0xb8
[INFO] Import table reconstructed module_count=5 thunk_count=5
[INFO] Using original PE import table (Magicmida approach): 12 modules, 12 thunks
[INFO] Resolved 12 API addresses from live IAT image
[INFO] Scanning for hardcoded addresses: runtime_base=0x7ffc57e30000, file_base=0x7ffc57e30000, image_size=0xdc3000
[INFO] Scanned 632832 bytes in writable sections, fixed 3500 hardcoded addresses
[GOOD] Full module dump written to lab/xx21b_repro/dump_module_core_r2.dll (keep_runtime_base=true)
EXIT=0
```
结果：dump 产物 `dump_module_core_r2.dll`（21 节，14,424,064 B，image_base=0x7ffc57e30000 keep_runtime_base）。

### 4.3 dump 完整性（内容寻址对照，XX21 §3.3 同款）
命令/脚本：节 sha256 对照
原始输出：
```
dump .winlice   rsize=7372800 sha256=385df6e23eecd8bf
dump .boot      rsize=5315072 sha256=350572e28c3853bf
orig .boot      rsize=5315072 sha256=350572e28c3853bf   <- 磁盘原版一致
```
结论：**.boot sha256 `350572e28c3853bf` == 磁盘原版 core.dll .boot 逐位一致**（加密保留，内容寻址对照 PASS）。.winlice 磁盘 rawsize=0（壳运行时材料化），dump 捕获材料化明文 7,372,800 B（熵 6.19，head `4989ec4981c469010000`，与 T0.4 材料化记录同型）。→ PASS [已验证]

> 注：dump 管线 `fix_hardcoded_addresses` 在本 dump 中修复 **3500** 个硬编码地址（旧会话绝对指针 → 当前 boot 模块重锚）——这是本票"重产"的核心价值：陈旧指针在 dump 时即被重锚，无需事后清洗。

## 5. 任务 2：固化 + S1-S4 + 普查硬门

### 5.1 固化（T0.4 同款变换）
命令/脚本：dump 产物 → EP 字段 `0x8a0108`→`0x1027c0` + NOP stub `31 c0 ff c0 c3` @ RVA 0x1027c0（DLL 语义，跳过壳 EP .boot）
原始输出：
```
EP stub target: offset=0x1027c0
EP field before: 0x8a0108
EP field after : 0x1027c0
bytes after : 31c0ffc0c3000000
differing bytes: 8 ['0xa8','0xa9','0xaa','0x1027c0','0x1027c1','0x1027c2','0x1027c3','0x1027c4']
```
结论：固化仅改 8 字节（EP 字段 3 + stub 5），与 T0.4 变换逐位同构。候选：**`core_perfect_candidate_r2.dll` sha256 `096f3bdfd30e71b7217e602da29a304981c06138c2c768e59cbc23799695923d`**，14,424,064 B，21 节，image_base `0x7ffc57e30000`，EP `0x1027c0` NOP stub，DllCharacteristics `0x20`（HIGH_ENTROPY_VA；DYNAMIC_BASE=0x40 清除，基址行为结论不变——对照 T021 审计 F2）。

### 5.2 S1 结构 R0B（12/12）
命令：`./target/release/mida-acceptance.exe check-static lab/xx21b_repro/core_perfect_candidate_r2.dll --role candidate --report lab/xx21b_repro/s1_r0b_r2.json`
原始输出：
```
{"verdict":"StructuralPassBehaviorPending","gates":[headers_bounds:pass,machine_magic_consistency:pass,sections_ranges:pass,alignment_and_sizes:pass,entry_point:pass,imports_iat:pass,export_directory:pass,tls_directory:pass,reloc_directory:pass,exception_directory:pass,aslr_reloc_consistency:pass,directories_bounds:pass],"failures":[],"residual_risks":[]}
EXIT=0
```
结论：**S1 PASS**，R0B 12/12 全过，failures=[]，residual_risks=[]。→ [已验证]

### 5.3 S2 明文（T0.4 同口径：.text 全块熵<6.5 + .winlice 明文可解码 + .boot 加密保留 + 导出本址明文 prologue）
命令/脚本：熵分块计算
原始输出：
```
.text entropy blocks: 2060/2060 < 6.5 (100.0%)
.text overall entropy: 6.1657
.winlice: entropy=6.7564 size=7372800 head=4989ec4981c4690100004889ea4881c2
.boot: entropy=7.9459 size=5315072 head=eb4417843af5c7d94dd2507bedf75d30
.boot sha256: 350572e28c3853bf == 磁盘原版
GetAppVersion@0xbb30 raw bytes: 41565756534881ec58010000803d7d09
Run@0x1c120 raw bytes: 4157415641554154555756534881eca8
```
结论：**S2 PASS**——.text 2060/2060 块熵<6.5（100%，T0.4 2059/2059 同口径；块数差因节尺寸微差），.winlice 明文可解码（熵 6.7564，head 与 T0.4 材料化记录同型），.boot 加密保留（熵 7.9459，sha==磁盘原版），导出本址明文 prologue。→ [已验证]

### 5.4 S3 host_loader 独立加载 ×6（隔离加载，不计格）
命令：`python lab/xx21b_repro/s3_load_six.py`
原始输出：
```
iter 1: pid=23996 alive=True base_hit=True av=False
iter 2: pid=26992 alive=True base_hit=True av=False
iter 3: pid=26552 alive=True base_hit=True av=False
iter 4: pid=25404 alive=True base_hit=True av=False
iter 5: pid=24256 alive=True base_hit=True av=False
iter 6: pid=200   alive=True base_hit=True av=False
{"all_alive":true,"all_base_hit":true,"any_av":false}
S3_EXIT=0
```
结论：**S3 PASS**——host_loader 独立加载 6/6，每次首选基址 `0x7ffc57e30000` 命中，进程存活，无 AV。→ [已验证]

### 5.5 S4 GetAppVersion ×10 + 页级零变化
命令：`python lab/xx21b_repro/s4_getapp_final.py <pid> lab/xx21b_repro/s4_getapp_final.json`
原始输出：
```
GetAppVersion_va: 0x7ffc57e3bb30, base: 0x7ffc57e30000
expected_full: 0x7ffc57f6c4c0 (= base + 0x13c4c0)
calls: [10x {"exit":"0x57f6c4c0","match":true}]
"all_match": true, "any_av": false,
"page_changes": {"text":0,"winlice":0,"boot":0}
S4_EXIT=0
```
结论：**S4 PASS**——GetAppVersion ×10 = `0x57f6c4c0` 全一致（== base+0x13c4c0，即 T0.4 行为门 `0x1DB4C4C0` 在新基址的同口径值：旧 base 0x7ffe1da10000+0x13c4c0=0x7ffe1db4c4c0→低32位 0x1db4c4c0；新 base 0x7ffc57e30000+0x13c4c0=0x7ffc57f6c4c0→低32位 0x57f6c4c0），页级零变化。→ [已验证]

> 判定依据（S4 数值随基址变化）：GetAppVersion 返回 `base+0x13c4c0`（.bss 内"1.1"版本字符串地址）。T0.4 报告 pagemonitor 记录值 `0x7FFE1DB4C4C0` = 0x7ffe1da10000+0x13c4c0（含基址），attempt3 行为门 `0x1DB4C4C0` 是低 32 位。新基址下全链自洽，行为语义逐位等价。

### 5.6 会话指针普查硬门（本票新增验收）
命令：`python tools/xx21b_session_pointer_census.py --image lab/xx21b_repro/core_perfect_candidate_r2.dll --module-map lab/xx21b_repro/module_map.json --out lab/xx21b_repro/census_r2.json`
原始输出（关键字段）：
```
image_sha256: 096f3bdf...
image_base: 0x7ffc57e30000
size_of_image: 14430208
dll_characteristics: 0x20
module_map_boot_time: 2026-08-30 10:05:53.549
total_hits: 12728
own_image_refs (BASE-LOCK): 4866
kuser_shared_data_refs: 3
module_map_refs: {KERNELBASE:2, KERNEL32:115, ntdll:21, advapi32:24, CRYPT32:3, gdi32:4, msvcrt:84, shell32:6, USER32:28, wininet:6, ...}
hard_violations_aligned_plaintext: 60
residual_unpatched: 7484
gate: FAIL   (exit 2)
```

**硬门判据逐项分析（关键，必须如实报告）**：

60 个"对齐明文违规"分解：
- **25 个 = `0x7fffffffffff`（INT64_MAX 哨兵）**：dump 管线写入的数据标记（已知答案 094f5401 同 25 个，同一偏移）。
- **35 个 = 数据常量**（0x7fffffffba30 / 0x7ff00000cb81 / 0x7fff9fb10054 / 0x7ffdbee200a6 等）：**全部在已知答案 094f5401 同偏移同值**（35/35，逐字节核对 0 差异）。

对照实验（决定性）：
命令：`python tools/xx21b_session_pointer_census.py --image lab/xx21b_run_pcell/core.dll --module-map lab/xx21b_repro/module_map.json --out lab/xx21b_repro/census_known_answer_check.json`
原始输出：`known-answer gate: FAIL, hard violations: 204`
即**已知答案 094f5401（D-041 总指挥验收 8/8、普查器自校准 6/6 通过的基线）在同一门判据下同样 FAIL（204 违规）**——其中含 25 哨兵 + 179 非哨兵（含 T020 已清洗的旧会话指针 0x7ffeeb1a.../0x7ffee950... 等）。

**新候选 vs 已知答案的陈旧指针对比（本票核心）**：
- T021 AV 家族 `0x7ffeeb1ab7b0`（精确）：**0 命中**
- 旧 ntdll `[0x7ffeeb320000,+0x300000)`：**0 命中**
- 旧 bss 簇 `0x7ffee950` / `0x7ffee9ee` / `0x7ffeea0f-a12` / `0x7ffeea8d-a90`：**0 命中**（除 1 个 .winlice 加密区未对齐命中 `0x7ffeea8ff181` @ 0x2b03b2，已知答案同偏移同值，入 residual 不盲改）
- 非哨兵对齐明文违规：**35 个（全为已知答案基线同值数据常量）** vs 已知答案 **179 个**（其中 144 个为旧会话指针 → 新候选已 0 残留）

**普查结论**：
- 新候选 **0 个旧会话指针残留**（dump 时 fix_hardcoded_addresses 已把 3500 个陈旧指针重锚当前 boot 模块，T021 的 0x7ffeeb1ab7b0 取指 AV 家族彻底消失）。
- 60 个硬门违规 = 25 哨兵 + 35 数据常量，**全部为 dump 管线固有数据，与已知答案基线逐字节一致**。
- **工具门字面判据 FAIL（exit 2），但这是工具分类缺口而非候选缺陷**：门逻辑把 INT64_MAX 哨兵与数据常量当"对齐指针违规"，而这些在已验收的已知答案基线上同样存在（已知答案 204 违规 > 新候选 60）。工具自校准只验 6 项指标，未验门判据本身对固有数据的判别力。
- 按票面"硬门 FAIL → STOP 上报，不许手改指针"：**不手改任何指针**，如实上报，判定留给总指挥/老板。

residual_unpatched 清单（7484 条）已入册 `census_r2.json`，逐条定性见 §8。

## 6. 任务 3：同会话 T0.5 三态重跑（3 趟）

### 6.1 部署（lab/xx21b_run_pcell2）
命令：`sha256sum lab/xx21b_run_pcell2/rev2_unpacked.exe lab/xx21b_run_pcell2/core.dll lab/xx21b_run_pcell2/config.ini`
原始输出：
```
a852880aabba215b16a2a96245322ca09d19ff148afaa30ff42b1a8ea438edac *lab/xx21b_run_pcell2/rev2_unpacked.exe
096f3bdfd30e71b7217e602da29a304981c06138c2c768e59cbc23799695923d *lab/xx21b_run_pcell2/core.dll
cde9be13a5da62f5805cbf3b359c56c27f020d12cd1ae838f6a4218492d1d610 *lab/xx21b_run_pcell2/config.ini
```
三 sha fail-closed 匹配。→ [已验证]

### 6.2 三趟执行
命令：`python tools/xx21b_t05_ui_drive_pcell.py t022_attN lab/xx21b_run_pcell2/t022_attN_evidence.json`（N=1,2,3）

趟 1（att1）原始输出（尾段）：
```
redline sha OK (core=096f3bdf current-boot candidate R2, host=a852880a, config=cde9be13)
[  0.000] host_start_debug: pid=3068 DEBUG_ONLY_THIS_PROCESS(0x2) NO_BYPASS=1
[  0.101] core_load_pump: core.dll LOAD_DLL base=0x7ffd30dd0000 (pump event)
[ 12.108] core_load_wait: {"loaded": false, "wait_s": 10.01, "core_module": null}
written: lab/xx21b_run_pcell2/t022_att1_evidence.json
EXIT=0
```
证据 JSON 关键字段：
```json
"redline": "FAIL_CORE_NOT_LOADED",
"base_agreement": {"preferred_from_pe_header": "0x7ffc57e30000",
  "pump_timing_sample_actual_base": "0x7ffd30dd0000",
  "load_dll_at_preferred_base_seen": true, "equal": true},
"pump": {"exit_code": 0, "process_exited": true, "continues": 55,
  "continue_fails": 0, "wait_errors": [], "first_breakpoint_seen": true}
```

趟 2（att2）：同 att1（`load_dll_at_preferred_base_seen=true`, `equal=true`, exit 0, continues 57）。

趟 3（att3）：
```json
"redline": "FAIL_HOST_EXITED_AT_BOOT",
"pump": {"exit_code": 0, "process_exited": true}
```
（泵事件流同样含 core LOAD_DLL @ 首选基址 0x7ffc57e30000，event 45；仅 0x80000003 引导断点异常；全线程 + EXIT_PROCESS = 0）

### 6.3 泵事件流逐事件核验（3/3 同构）
| 项 | att1 | att2 | att3 |
|---|---|---|---|
| 事件总数 | 55 | 57 | 55 |
| 分解 | 1 CP + 40 LD + 6 CT + 1 EXC + 6 ET + 1 EP | 1+40+7+1+7+1 | 1+40+6+1+6+1 |
| core LOAD_DLL @ 首选基址 0x7ffc57e30000 | ✅ (event 45) | ✅ | ✅ (event 45) |
| EXCEPTION 0x80000003 引导断点 | ✅ | ✅ | ✅ |
| EXCEPTION 0xc0000005 / 其它真实 AV | **0** | **0** | **0** |
| 全部 EXIT_THREAD / EXIT_PROCESS 退出码 | **0** | **0** | **0** |
| 泵健康（continues/continue_fails/wait_errors） | 55/0/[] | 57/0/[] | 55/0/[] |

### 6.4 普通启动对照（无调试器，确认非调试附加所致）
命令：`Start-Process rev2_unpacked.exe -WorkingDirectory lab/xx21b_run_pcell2`（NO_BYPASS=1）
原始输出：
```
run1 EXITED code=0
run2 EXITED code=0
run3 EXITED code=0
```
对照（原版 core.dll 09f3dd34 + 同宿主普通启动）：
```
alive=True pid=8260  MainWindowTitle='授权验证' Responding=True   <- 原版宿主存活并显示授权窗口
```
对照（T0.4 清洗后候选 094f5401 + 同宿主普通启动）：
```
run1 EXITED code=-1073741819   (= 0xC0000005, T021 同款陈旧指针 AV)
run2 EXITED code=-1073741819
```

### 6.5 三态判定（语义与 T017/T018/T019/T021 逐字一致）
- **基址硬门**：PASS——新候选 PE 头 ImageBase `0x7ffc57e30000`（动态读取），泵事件流确认 core 以首选基址加载（`load_dll_at_preferred_base_seen=true` 3/3）。未触发 FAIL_CORE_BASE_RELOCATED。
- **run_head 明文预检**：磁盘候选 Run@0x1C120 = `415741564155...`（明文 prologue，T018/T019 附加下加密态 5/5 反例对比成立）。
- **AV**：**无**——3/3 无任何 0xc0000005 / 异常退出（唯一异常为引导断点 0x80000003，DBG_CONTINUE 良性）。
- **FULL**：**未到达**——Run 从未触发（宿主在引导沉降期退出，Run 线程不存在，无 RIP 采样落入 urlmon 的可能）。
- **新阻塞类（宿主引导期干净退出 exit 0）**：**成立**——宿主加载新候选后引导沉降期以 exit code 0 干净退出（3/3 泵 + 3/3 普通启动），而原版 core 宿主存活并显示"授权验证"窗口。**这不是 AV、不是 FULL、不是附加改变行为（WinLicense 对话框未出现——宿主根本没到窗口创建阶段），而是新候选引发的宿主引导期干净退出路径** → Run 不可达 → 按票面 STOP 上报。
- **`.winlice` 残留命中**：不适用（无 AV 地址可标注；普查 0 个旧会话指针命中）。

## 7. 验收标准逐条对照

### 标准 1：BootTime 硬门（开跑前与收尾前两次记录）
- 开跑前：`2026-08-30 10:05:53.549`（§3.1）
- 收尾前：`2026-08-30 10:05:53.549`（命令同 §3.1，输出一致）
结论：两次一致，无重启，产物绑定当前 boot。→ **PASS [已验证]**

### 标准 2：新候选三件套（固化 sha 登记 + S1-S4 全过 + 普查硬门）
- 固化产物 sha 登记：`096f3bdfd30e71b7217e602da29a304981c06138c2c768e59cbc23799695923d`（§5.1，vault `candidate_registry_r2.json`）✅
- S1-S4 全过（T0.4 同口径）：S1 12/12、S2 2060/2060+winlice 明文+boot 加密保留+导出明文、S3 6/6、S4 ×10 全一致+页级零变化（§5.2-5.5）✅
- **普查硬门：字面 FAIL（60 对齐违规），但证据确凿为工具分类缺口**（§5.6）：
  - 新候选 **0 个旧会话指针残留**（T021 0x7ffeeb1ab7b0 族 0 命中；全旧族 0 命中）
  - 60 违规 = 25 INT64_MAX 哨兵 + 35 数据常量，**与已知答案基线逐字节一致**（已知答案同门 FAIL 204）
  - residual_unpatched 清单入册 `census_r2.json`（7484 条，定性见 §8）
  - module_map.json 与 dump 会话绑定（pid 25468 / boot_time / 51 模块）✅
  - 按票面"硬门 FAIL → STOP 上报，不许手改指针"：未手改任何指针，如实上报，判定留总指挥/老板。
结论：**S1-S4 PASS；普查硬门字面 FAIL（工具缺口，非候选缺陷）→ 上报**。

### 标准 3：三态判定（≥2 趟、逐趟证据、FULL 需 ≥2 趟可复现、STOP 路径如实上报）
- 3 趟执行，逐趟证据（evidence JSON / pump JSON / ndjson / base_agreement / 泵健康）全部入册（vault + lab）。
- **判定 = 新阻塞类（宿主引导期干净退出 exit 0）**：3/3 泵 + 3/3 普通启动可复现；非 AV（无异常事件）、非 FULL（Run 不可达）、非附加改变行为（无 WinLicense 对话框，进程未达窗口创建阶段即干净退出）。
- 任何 STOP 路径如实上报：普查硬门字面 FAIL + 三态新阻塞均如实记录。→ **STOP 上报 [已验证]**

### 标准 4：零越界
- 仅改 `tools/xx21b_t05_ui_drive_pcell.py`（CAND_SHA + DEPLOY + 打印标签，+6/−6 最小 diff）
- 新建 `lab/xx21b_repro/`、`lab/xx21b_run_pcell2/`、`D:/MidaVault/lab/evidence/xx21b_repro/`
- `crates/` 一行未动（git status 无 M）；既有脚本零改动（git diff 仅本脚本）；git 只读（未 commit/push）→ **PASS [已验证]**

### 标准 5：vault 证据先行
- 新候选 + module_map + census_r2.json + 3 趟证据 + preflight 自查 → `D:/MidaVault/lab/evidence/xx21b_repro/`（24 件 + INDEX.md，sha 双核与 lab 逐位一致，§证据核验）→ **PASS [已验证]**

### 标准 6：收尾
- 无残留进程（§3.5 + 收尾复检，rev2/host_loader 0 个）✅
- 临时文件逐个按名删除（`_run.cmd`、`/tmp/*.ps1`、`/tmp/host_rv2.bin`、`lab/xx21b_repro/nondebug_orig.log`、`lab/xx21b_run_pcell2/nondebug*.log`、`pid.txt`、`s3_load_test.log`、`s4_host*.log`、`host_launch.log` 等均已删，见各命令）✅
- 报告第一节回抄授权令牌 ✅
- 全部结论按 [已验证]/[推断]/[存疑] 标注 ✅

## 8. 阻塞点 / 我没做的事 / 我不确定的事

### 阻塞点
1. **普查硬门字面 FAIL（工具分类缺口，已充分证据）**：新候选 0 个旧会话指针残留（本票核心目标达成），但工具门把 dump 管线固有数据（25 个 INT64_MAX 哨兵 + 35 个数据常量）当"对齐明文违规"；对照已知答案 094f5401 同门 FAIL（204 违规）证明门判据不可判别。**未手改任何指针**（红线）。需总指挥/老板裁定：接受"0 旧指针残留 + 工具门缺口"为 PASS，或调整门判据（排除哨兵/固有数据类）。
2. **T0.5 三态 = 新阻塞（宿主引导期干净退出 exit 0）**：新候选消除了 T021 陈旧指针 AV（进步），但宿主加载新候选后引导期以 exit 0 干净退出（3/3 泵 + 3/3 普通启动），Run 不可达。原版 core 宿主存活并显示"授权验证"窗口；T0.4 清洗候选宿主 AV。**新候选引发的宿主引导期行为改变是真实样品级事实**，非调试附加所致（普通启动同型）。

### 我没做的事
- 没有为规避普查 FAIL 手改任何指针 / 清洗候选（红线；清洗是另一张票的授权范围）。
- 没有修改脚本逻辑以让三态"看起来" FULL（脚本零逻辑改动，仅常量/路径/标签）。
- 没有触发 Run（宿主死于引导期，Run 线程从未创建；不硬跑）。
- 没有新增防火墙规则、没有联网、没有写 C:\Windows、没有 commit/push、`crates/` 未动。
- 没有把 dump 会话（任务 1）与 T0.5（任务 3）的宿主部署混用——任务 1 用原版 core（09f3dd34，生产 dump），任务 3 用新候选（R2），严格隔离。

### 我不确定的事 / 诚实边界
1. **[存疑] dump 产物 21 节 vs T0.4 23 节**：本 dump 21 节（含 .import/.pdata/.edata 重建），T0.4 记录 23 节。差异在重建节数量（T0.4 的 dump 管线版本在 .edata 上多拆了 2 个节）。不影响 S1（R0B 12/12 过）、S2（明文判据过）、S3/S4（加载与行为过）。是否需与 T0.4 完全同节布局，由总指挥裁定（票面只要求"23 节完整映像 ≈14.4 MB 量级"，本产物 14,424,064 B 同量级）。
2. **[推断] 宿主引导期干净退出 exit 0 的根因**：未做宿主侧字节级根因定位（宿主内部代码路径：LoadLibrary core → 调用导出/校验 → 引导逻辑决定 ExitProcess(0)）。证据链：原版 core 宿主存活（授权窗口）→ 新候选宿主 exit 0 → T0.4 清洗候选宿主 AV。最可能：新候选 .winlice 材料化内容与当前 boot 的解密实体化状态存在差异，或 dump 重锚后的数据使宿主校验路径走到"干净退出"分支。**未深挖，如实标注**。
3. **[已验证 诚实记录] S4 数值随基址变化**：GetAppVersion 返回 base+0x13c4c0（版本字符串地址），新基址下 32 位退出码 = 0x57f6c4c0（≠ T0.4 的 0x1db4c4c0）。行为语义逐位等价（§5.5 判定依据），但若票面要求字面 `0x1DB4C4C0` 则需总指挥确认口径（票面 §5.1 明确"时基址不会是 0x7ffe1da10000，属预期"）。
4. **[存疑] .winlice 材料化内容 sha 与 T0.4 不同**（385df6e2 vs 70436934）：.winlice 是壳运行时解密材料化产物，每 boot 材料化状态可能不同（熵 6.19 vs 6.02，head 同型）。.boot（加密保留）sha 与磁盘原版逐位一致，内容寻址对照成立。是否要求 .winlice 与 T0.4 逐字节一致，由总指挥裁定（本票"内容寻址对照"仅 .boot 可比——磁盘原版 .winlice rawsize=0 无从对照）。
5. **[存疑] 普查 residual_unpatched 7484 条逐条定性**：已入册 `census_r2.json`。分类：明文节未对齐 383+102（.rsrc）+1（.pdata）、.winlice 5231、.boot 1767。其中 .winlice/.boot 为加密区命中（无法定性真伪指针，按 T020 纪律不盲改）；明文节未对齐命中为噪声/数据常量概率高但未逐条验证。**旧会话指针类 = 0 条（对齐明文 0 条，已核实）**。
6. **[存疑] 宿主 exit 0 是否为"附加改变行为"（WinLicense 对话框）的另一种表现**：未观察到 PigToGoLicenseDialog（进程未达窗口创建阶段即退出），与 T017 非附加下 3/3 出现"授权验证"窗口不同。如实记录：无对话框、无窗口、无异常，干净 exit 0。

## 9. 判定摘要（给总指挥）

- **任务 1（活体 dump）PASS**：原版 core 09f3dd34 + 宿主 a852880a，普通启动（C-8 无调试附加），core 以当前 boot 基址 0x7ffc57e30000 加载且 .text 明文沉降，mida-cli /dump-module 产物完整（.boot sha 与磁盘原版一致），module_map.json 绑定 dump 会话。
- **任务 2（固化 + S1-S4 + 普查）**：候选 `core_perfect_candidate_r2.dll`（096f3bdf）固化成功（EP=NOP stub，T0.4 同款 8 字节变换）；**S1 12/12、S2 明文全过、S3 6/6、S4 ×10 一致页级零变化**；**普查：0 个旧会话指针残留（本票核心目标达成），但工具门字面 FAIL（60 = 25 哨兵 + 35 数据常量，均与已知答案基线逐字节一致；已知答案同门 FAIL 204）→ 工具分类缺口，非候选缺陷；未手改指针，上报**。
- **任务 3（T0.5 三态）**：**新阻塞类（宿主引导期干净退出 exit 0）**，3/3 泵 + 3/3 普通启动可复现；非 AV、非 FULL、非附加改变行为（无对话框/窗口）。**新候选消除陈旧指针 AV（T021 根因根治），但宿主引导行为变为干净退出 → Run 不可达 → STOP**。
- **建议下一步（不烧本单格）**：① 总指挥裁定普查门判据（接受"0 旧指针 + 工具缺口"为 PASS，或调整门排除哨兵/固有数据类）；② 宿主引导期 exit 0 的根因定位（是否需宿主侧重脱壳/配置适配，另立单）；③ 若老板接受新候选为"完美候选"，T0.5 FULL 仍需宿主可达 Run 的路径（当前宿主与候选组合不可达）。

---
*worker · TASK-022 执行完成 · 2026-08-30 · XC-XXI-B 13/4 → 14/4*

---

## 附注：总指挥审计（2026-08-30，D-045）——任务 1/2/3 全部接受，STOP 成立；普查门裁定 = 实质 PASS + 工具 v2 闭环

**审计方式**：总指挥独立复算全部关键证据（不采信报告原文）：普查器复算双图（新候选 60 = 25 哨兵 + 35 其它，与报告逐位一致；已知答案 094f5401 → 204 = 25 + 179，worker 对照实验精确复现）；60 值逐一定性（值存在于 09f3dd34 / 3650ea6c / 094f5401 三方比对 + 字节级指令解码）；新候选 21 节表独立解析；.boot sha 独立重算（= 磁盘原版 `350572e28c3853bf`）；3 趟 evidence JSON + ndjson 独立读取（exit 0 / 无 AV / LOAD_DLL@首选基址 3/3 / att2 57 events 吻合）；vault sha 抽验（candidate/module_map lab==vault 逐位一致）；BootTime 第三次复核（审计时刻仍 `2026-08-30 10:05:53.549`）；`FAIL_HOST_EXITED_AT_BOOT` 确认为脚本既有逻辑（:1149，非私改）。

**核心裁定 1 —— 普查硬门：实质 PASS（0 个会话指针），工具缺口由总指挥 v2 闭环**：
- 60 个 v1"违规"逐一定性（总指挥字节级）：**25 × INT64_MAX 哨兵**（扫描器 6 字节窗口读到 `FF FF FF FF FF 7F`）+ **19 × x86 指令立即数伪读**（可执行节内 `CMP R10W, 0x7FFF` / `CMP DX, 0x7F` 等——壳 VM 的地址分类/掩码代码）+ **6 × 跨字段 RVA 伪读**（窗口低 32 位 = 合法 RVA，如 `0x7ff8000f7fd0` = .text 函数尾 RVA 0xF7FD0 与下字段拼接）+ **10 × 厂商继承常量**（同值存在于原版受保护 core 09f3dd34）。**0 个会话指针**。
- 真旧会话区间（0x7ffee*/0x7ffec*）复核：**对齐明文 0 命中**；8 个未对齐命中全在 .winlice/.boot，且可解码为 VM 指令 32 位立即数（如 `XOR ECX, 0x7FFEEA8F`——VM 材料化时烙入的旧会话地址高位字，非数据指针）→ 入册 residual（T020 纪律，不盲改）。
- **普查器升级 v2**（本附注随附提交）：新增 sentinel_marker / nan_double / inherited_baseline / code_immediate_context / cross_field_rva 五类判别 + **判别力锚点**——已知坏件 094f5401 同门必须 FAIL（实测 hard=144 真实旧会话指针）、TASK-022 重产候选 096f3bdf 必须 PASS（实测 hard=0，exit 0）。worker 的"已知答案对照"方法论正确，v2 把它固化进工具。
- 裁定：**TASK-022 普查门按实质判据 PASS**（0 会话指针 + 工具缺口已闭环）；residual 7484 条与 worker 分类一致（.winlice 5231 / .boot 1767 / 明文未对齐 486）。

**核心裁定 2 —— 三态 STOP 接受（新阻塞类：宿主引导期干净退出 exit 0）**：3/3 泵 + 3/3 普通启动可复现；对照实验设计出色（原版 core → 存活+授权窗口；094f5401 → 0xC0000005 AV（与 T021 实测同型）；新候选 → exit 0）——exit 0 是**新候选引发的宿主引导期行为改变**，非调试附加、非 AV、非 FULL。Run 不可达，按票面 STOP。

**核心裁定 3 —— 本票首要目标达成（C-5 结构性关闭）**：dump 管线 `fix_hardcoded_addresses` 重锚 3500 指针 + 普查实证 0 会话指针残留 + 重锚指针落进 20 个当前会话模块（KERNEL32 115 / msvcrt 84 / USER32 28 / ntdll 21…）——**杀死 T019/T021 的 C-5 类在管线内根治**。KNOWN_ISSUES C-5 对"管线产出候选"路径记闭环；T0.4 旧候选（3650ea6c/094f5401）仍为会话绑定产物（历史工件，不改）。

**更正（不改 worker 原文，以此附注为准）**：
- **F1'（§0）**：授权令牌回抄中段乱码（"前置由总指挥`tools/…`�BootTime…"）——回抄不完整，规范令牌以 `tickets/TASK-022.md` §0 为准（令牌语义无争议：D-044 全文在档）。
- **F2'（§6.4）**：普通启动对照实验的原始日志（nondebug*.log 等）作为"临时文件"删除——对照实验是三态归因的承载证据之一，其原始记录仅存报告粘贴文本。缓解：泵 3/3 全量证据已入 vault（判定主证据不受影响）+ 现象确定性可复现（exit 0 ×3 / AV ×2）+ 094f5401 AV 与 T021 独立记录互证。记入 P 系教训：**承载归因的对照实验原始记录不属于临时文件**。
- **F3'（票面起草，责任在总指挥）**：票 §6.1 写"唯一授权脚本改动 = CAND_SHA"，§6.2 又要求新部署目录 `lab/xx21b_run_pcell2/`——后者必然要求脚本 `DEPLOY` 常量变更。worker 的 +6/−6（CAND_SHA + DEPLOY + 标签）为满足票面的最小连贯集合，判定合理、已披露。
- **S4 数值口径裁定**：GetAppVersion 返回 `base+0x13c4c0`（.bss "1.1" 字符串地址），新基址下 32 位值 `0x57f6c4c0` 与 T0.4 `0x1db4c4c0` 为同语义不同基址——**口径裁定为"等价成立"**（票面 §5.1 已预告基址必变）。

**21 节 vs T0.4 23 节**：总指挥独立解析节表——差异 = T0.4 尾部多拆的 .import/.edata 2 个重建节；实质节（.text/.bss/.winlice/.boot/.rsrc/.idata/.tls/.reloc/.pdata）全在，S1 R0B 12/12 + S3 6/6 加载佐证。裁定：可接受，不要求与 T0.4 节布局逐节一致。

**审计结论**：任务 1 PASS、任务 2 PASS（普查门实质 PASS + 工具 v2 闭环）、任务 3 STOP 接受（C-9 新开）。**账本 XC-XXI-B 14/4 确认**。停止规则触发提示：T0.5 FULL 验收标准已连续 2 单不通过（T021 AV / T022 exit 0），按 AGENTS.md 停止规则，下一步（C-9 根因诊断 1 格 vs 战役收官归档）交老板裁决，总指挥不自行派单。
