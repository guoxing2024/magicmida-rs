# TASK-010 — 调查 C-6：重脱壳 text-poll AV 风暴与 debuggee 基址分配差异的因果链（只读）

- **优先级**：P1
- **状态**：📋 待领取
- **岗位**：qa（只读调查，**禁改任何生产代码**）
- **授权**：总指挥 2026-08-29 开单（TASK-006R 收口时裁决：路径 a 调查优先于继续烧格重试）
- **账本**：**零实弹**——本单全部为离线/只读分析（读既有日志、vault 证据、静态分析壳逻辑）。如调查过程中发现必须实弹（如需新 trace 才能定性）→ STOP 上报，另行授权。

## 项目背景

1. TASK-006R（报告 `runs/20260829-TASK-006R.md`，归档 `D:/MidaVault/lab/evidence/xx21b_006r/`）实弹 9 次重脱壳全部 BLOCKED：text-poll 阶段 AV 风暴（exc 恒 `0x7ffa95400bd8` = ntdll+0x160bd8，单次 20 万–300 万次），`.text` 永不 stable，dump 从未到达。TASK-009 修复的实弹验证被阻塞（C-6）。
2. 关键差异：本时段（21:18-21:56）debuggee image_base 恒 `0x7ff799fc0000`（9/9 确定性）；上次成功时段（04:0x，同一 boot、同一样品、同一命令）恒 `0x7ff6c0c60000`（1/5 成功 + 4/5 随机失败）。样品 preferred ImageBase = `0x140000000`。
3. 待定性的问题：**image_base 差异是 AV 风暴的因、果，还是共同第三因素（如 ASLR 池状态/内存布局熵）的表象？** Themida 壳的反调试逻辑是否依赖运行时映像基址的内部一致性？
4. 本单结论直接决定下一步走哪条路：(a) 基址差异是因 → 复现"收敛友好"布局（如重启后）再烧格；(b) 时序竞争为主 → 换收敛策略；(c) 引擎侧缺陷 → 开修复工单。

## 输入（全部只读）

| 材料 | 位置 |
|---|---|
| 本次失败证据 | `D:/MidaVault/lab/evidence/xx21b_006r/`（attempts 记录、baseline、build_log；巨大风暴日志已被 worker 清理，不在盘上） |
| 上次成功/失败记录 | `runs/20260829-TASK-006.md`、`lab/xx21b_resume/`（旧产物与 sidecar） |
| C-6 条目 | `docs/KNOWN_ISSUES.md` C-6 |
| 壳静态分析入口 | `lab/xx21_step1_static_out.json`、`lab/xx21_step1_static_deep_out.json`（既有静态分析产物，只读） |
| 引擎 text-poll 逻辑 | `crates/` 下 debug loop / text-poll 相关代码（**只读**，理解 30s idle 判定与 AV 事件转发路径） |

## 你要改的文件

**零。** 只写调查报告 `runs/<日期>-TASK-010.md`。禁改 `crates/`、禁改 `docs/`（总指挥验收后统一入册）、禁 git 写操作。

## 任务目标（一句话可观察的变化）

给出 C-6 的因果链定性结论——基址差异与 AV 风暴的关系是 (a) 因 / (b) 果 / (c) 共因表象 / (d) 证据不足无法定性——并附可复核的推理链与（可能的）下一步建议。

## 具体要求

1. **ntdll+0x160bd8 定性**：这是什么代码？（用本地符号/反汇编工具只读分析 ntdll 该偏移的函数；如果机器上无合适工具，用静态知识 + 该偏移附近的特征推断，明确标注可信度。）为什么 debuggee 的 AV 会**恒定**落在这里？"exc=0x7ffa95400bd8"是故障指令地址还是访问目标？（报告口径两者混淆了——`AccessViolation { address: … }` 的 address 字段语义要查引擎代码确认。）
2. **image_base 分配机制**：Windows 对非 preferred-base 映像的 ASLR 分配为什么会从 `0x7ff6c0c60000` 区段漂到 `0x7ff799fc0000` 区段？（同一 boot 内两个时段。）列出可能的机制（ASLR 池耗尽状态、临时映射残留、进程创建参数差异等）并评估各自可能性。
3. **壳的反调试逻辑对基址的依赖**：从既有静态分析产物（step1 静态输出）+ `runs/20260829-TASK-006.md` 记录的壳行为，找 Themida 壳是否含有"映像基址自检/基址相关的解密密钥/基址相关性反调试"。**只读既有产物**，不要求新 trace。
4. **对照分析**：上次会话 4 次失败（同基址 `0x7ff6c0c60000`、随机失败）的失败模式与本次（基址漂移、确定性失败）是否同型？（读 TASK-006 报告里的失败记录对比 AV 模式。）
5. **结论与建议**：按 (a)-(d) 定性，给出下一步路线建议（含是否建议重启后重试的判断依据）。

## 验收标准

1. ntdll+0x160bd8 的定性结论（含工具/方法与可信度标注）。
2. `AccessViolation { address }` 字段语义从引擎代码（只读）确证的引用行。
3. 基址漂移机制的机制清单 + 各自可能性评估。
4. 壳基址依赖的证据或"未找到"的明确结论。
5. 最终定性 (a)-(d) + 下一步建议。
6. 全文逐条标 `[已验证]` / `[推断]` / `[存疑]`。

## 交付格式

`runs/<日期>-TASK-010.md`，调查报告，无原始输出要求（离线分析），但每个关键结论必须附推理链与出处（文件:行号或 vault 路径）。

## 停止规则

- 需要实弹/新 trace 才能推进 → STOP 上报。
- 需要改引擎代码才能验证假设 → STOP，写成新工单建议。
- 连续 2 小时无实质进展 → 停，报告已有发现。
