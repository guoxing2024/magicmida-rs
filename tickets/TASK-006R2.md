# TASK-006R2 — TASK-006R 二次复跑：一格实弹同时验缺陷 A 修复与 C-7 风暴终止

- **优先级**：P1
- **状态**：⏸ **等老板批 1 格实弹**（批了才能派；未批不许开跑）
- **岗位**：developer（**实弹**）
- **账本**：XC-XXI-B **3/4 → 4/4**（同路线的多次重试按 T0.5 先例仍计 1 格，但见下面的停止规则）
- **前置**：TASK-009 ✅ / TASK-011 ✅ / TASK-012 ✅（全部已验收，本地提交 `aabeae9`）

## 为什么这一格值得烧

TASK-006R 首跑（1 格）三关全过但**验证点不可达**：9/9 次重脱壳在 text-poll 阶段陷入恒同 AV 环，`.text` 永不 stable，dump 从未到达，TASK-009 的三个证据点 0 命中、无产物。TASK-010 只读调查定性为 (c)，并挖出真正的放大器 = **C-7 引擎缺口**（text-poll 阶段无风暴终止）。C-7 已由 TASK-011 修好、TASK-012 加固。

**这一格是一格两验**（两件事只能在同一次重脱壳里一起验，分不开）：
1. **缺陷 A（C-4）修复的实弹替换验证**：路径 A/B 二分。
2. **C-7 风暴终止的实弹观察**：如果又撞上恒同 AV 环，引擎现在应该**主动 fail-closed 中止**，而不是烧到外部超时。

## 红线（违反即整单作废）

- **样品身份**：`D:\Tools\RE\dumps\gto\启动器.exe` 是**可变定位符、不是身份**。先把字节解析到 vault 对象，与 `lab/cases/v2/*.json` 的 `protected_input` 比对；**不匹配 = `SampleIdentityMismatch`，立即 STOP，不许执行**。
- `NO_BYPASS=1` 全程；网络 `deny_all`；样品不外发。
- **git 只读**：不 commit / push / stash，不改 config。
- 样品、产物、dump、第三方工具、运行日志、构建输出**一律不进 Git**——进 vault（`D:/MidaVault/`）。
- **禁止伪造证据**。所有结论按 `[已验证]` / `[推断]` / `[存疑]` 标注。

## 第 0 关：构建核验（不过就 STOP，不许烧格）

必须先证明你手上的 exe 真的含三个修复，否则这一格会白烧在旧二进制上（这是 TASK-006R 加的门，继续保留并扩展）：

1. `git log --oneline -1` 的 HEAD 必须含 `aabeae9`（TASK-012 收尾提交）。
2. 在 MSVC 环境（`tools/_enter_msvc_env.cmd`，**Git Bash 的 link.exe 会遮蔽 MSVC 链接器，必须用这个**）构建 release。
3. 对构建出的 exe 做字符串命中，**四条全部必须命中**：
   - `zero-filled IAT region`（TASK-009 兜底清零）
   - `TASK-009 fail-closed`（TASK-009 fail-closed 门）
   - `guardless constant-AV storm abort`（C-7 host 腿）
   - `C-7: guardless constant-AV storm`（C-7 引擎侧告警）
   任一条不命中 → **STOP 上报**，不许开跑。
4. 贴构建日志与四条命中的原始输出，把 exe 的 sha256 与体积记进报告。

## 第 1 关：会话基线

记录并贴出：debuggee image_base、ntdll/kernel32 基址、boot 时间、ScyllaHide 注入日志路径。这是 TASK-010 用来做因果判断的关键上下文，每次都要留。

## 第 2 关：重脱壳 + 三条终态判定

跑重脱壳。**三种终态都合法，如实上报即可**：

- **路径 A（fail-closed 生效）**：dump 阶段命中 `TASK-009 fail-closed`，拒绝写出产物，`[GOOD] Candidate written` **不出现**。→ 这**不是失败**，是缺陷 A 修复按设计工作（IAT 重建不完整时不再 fail-open 出坏产物）。贴日志、记 Unresolved 计数。
- **路径 B（产物写出）**：`[GOOD] Candidate written` 出现 → 立刻做**当场存活探针**（P-4 新规：产物写完立即跑一次，非 0/259 即阻塞上报），再看 `.rdata 0x1137d0` 槽的字节是否已被清零/正确重建（这是缺陷 A 的原始现场）。
- **路径 C（C-7 中止）**：又撞上恒同 AV 环 → 引擎应打出 `guardless constant-AV storm abort` 并**主动中止**。这一路要记：从第一次 AV 到中止之间的**事件数**（期望 ≈1024）、耗时、**日志体积**（期望远小于 3.5GB），以及中止后有没有残留 debuggee 进程。

## 停止规则（**上次 worker 越界了，这次严格执行**）

- **连续 2 次失败即停**，写报告上报。TASK-006R 那次跑了 9 次——虽然如实披露了，但那是越界，且烧掉了大量时间。
- **不许为了走通路径 B 反复重跑刷结果**。
- 每次尝试都要记 image_base，便于跨时段对照。

## 日志留存（**上次的取证缺口，这次补上**）

TASK-006R 把 3.5GB/3.2GB 的风暴日志删了，导致 AV 计数**事后不可复核**。这次要求：

1. **删之前**先算好并记进报告：AV 事件总数、恒同元组及其出现次数、事件类型分布（`grep -c` 原始命令一并贴出）。
2. 保留一份**有界摘要**进 vault：头 200 行 + 尾 200 行 + 计数统计。
3. 大日志本体可以删（不许进 Git），但报告里要写清删了什么、体积多少。

## 交付物

- `runs/20260829-TASK-006R2.md`：第 0/1/2 关的原始输出、走的是哪条路径、三条证据点各自命中情况、C-7 中止的量化观察（若走路径 C）、停止规则遵守情况、日志留存清单、以及「我没做的事 / 我不确定的事」。
- vault 归档：构建日志、exe、会话基线、有界日志摘要（放 `D:/MidaVault/lab/evidence/xx21b_006r2/`）。
- 工作区留给总指挥，**不提交**。

## 验收标准（缺一条即打回）

1. 第 0 关四条字符串命中的原始输出 + HEAD 含 `aabeae9` 的证明。
2. 样品身份核验 PASS 的原始输出（哈希比对）。
3. 会话基线原始输出。
4. 重脱壳完整日志（有界摘要）+ 明确的终态判定（A / B / C 之一）+ 三条证据点的命中情况逐条列出。
5. 若路径 C：AV 事件数、到中止的耗时、日志体积、残留进程情况四项数字齐全。
6. 若路径 B：当场存活探针结果 + `0x1137d0` 槽字节。
7. 停止规则遵守证明（尝试次数 ≤2 次失败即停）。
8. 「我没做的事 / 我不确定的事」。
