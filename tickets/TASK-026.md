⛔ 实弹工单——授权令牌见下。未经总指挥在飞批准，本票不得开跑。

# TASK-026 — 变体组合深观测 + T0.5 三态尝试（战役最接近 FULL 的一票）

## 0. 授权令牌（原文回抄到报告第一节）

> `老板 · 2026-08-31 · 原话"A. TASK-026（1 格，16/4 → 17/4）"（按全案解释：批准 D-051 选项 A = TASK-026 变体组合（宿主 a852880a + 自举变体 7b470117 + config cde9be13）深观测 + T0.5 三态尝试——分钟级存活观测（C-10：窗口是否迟到）+ 泵下 RIP 采样（宿主存活态在做什么）+ CreateRemoteThread Run 触发 + 三态判定语义逐字沿用，实弹 1 格（深观测趟 + Run 触发趟含重试，按 T018 先例），账本 XC-XXI-B 16/4 → 17/4；BootTime 存在性硬门以票面为准）· 前置由总指挥亲验（2026-08-31）：HEAD = eb91329；BootTime = 2026-08-30 10:05:53.549 连续无重启（变体绑定本 boot）；变体 7b470117 在位且 EP 字段 = 0x8a0108、ImageBase = 0x7ffc57e30000、Run@0x1c120 prologue = 415741564155 明文；pcell harness 基座在`

## 1. 背景（先读 runs/20260831-TASK-025.md + D-051）

- C-9 已破解（变体 EP→原壳入口 0x8a0108，宿主不再 exit(0)）；**C-10**：宿主存活 30-60s+ 但无窗口（未达 T017 等价）。
- **关键旁证**：泵下 attach_changed_behavior=False、无 WinLicense 对话框 → **C-8 反调试未在变体组合触发 → 调试泵方法论与自举变体兼容**（对照 T018 原版 core + 泵必弹 WinLicense）——T0.5 三态方法论在本组合可用，这是本票成立的前提。
- 本票 = 战役最接近 FULL 的一票：结局开放——消息循环 = PARTIAL 重演（T0.4 Step1 同型）、urlmon 命中 = **FULL**、卡死/新阻塞 = C-10 定性、AV = STOP。全部如实上报。

## 2. 红线（全程）

- `NO_BYPASS=1`；样品 sha 不匹配即 STOP；样品不外发；禁止伪造证据；防火墙只读；git 只读；不新增依赖；`crates/` 一行未动。
- 诊断+观测 only：不许改宿主/候选/壳任何字节（变体 7b470117 原样使用）；不许清洗；不许绕过授权（授权流程是行为等价的一部分）。
- 工具改动仅限常量 + 最小观测扩展（见任务 1），全部如实披露 diff。
- **BootTime 存在性硬门**：变体重锚指针绑定 BootTime `2026-08-30 10:05:53.549`——开跑前与收尾前各查一次，变动即 STOP。

## 3. 任务 1：工具准备（常量 + 最小观测扩展）

1. `tools/xx21b_t05_ui_drive_pcell.py` 常量改动：`CAND_SHA` → 7b470117（变体）、`DEPLOY` → `lab/xx21b_boot`、标签；**基址硬门/run_head 明文预检/三态语义/`.winlice` 残留标注零改动**（变体 ImageBase = 0x7ffc57e30000 动态读；Run prologue 明文已预验）。
2. 最小观测扩展（授权，如实披露 diff）：**Run 触发前深观测段**——core 加载 + 基址硬门通过后，先观测 `OBS_SECONDS`（默认 180s，可参数覆盖）：每 5s 窗口枚举（EnumWindows/FindWindowW PigToGoLicenseDialog）+ 每 2s 全线程 RIP 采样（调试句柄，CONTEXT 0x100001）+ 每 30s 记录线程数/CPU 态；观测期满或窗口出现即进入 Run 触发段。观测数据全部入证据 JSON。
3. 语法自查 + selftest（EP 期望 = 0x8a0108 变体口径；注意 pcell harness 的 EP 相关自测若引用 0x1027c0 需同步为变体口径——属常量口径改动，披露）。

## 4. 任务 2：深观测 + Run 触发（实弹，本格）

1. **深观测趟（≥2 趟）**：泵 + 变体组合，只观测不触发 Run——回答 C-10：180s 内窗口是否出现；宿主线程 RIP 分布（VM 区旋转 / 消息循环等待 / idle）；线程数变化。**若窗口出现 → 立即记录（C-10 破解）并继续 Run 触发段 + UI 驱动**（T017 窗口电池同款：BM_CLICK/WM_COMMAND，尊重"不输入真实注册码、不绕过授权"红线——只观测窗口出现与消息泵存活）。
2. **Run 触发趟（≥2 趟）**：深观测段结束后 CreateRemoteThread Run（参数 = 宿主映像基址，动态）——三态判定**语义与 T017/T018/T019/T021 逐字一致**：FULL = Run 线程 RIP 落入 urlmon.dll 模块区间 + 进程存活 + ≥2 趟可复现；新阻塞 = RIP 卡在新位置 → STOP；AV = EXCEPTION 或异常退出 → STOP；附加改变行为 → 如实上报 → STOP；urlmon IAT 槽（0x16f300 动态解析）前后值 + 页级变化全记录。
3. 泵健康自证逐趟入证据；无残留进程；临时文件逐个删除贴证明。

## 5. 验收标准（逐条对照附命令与原始输出）

1. BootTime 双查 = `2026-08-30 10:05:53.549`（变了即 STOP）。
2. 深观测趟 ≥2：C-10 观测数据（窗口/线程 RIP 分布/线程数）入证据；窗口出现与否如实记录。
3. Run 触发趟 ≥2：三态判定 + 证据（Run 线程 RIP 采样序列、urlmon 槽前后值、页级变化、泵健康）；FULL 需 ≥2 趟可复现；任何 STOP 路径如实上报。
4. 结局归类（PARTIAL 重演 / FULL / 新阻塞-C10 定性 / AV）+ 证据链；与 T0.4 Step1（老宿主+老候选消息循环 PARTIAL）对照说明。
5. 零越界：仅工具常量+观测扩展 + 复用 `lab/xx21b_boot/` + vault 证据；crates/ 零改动；变体/宿主字节零改动（sha 前后双验）。
6. vault 证据先行：`D:/MidaVault/lab/evidence/xx21b_t05/`（追加 TASK-026 节）或新建 `xx21b_threestate/`（INDEX.md 登记 sha）。
7. 报告 `runs/20260831-TASK-026.md`：令牌回抄第一节、结论按 [已验证]/[推断]/[存疑] 标注、"我没做的事/我不确定的事"不许留空。

---
*总指挥拟票 · 2026-08-31 · D-052 · 老板 2026-08-31 批选项 A · 串行纪律 D-014/D-026*
