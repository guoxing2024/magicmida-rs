⛔ 实弹工单——授权令牌见下。未经总指挥在飞批准，本票不得开跑。

# TASK-025 — C-9 最小自举实验：候选 EP 指回原壳入口（4 字节离线改动 + 实弹对照矩阵）

## 0. 授权令牌（原文回抄到报告第一节）

> `老板 · 2026-08-31 · 原话"A. 最小自举实验（1 格，15/4 → 16/4）"（按全案解释：批准 D-049 选项 A = TASK-025 最小自举实验——候选 EP 字段从 NOP stub 0x1027c0 改指原壳入口 0x8a0108（4 字节离线改动），实弹对照矩阵判定宿主是否越过 C-9 查询点，实弹 1 格（非附加对照 + 泵诊断趟含重试，按 T018 先例），账本 XC-XXI-B 15/4 → 16/4；三种预期结局 (a)(b)(c) 均为有效判定，BootTime 存在性硬门以票面为准）· 前置由总指挥亲验（2026-08-31）：HEAD = 21eccf7；BootTime = 2026-08-30 10:05:53.549 连续无重启（候选绑定本 boot）；候选 096f3bdf 在位且 EP 字段 = 0x1027c0（文件偏移 0xa8）；壳入口 0x8a0108 ∈ .boot [0x8a0000,0xdb1a00)；工具基座 tools/xx21b_c9_exit_trace.py 在`

## 1. 背景（先读 runs/20260831-TASK-024.md + D-049）

C-9 已收窄：宿主 Themida VM 区对沉睡 core（NOP stub DllMain）的查询失败 → CRT exit(0)（2.4ms，D-049）。本票实验 = **移除我们自己的固化阉割**：让候选 DllMain 恢复为原壳入口（原版 core 加载时本来就执行它，T017 同款加载路径），检验宿主是否因此走到授权对话框。三种预期结局**各自都是决定性发现**：
- **(a)** 非附加下宿主存活并出现 PigToGoLicenseDialog"授权验证" → **C-9 破解**（宿主查询通过）；
- **(b)** 壳反调试触发（WinLicense 对话框 / 泵下行为改变，C-8 同族）→ 证明调试泵方法论与活壳结构性冲突 → T0.5 FULL 语义裁决升级为老板议题（RIP 口径 vs 窗口证据口径，T017 建议②）；
- **(c)** 壳在重建映像上无法自举（AV/异常退出）→ 死路入档，C-9 以"候选不可自举"收官。

## 2. 红线（全程）

- `NO_BYPASS=1`；样品 sha 不匹配即 STOP；样品不外发；禁止伪造证据。
- 变体 = 096f3bdf 的 **4 字节**衍生（EP 字段 0x1027c0→0x8a0108，文件偏移 0xa8，小端）——**除这 4 字节外与 096f3bdf 逐字节一致**（交付 diff 证明）；NOP stub 遗留字节（0x1027c0 处 5 字节）保留不动（死代码，最小改动纪律）。
- 诊断 only：不许改宿主、不许改候选其它任何字节、不许清洗、不许碰 .winlice/.boot 内容。
- 防火墙只读；git 只读；不新增依赖（工具=既有 xx21b_c9_exit_trace.py 常量级改动）；`crates/` 一行未动。
- **BootTime 存在性硬门**：变体继承 096f3bdf 的重锚指针，绑定 BootTime `2026-08-30 10:05:53.549`——开跑前与收尾前各查一次，变动即 STOP（重启 = 变体作废 = 票作废）。

## 3. 任务 1（离线，0 格）：构建变体 + 自查

1. 复制 096f3bdf → `lab/xx21b_boot/core_shell_bootstrap_variant.dll`，仅改文件偏移 0xa8 起 4 字节（EP 字段 0x1027c0 → 0x8a0108；0x8a0108 小端 = `08 01 8a 00`——**不许手抄字节：用 struct.pack('<I', 0x8a0108) 生成写入，写后独立回读 PE 头校验**）。
2. diff 证明：变体 vs 096f3bdf **恰 4 字节差异**（偏移 0xa8-0xab）；独立 PE 解析 EP 字段 = 0x8a0108；其余全等。
3. 登记 manifest（variant_of / change / 新 sha / 用途）入 lab 与 vault。
4. 部署 `lab/xx21b_boot/`：宿主 a852880a + 变体 core.dll + config cde9be13（三 sha fail-closed）。

## 4. 任务 2（实弹，本格）：对照矩阵

1. **工具授权改动**：`tools/xx21b_c9_exit_trace.py` 仅常量（DEPLOY → `lab/xx21b_boot/`、CAND_SHA → 变体 sha、标签），最小 diff 如实披露。
2. **非附加对照（决定性观测，≥2 趟）**：普通启动（`Start-Process`，NO_BYPASS=1，无调试器）——观测窗口清单（PigToGoLicenseDialog"授权验证" / WinLicense / 无窗）、退出码、+30s 存活判定。**结局 (a)/(b)/(c) 的主判据**。
3. **泵诊断趟（≥2 趟）**：`xx21b_c9_exit_trace.py` 全断点（退出漏斗 4 + 候选 EP = 现在自动指向壳入口 0x8a0108）——记录：壳入口是否被调用、壳自举期间漏斗/异常活动、与 T023 基线（exit 0 / 无 AV / 无窗口）的偏离、attach_changed_behavior。
4. **结局分类**：按 (a)/(b)/(c) 如实归类；出现票面未预期行为 → 如实上报不硬凑。
5. 泵健康自证逐趟入证据；无残留进程；临时文件逐个删除贴证明。

## 5. 验收标准（逐条对照附命令与原始输出）

1. BootTime 双查 = `2026-08-30 10:05:53.549`（变了即 STOP）。
2. 变体 4 字节 diff 证明 + EP 字段独立解析 = 0x8a0108 + manifest 登记。
3. 非附加对照 ≥2 趟：窗口清单 + 退出码 + 存活判定（主判据）。
4. 泵诊断趟 ≥2 趟：壳入口命中记录 + 漏斗/异常 + 与 T023 基线偏离 + attach_changed_behavior。
5. 结局归类 (a)/(b)/(c) + 证据链；C-9 判定更新（破解 / 方法论冲突 / 死路）。
6. 零越界：仅变体新文件 + 工具常量改动 + 新部署目录 + vault 证据；crates/ 零改动；既有脚本零改动。
7. vault 证据先行：`D:/MidaVault/lab/evidence/xx21b_boot/`（变体 + manifest + 全部趟证据 + INDEX.md 登记 sha）。
8. 报告 `runs/20260831-TASK-025.md`：令牌回抄第一节、结论按 [已验证]/[推断]/[存疑] 标注、"我没做的事/我不确定的事"不许留空。

---
*总指挥拟票 · 2026-08-31 · D-050 · 老板 2026-08-31 批选项 A · 串行纪律 D-014/D-026*
