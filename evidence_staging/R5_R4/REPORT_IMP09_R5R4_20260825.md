# IMP-09-CARRIER-R5-R4 — Execution Report (EXECUTION REPORT)

Work order: WORK_ORDER_IMP-09-CARRIER-R5-R4-TEARDOWN_20260825.md
Branch: codex/imp09-carrier-r5-r2
Baseline HEAD: 4c8661f04bfdcc0ca14d3499aeb5b0d825a89c10 (R5-R3, audited PASS 242/0)
Executed by: this session (work-order executing worker)
Date: 2026-08-25

---

## 1. Status declaration (work order section 4.5)

```ini
offline_mock      = true
live_authorized   = false
protected_sample  = NOT_AUTHORIZED
```

This delivery is PURE OFFLINE implementation and verification: every
production teardown path is driven in-process by unit tests (real
`VirtualFreeEx` against the own-process handle for the success paths, an
injectable `FreeBackend` failure seam — exactly like `CleanupBackend` — for
the failure ledger). The injectable backend is a dependency-injection seam,
NEVER a production-path substitute. No live Windows PASS.

## 2. Deliverables

1. Production code + tests (positive + negative >= 4):
   - crates/cli/src/unpacker/walker_teardown.rs (new, 805 lines):
     `TeardownFreeEvent` / `TeardownOutcome` / `WalkerTeardownReport` /
     `TeardownLedger` / `FreeBackend` + `Win32FreeBackend` /
     `teardown_walker_allocations` / `teardown_walker_session_report`
     + 12 tests (module imp09_r5r4_teardown_tests).
   - crates/cli/src/unpacker/antidebug_controller.rs: `WalkerTeardownGuard`
     now runs the structured teardown and stashes the report;
     `teardown_walker_session()` returns the report;
     `WalkerEvidenceRecord.teardown` carries the report into evidence;
     5 new controller tests.
   - crates/cli/src/unpacker/mod.rs: `pub mod walker_teardown;`.
2. Design doc (release order + R5-R2 guard ledger relation diagram):
   docs/IMP09_CARRIER_R5_R4_TEARDOWN_DESIGN_20260825.md
3. Raw evidence set (simplified): evidence_staging/R5_R4/
   (manifest + raw test outputs + exit codes + source hashes).
4. This report (actual exit-gate values in section 3).

## 3. Exit gates (work order section 5)

```ini
R5_R4_TEARDOWN = PROVEN
R5_R4_EVENT_LEDGER = PROVEN
R5_R4_IDEMPOTENCY = PROVEN
R5_R4_ABORT_PATH = PROVEN
R5_R4_NO_LEAK = PROVEN
R5_R3_GATES_UNCHANGED = true
LIVE_AUTHORIZED = false
```

### Proof per gate

| Gate | Evidence |
|---|---|
| R5_R4_TEARDOWN | Production teardown path (params then section, real `VirtualFreeEx` + `GetLastError` capture); `teardown_walker_session_report` wired into `WalkerTeardownGuard::drop`; `r5r4_teardown_production_releases_both_and_ledger_zeroed`, `r5r4_teardown_walker_allocations_production_wrapper`, controller `imp09_r5r4_guard_records_structured_teardown_after_proceed` |
| R5_R4_EVENT_LEDGER | Every free step recorded `(sequence, address, size, free_type, ok, last_error)` (T2); exportable via `WalkerTeardownReport.events` (PartiallyReleased/Failed included); `r5r4_free_failure_is_partially_released_with_exported_steps`, `r5r4_first_free_failure_is_failed_with_step`, `r5r4_evidence_record_roundtrips`, `imp09_r5r4_explicit_teardown_is_idempotent_and_report_exportable` |
| R5_R4_IDEMPOTENCY | Second release of the same allocation refused + recorded (T3); second teardown run issues NO free; `r5r4_double_free_rejected_and_recorded`, `r5r4_ledger_inconsistency_detected`, `r5r4_teardown_idempotent_second_run_frees_nothing` |
| R5_R4_ABORT_PATH | ABORTED session (execute gate fails closed) exits run() through the same guard — allocations freed, report recorded; `r5r4_teardown_aborted_path_releases_all_and_ledger_zeroed` + controller `imp09_r5r4_guard_records_teardown_after_abort_failure` + `imp09_r5r4_early_failure_records_no_session_teardown` |
| R5_R4_NO_LEAK | T5: guard ledger empty after normal / failure / abort paths; `ledger_empty` flag asserted in all three; `r5r4_teardown_production_releases_both_and_ledger_zeroed` (region_is_free via VirtualQuery), controller tests assert `report.ledger_empty` |
| R5_R3_GATES_UNCHANGED | R5-R3 gates all re-run green (imp09_r5r3_* 3 tests); R5-R2 gates green (imp09_r5r2_* 6 tests); walker_session 43 green single-threaded; runner_preflight.rs untouched; same 3 environmental baseline failures before/after; teardown reuses R5-R2 allocation accounting (no second ledger) |
| LIVE_AUTHORIZED | false (no live dispatch; walker_dispatch stays None/mock) |

## 4. Test statistics

### New tests (17, all passing)

- walker_teardown module (12):
  r5r4_release_order_is_params_then_section,
  r5r4_teardown_production_releases_both_and_ledger_zeroed,
  r5r4_teardown_idempotent_second_run_frees_nothing,
  r5r4_teardown_aborted_path_releases_all_and_ledger_zeroed,
  r5r4_free_failure_is_partially_released_with_exported_steps,
  r5r4_first_free_failure_is_failed_with_step,
  r5r4_double_free_rejected_and_recorded,
  r5r4_ledger_inconsistency_detected,
  r5r4_teardown_failure_does_not_affect_output_consumption,
  r5r4_teardown_walker_allocations_production_wrapper,
  r5r4_teardown_walker_allocations_with_none_is_released,
  r5r4_evidence_record_roundtrips.
- controller tests (5):
  imp09_r5r4_guard_records_structured_teardown_after_proceed,
  imp09_r5r4_guard_records_teardown_after_abort_failure,
  imp09_r5r4_early_failure_records_no_session_teardown,
  imp09_r5r4_teardown_failure_does_not_block_output_consumption,
  imp09_r5r4_explicit_teardown_is_idempotent_and_report_exportable.

### Negative gates (work order section 4.2, >= 4 — 5 delivered at module level + controller-level T1)

| Required | Tests |
|---|---|
| free 失败（injectable）→ PartiallyReleased | r5r4_free_failure_is_partially_released_with_exported_steps |
| double-free 拒绝 | r5r4_double_free_rejected_and_recorded + r5r4_ledger_inconsistency_detected |
| 账本不一致检测 | r5r4_ledger_inconsistency_detected |
| teardown 失败不影响已产出 output 的消费 | r5r4_teardown_failure_does_not_affect_output_consumption + controller imp09_r5r4_teardown_failure_does_not_block_output_consumption |
| 首步失败 → Failed{step,error} | r5r4_first_free_failure_is_failed_with_step |

### cargo test --workspace (final state)

- mida-cli --lib (--test-threads=1): 524 passed; 3 failed; 1 ignored.
  3 个失败均为既有环境性失败（与基线一致，非本单引入）：
  runner_preflight::tests::resolver_extended_path_prefix_cannot_bypass_sibling_boundary /
  resolver_rejects_symlink_into_subdirectory_escape /
  resolver_rejects_symlinked_sibling_escape
  （沙箱无符号链接创建特权；runner_preflight.rs 属禁止修改清单，未改动。）
- mida-antidebug-runtime: lib 87 + attestation 68 + proc_surfaces 34 +
  walker_protocol 26 + walker_protocol_section 27 = 242 all pass, 0 failed.
- walker_session (mida-cli lib): 43 tests all pass under --test-threads=1.
  并行运行时出现的 13 个失败是 walker 运行时进程全局单例的既有测试间互斥
  （INSTALL_LOCK / WALKER_TEST_LOCK 模式），基线同样存在，非本单引入。

## 5. cargo fmt -- --check (recorded separately, honest)

- Workspace level: NOT_PASS (exit code 1, pre-existing 169 diffs in
  NON-work-order files; the workspace was never fmt-clean at baseline).
- All 3 work-order-touched files are fmt-clean (rustfmt --edition 2021
  --config skip_children=true --check exit code 0):
  walker_teardown.rs / antidebug_controller.rs / mod.rs.
- Per the precise-file-list rule, non-work-order files were NOT reformatted
  (an accidental crate-wide format of runtime_loader.rs and
  tests/digest_authority.rs was fully reverted via git checkout).

## 6. Prohibited-list compliance (work order section 2)

- runner_preflight.rs NOT modified.
- round_flags extension layout NOT touched.
- R5-R2 lifecycle window order, candidate mapping proof / envelope /
  liveness / execute gates NOT relaxed.
- `VirtualFreeEx`/`GetLastError` teardown semantics unchanged; teardown
  REUSES the R5-R2 allocation accounting (WalkerSessionMemory.params_va /
  section1_va) — no second ledger.
- No live dispatch / CreateRemoteThread / WPM wiring.
- No mock masquerading as production: the injectable `FreeBackend` failure
  seam is DI only (CleanupBackend pattern); production uses `Win32FreeBackend`.
- No silent retry: first failure aborts the sequence, failures exported.
- Correction hard limit 1: delivered once, no correction round triggered.

## 7. Conclusion

All R5-R4 exit gates are PROVEN (section 3). The production teardown is now
observable: structured per-step `VirtualFreeEx` events with `GetLastError`
capture, `TeardownOutcome` reported separately from the walker execute
verdict (T1), double-free refusal with events (T3), ABORTED path through the
same teardown (T4), and no-leak ledger-zeroed assertions across normal /
failure / abort paths (T5). The report is carried into the walker evidence
sidecar. offline_mock=true / live_authorized=false /
protected_sample=NOT_AUTHORIZED; this work order produces no live acceptance
and may be submitted for R5-R4 review.
