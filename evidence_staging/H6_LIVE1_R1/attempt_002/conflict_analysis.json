{
  "schema_version": "mida.gto-h6-live1.conflict/v1",
  "status": "NOT_EXECUTED",
  "attempt_sequence": 2,
  "ledger": {
    "used": 1,
    "cap": 2,
    "remaining": 1,
    "note": "attempt_002 NOT consumed — no child spawned, no preflight-reject burned"
  },
  "conflict_type": "BLOCKING_STRUCTURAL_GAP",
  "decision": "STOP_AND_REPORT — no attempt run, no verdict fabricated",
  "summary": "At HEAD a9e310f the production walker dispatch bridge is NOT wired: both production AntidebugStageOptions construction sites (crates/cli/src/unpacker/mod.rs ~L791 CREATE_PROCESS path and ~L1227 post-attach path) pass walker_dispatch: None, so the controller execute gate deterministically returns NotImplemented and Proceed is blocked. The work order step 6 (real dispatch via walker_dispatch.rs bridge, dual-sealed cross-check) cannot be reached with this code. Wiring the bridge would be a code-semantic change, forbidden for attempt_002 without a new work order through the general audit.",
  "evidence": {
    "bridge_impl_file": "crates/cli/src/unpacker/walker_dispatch.rs",
    "bridge_impl_commit": "9b05abc (IMP-09-DISPATCH-BRIDGE: production target-side WalkerExecute bridge, audited PASS)",
    "design_doc": "docs/IMP09_DISPATCH_BRIDGE_DESIGN_20260825.md",
    "design_doc_quote": "The type is constructible and fully unit-tested offline (T1-T12), but it is NOT wired into any production path in this order: the two production AntidebugStageOptions construction sites (unpacker/mod.rs lines ~790 and ~1227) pass walker_dispatch: None, so the controller records + returns NOT_IMPLEMENTED at the execute gate (fail-closed). Live dispatch authorization is deferred to the LIVE order; the bridge here proves the dispatch MECHANICS offline, never a live Windows PASS.",
    "production_sites": [
      {
        "site": "crates/cli/src/unpacker/mod.rs",
        "line": 791,
        "path": "CREATE_PROCESS",
        "value": "walker_dispatch: None"
      },
      {
        "site": "crates/cli/src/unpacker/mod.rs",
        "line": 1227,
        "path": "post-attach",
        "value": "walker_dispatch: None"
      }
    ],
    "controller_gate": {
      "file": "crates/cli/src/unpacker/antidebug_controller.rs",
      "function": "execute_walker_production",
      "behavior": "walker_dispatch None -> WalkerExecuteOutcome::NotImplemented -> Proceed blocked (fail-closed)"
    },
    "loader_capability": {
      "run_runtime_loader": "exists and injects runtime + resolves MidaExportsV2 remote (resolve_mida_exports_remote)",
      "walker_export_rva": "resolve_walker_export_rva_from_file exists",
      "bridge_constructor_inputs_available": true
    },
    "auth_var": "MIDA_GTO_LIVE_AUTHORIZED=1: no code reads it anywhere (grep across crates/, docs/, tools/ found only the work order text itself). The work order instructs setting it in a single command window with evidence, but no gate consumes it — so even a full env setup cannot unlock dispatch.",
    "observation_only": "attempt_001 ran with MIDA_GTO_OBSERVATION_ONLY=1 (research channel, no runtime injection, no walker, no dispatch) — it could never reach step 6. The ledger shows used=1 per handoff; the gap is in the code, not the attempt history."
  },
  "criteria_check": {
    "LIVE_PASS": "NOT EVALUATED (cannot reach step 6; no dispatch occurs)",
    "LIVE_FAIL": "NOT DECLARED — no attempt executed; FAIL is only for an executed attempt that exits/errors",
    "ledger": "used=1/2 unchanged; attempt_002 NOT consumed"
  },
  "remediation": "Requires a new work order through the general audit (总审计) authorizing the one-line wiring of WalkerDispatchBridgeImpl into both production AntidebugStageOptions sites (or an explicit LIVE wiring gate behind MIDA_GTO_LIVE_AUTHORIZED), plus a follow-up attempt_002 execution.",
  "created_utc": "2026-08-25T17:14:27.910Z"
}
