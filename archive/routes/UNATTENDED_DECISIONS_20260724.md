# Unattended Decisions Lock — 2026-07-24

Operator answers (binding for this unattended run). No push; local commits only.

| # | Decision | Choice |
|---|----------|--------|
| D1 | Completion bar | **Perfect unpack 1.0** (structure + load + behavior + repro + multi-family) |
| D2 | B-B / VNEXT-BEH | **Authorized** only with vault real-sample behavior evidence (**≥ Origin**); synthetic alone insufficient |
| D3 | Pure default | **Origin only** defaults to pure-rebuild; other samples remain legacy |
| D4 | GTO depth | **Independent GTO host** (must not depend on ThemidaState main path) |
| D5 | ScyllaHide x86 | Search disk; fill real hashes if found; else residual (not invented) |
| D6 | Oreans repro | Origin + Lunlun + holdout **3× each** engineering harden (not historical R3 10× re-claim) |
| D7 | Behavior probe scope | Origin + Lunlun + holdout + GTO |
| D8 | Permissions | Auto **commit**; **no push**; no CI/remote |

## Run-control answers (2026-07-24, all A)

| # | Decision | Choice |
|---|----------|--------|
| Q1 | Partial B-B failure | **Stop**: no VNEXT-BEH / no 1.0 claim; write fail audit + residual; commit engineering progress |
| Q2 | Fix-loop cap | **Max 2 rounds** of targeted fix (code + rebuild + re-run failed cases), then residual stop |
| Q3 | Origin pure live fail | **No 1.0 claim**; keep pure-default code + residual; `--no-pure-rebuild` for contrast only |
| Q4 | Holdout identity | **`xiongxiong_duokai`** |
| Q5 | Commit grain | **1–2 functional commits** (GTO/pure/BB tools + audit docs); no push |
| Q6 | Untracked `tools/_*` | Commit **only this-round essentials** (`_behavior_bb_gate.py`, `_behavior_probe.py`); leave scratch untracked |
| Q7 | Public claim bar | **1.0 / VNEXT-BEH only if all 4 B-B cases green + evidence bind**; else engineering complete + residual |

## Derived work order

1. Truth lock (this file) + residual honesty  
2. Scylla x86 search  
3. Oreans 3× harden  
4. Origin-only pure default (CLI + docs)  
5. Independent GTO host path  
6. Vault `load_no_crash_v0` (or successor) probes + bind to R0B candidates  
7. Scheduled B-B → `validation_summary` task **VNEXT-BEH** only if all D2/D7 Pass bind  
8. Audit package refresh  

## Non-claims until gates green

- Do not claim 1.0 before VNEXT-BEH + GTO host independence + 3× harden evidence.  
- GTO experimental profile may remain for dump stages until host independence lands.  
- Pure flip is **not** global.
