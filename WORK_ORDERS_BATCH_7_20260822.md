# 工作单批次 7 — 执行驱动覆盖率实验(总指挥签发,owner 选项 A)

**签发人**: 项目总指挥 · 2026-08-22
**授权**: `docs/GTO_H5_LIVE_AUTHORIZATION_3_20260822.md`(已签发)
**账本**: GTO-H5-LIVE-3 · cap=2 / used=0

---

## WO-701(P0)覆盖率测量设计说明(离线,零代码)

产出 `docs/GTO_H5_LIVE3_COVERAGE_DESIGN.md`,在授权 §二.3 框架内细化:

1. **条带扫描实现口径**:64KB 步长 × 4KB 样本的偏移表(确定性生成,写入设计文档);
   RPM 读失败页的处理(记 `unreadable`,不计入分母还是计入?给出并论证选择);
2. **扫描时点触发器**:t0 / 主窗口出现(复用现有 UI-window watch)/ +60s / +180s / 结束;
   每次扫描的预计耗时预算(RPM ~710 点是否 <5s,超时怎么办);
3. **交互协议**:窗口激活/前台化的具体消息序列;禁止项重申;
4. **dump 决策规则实现**:≥60% 覆盖率判定的精确计算式与数据来源(B 相末次扫描);
5. **fail-closed 表**:目标中途 AV/退出、扫描读失败率过高、窗口永不出现;
6. 离线单测方案(覆盖率计算纯函数、条带偏移生成确定性、决策规则三分支)。

**验收**: 零代码;总指挥批准后才进 WO-702。

## WO-702(P1,门禁于 WO-701 批准)覆盖测量实现 + 离线验证

按批准的设计实现(预计:`crates/pe/src/dumper/` 新模块 + `dump_process` 分支 +
CLI `--coverage-measure` 或复用 dump-timing 机制扩展):

- 双相采样(A 相锚点连续流 + B 相条带扫描)→ 时间线/快照侧车全量落盘(**无论结局**,A2 惯例);
- 覆盖率计算为纯函数(可离线测);dump 决策 fail-closed;
- T 系列离线测试全绿 + 全量 workspace ≥2272/0;
- CLI 门禁沿用授权变量模式(如 `MIDA_GTO_LIVE3_AUTHORIZED=1`),manifest 记审计凭证;
- **停止点**:diff 摘要报总指挥预实弹复核。

## WO-703(P1,门禁于 WO-702 放行)LIVE-3 Round 1 实弹

严格按授权文件 §二 执行:身份预检 → 观察宿主运行受保护输入 → 双相测量 →
dump 决策规则 → 报告 `docs/GTO_H5_LIVE3_R1_REPORT.md`(时间线+覆盖率表+空间分布+
非声明段)→ 记账 used=1/2。**数据即交付,覆盖率高低不影响验收。**

## WO-704(P2,条件)Round 2 补测/dump

仅当满足授权 §三 条件时启动;方案先报批。

---

## 红线

继承授权文件 §四 全部;worker 不执行 push;MSVC 环境;fmt/hygiene/双 lane 三件套。

**签发**: 项目总指挥 · 2026-08-22
